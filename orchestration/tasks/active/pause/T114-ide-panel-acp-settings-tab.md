<!-- T114 — IDE-панель, вкладка ACP settings. Агент не назначен. -->

## ЗАДАНИЕ — Shell-IDE правая панель: вкладка ACP settings

**Контекст:** фундамент таб-контейнера принят (T112, коммит `0e10e51`,
`orchestration/tasks/done/T112-ide-panel-tab-container.md`). Сейчас
`PanelTab::AcpSettings` рендерит заглушку "ACP settings — coming soon".
Эта задача даёт вкладке реальное содержимое — список/управление ACP-
бэкендами (Hermes и будущие).

**Референс:** `crates/services/src/hermes_acp/registry.rs` — уже есть
`AgentDescriptor { id, display_name, config }` и `known_agents() -> Vec<AgentDescriptor>`
(сейчас только `"hermes"`, честно закомментировано — только проверенные
рукопожатием ACP-бэкенды). `crates/services/src/hermes_acp/client.rs` /
`session.rs` — живое ACP-соединение, уже используется левой agent-панелью
(T107-T109, `side_panel_left`).

## Что делать

1. Разберись, что реально доступно для отображения на этой вкладке
   СЕЙЧАС без нового бэкенда: список `known_agents()`, статус подключения
   каждого (если такое состояние уже трекается где-то в `hermes_acp` —
   найди, не выдумывай), режимы/модели (T108 уже вытащил "real
   modes/models from ACP session response" — глянь `orchestration/tasks/
   done/` или грепни коммит по T108 item #6 в `MIGRATION.md`/`git log`,
   переиспользуй тот же путь получения данных, не дублируй).
2. Если реального состояния подключения не существует как читаемого
   значения (просто нет такого API) — не изобретай фиктивные
   заглушки/моки. Рендери то, что есть (статический список бэкендов из
   `known_agents()`), явно помечай в отчёте, чего не хватает для живого
   статуса (что нужно добавить в `hermes_acp`, чтобы это стало реальным
   в будущей задаче).
3. Это вкладка ПРАВОЙ панели, независимая от левой agent-панели
   (`side_panel_left`) — не путай зоны, не трогай `side_panel_left/*`.

## Зона файлов (не пересекается с параллельными T113/T115)

- `crates/app/src/side_panel_right/tab_acp_settings.rs` (новый)
- Можно читать (НЕ менять) `crates/services/src/hermes_acp/*` — если
  нужно новое публичное поле/метод для реального статуса, добавь МИНИМАЛЬНО
  и опиши в отчёте отдельно (это уже расширение публичного API сервиса,
  не только вкладки — архитектор должен это увидеть явно).

## Что НЕ делать

- НЕ трогай `crates/app/src/side_panel_right/view.rs`,
  `crates/app/src/side_panel_right/tabs.rs`,
  `crates/app/src/side_panel_right/mod.rs` — параллельно эти же файлы
  трогают другие T-задачи. Подключение вкладки в dispatch делает
  архитектор сам после приёмки.
- НЕ трогай `crates/app/src/side_panel_left/*` (левая agent-панель,
  чужая территория, T107-T109).
- Не выдумывай данные там, где реального источника нет — честный
  "нет данных" лучше фиктивного статуса.

## Интерфейс

`pub fn render_acp_settings_tab(cx: &mut Context<SidePanelRightView>) -> impl IntoElement`
(сигнатура ориентировочная — зафиксируй точную в отчёте).

## Верификация

Живой смок обязателен (release + `RUST_LOG=info`,
`CHRONOS_SMOKE_SIDE_PANEL=1`): вкладка открывается, показывает реальный
список из `known_agents()` (минимум "Hermes"), не крашится. Скрин
вкладки.

## Отчёт

`orchestration/tasks/report/T114-ide-panel-acp-settings-tab-report.md` —
точная сигнатура, что реально показано vs что осталось заглушкой и
почему, живой смок со скрином. Честно про PENDING при неудаче.
