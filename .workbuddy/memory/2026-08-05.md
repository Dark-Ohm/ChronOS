# 2026-08-05

## T254 ydotool/uinput блокер — живая диагностика (20:3x)

Пользователь прислал запрос «investigate this error: [Error message]» со
ссылкой на `docs/orchestration/tasks/active/T254-resume-capture-backlog-blocked-on-input.md`.
Реального текста ошибки в запросе НЕ было — placeholder не заполнен.
Единственный конкретный контекст ошибки = блокер T254 (ydotool/uinput
синтетический ввод для досъёмки 8 поверхностей брифа T223).

### Что проверил (live)
- `ydotoold` процесс жив: PID 813, parent = systemd --user (PID 773).
- systemd-юнит называется `ydotool.service` (НЕ `ydotoold.service`):
  loaded / active / running / enabled. `ydotoold.service` не существует —
  это была моя опечатка в первой проверке, не реальная проблема.
- `/dev/uinput` доступен: `crw-rw----+ root input`, ACL даёт
  `user:neo:rw-`. Модуль `uinput` загружен (modprobe OK).
- `ydotool mousemove --absolute 500 500` + `ydotool click 0xC0` → exit 0,
  синтетический клик реально эмиттится.
- ChronOS жив: PID 1894 `target/release/chronos` на tty1 (старт 20:25).

### Вывод
Блокер T254 функционально СНЯТ и подтверждён живой проверкой — совпадает
со статусом в самом файле задачи (2026-08-05). Код-фикс не требуется.
Реальной ошибки для «find the cause and fix it» не предъявлено.

### Открытый вопрос пользователю
Запрос пришёл с незаполненным placeholder `[Error message]`. Если имелась
в виду конкретная текущая ошибка (конкретная поверхность не кликается,
ошибка сборки/рантайм-паника, отсутствующая IPC-команда) — нужен её
реальный текст, иначе «причину и фикс» невозможно привязать.

### 20:34 — пользователь подтвердил: речь шла о блокере T254
Интерпретация верна. Блокер подтверждён снятым живой проверкой; код-фикс
не требовался (причина — kernel-module mismatch, решён ребутом). Внёс
подтверждающую запись в статус T254. Остаётся backlog досъёмки 7
поверхностей — отдельная работа, вне этого трекинг-тикета.

### 20:37 — переезд файла T254
Пользователь указал, что отчёт должен лежать в
`docs/orchestration/tasks/report/`, а не `active/`. Перенёс
`T254-resume-capture-backlog-blocked-on-input.md` туда через `git mv`
(файл был под трекингом; история сохранена). Явных ссылок на старый путь
в docs нет (токен T254 упоминается в прозе T253/T245/T256 и
report-log/T253-T254 — но это ID, не путь, править не нужно).

## T252/T253 — аудит empty-state + статус пересъёмки (21:1x)

Пользователь прислал «выполняй Т252-Т253».

### T252 (архитектурный аудит empty-state) — CLOSED (decision ratified)
- Пройден чек-лист по всем `crates/app/src/side_panel_right/tab/*.rs`.
  Все вкладки уже честно покрывают пустые/малые состояния.
- Зафиксирован паттерн из 6 классов отсутствия (целая вкладка пуста /
  под-секция пуста / async loading / error / усечение / не реализовано) +
  правило честности §13 + техника `min_h`-пола карточки (T249).
- Решение записано в `docs/DECISIONS.log` (`## 2026-08-05 — T252: …`) и
  в тикете `docs/orchestration/tasks/active/T252-empty-state-pattern-audit.md`.
- Код НЕ материализован намеренно (тикет велит «сначала решение»): случаи
  отсутствия осознанно разные. Кандидат `muted_note`-хелпер вынесен в
  отдельную задачу (не блокирует T252).

### T253 (пересъёмка System-таба) — DISPATCHED, blocker на живую съёмку
- T256 ЗАКРЫТ (commits 897c3d2 + 1ac6dd4): хедер берёт реальный заголовок
  активного окна Hyprland, фолбэк «Desktop», класс не подставляется
  (regression-гварды в `header.rs`).
- `/tmp/t253-live/` потерян — живые кадры не перенести, нужна свежая
  пересъёмка.
- Агент НЕ может переснять: headless-песочница без Hyprland/Wayland/граба.
  Живая съёмка (T233: `select-tab:system` + кроп по `hyprctl layers`) —
  только на реальном ПК архитектора.
- Статус T253 обновлён: остаётся свежая пересъёмка (обе темы →
  `notes/T253-*`) + финальный вердикт теста первого кадра + холодная
  vision-приёмка — всё на живом ПК.
- Греп по `side_panel_right`: фейковых маркеров (kitty/WINDOW_TITLE/
  permission-mock) не осталось, кроме regression-гвардов в тестах.
