# 2026-08-16

## Checkpoint #34 — T265-H v1 + kitchen (Grok)

- T265-H owner live PASS; epic T265 → `done/`. Super=Start, Super+Space=OSD.
- Per-repo kitchen `ChronOS/.chronos-ops/` in git; ecosystem kitchen separate.
- Queue still `docs/orchestration/` until per-ticket move. Don't batch-mv.
- T271 still needed (ipc swallows); no `log_err` in chronos crate.
- T298 parallel: T271 or T284-without-left. Not T266.
- T287-B accepted other architect. Don't touch dirty `composer.rs`.

## T296 — Display tab moved to right rail (DONE)
- `LeftTab::Display` removed from left panel (`side_panel_left/tabs/mod.rs`
  enum/PRIMARY_TABS/widths/labels/icons + tests; `workspace_view.rs` `display`
  field, `ensure_display`, render match arm). Left rail is now
  Project/Sessions/Chat/Plan/Tools/Skills/ContextFiles + Archive.
- Display content moved `side_panel_left/tabs/display.rs` →
  `side_panel_right/tab/display.rs` (brightness + wallpaper, unchanged).
- `PanelTab::Display` added: `ALL.len()==18`, `for_mode` both modes place it
  just above `EditorSettings` (bottom group), id `display`, icon
  `icons/rail-display.svg`, width 440, not resizable. `TabContent::Display`
  + `view.rs` render/entity_id match arms.
- `panels_config`: `default_*_bottom = ["display","editor_settings"]`.
  Живой `panels.toml` уже bottom. Sanitize **без** display в toml
  дописывает в **top** (оговорка приёмки, не блокер).

## Checkpoint #15
HEAD `b2320fa`. T288 T289 T291+E T290(попап) T290-E T296 DONE.
Дальше T285 или T292. Слева ИИ / справа ОС.
- Bar `system` hexagon widget DELETED (file + `mod.rs`/`instantiate`/
  `BUILTIN_NAMES`/`Default::right`). Display now reached only via right rail.
- Verified: `cargo test -p chronos --lib` (476 pass), `cargo build -p chronos`
  clean. Commit message: `fix(right-panel): Display tab lives on the right rail (T296)`.

## Checkpoint #17

T285 stays open. Live: `session/load OK` then `no active session for load`. Brief rewritten BACKEND `dc6dc38`. Stop for the day after this checkpoint.

## T292 accepted `92786c5` (architect)

Live `+`. Errata: `migration_idempotent` bins assert 1→0. Caveat: tautological rail click test; no tooltip. Next T285.

## T292 — workspace-mode moved from bar to right rail (DONE)
- New icons `icons/gamepad.svg` (Gamer) + `icons/mode-daily.svg` (Developer,
  coffee) registered in `assets.rs`; `WorkspaceMode::icon_path()` updated
  (kills T159 TODO; was rail-editor/bolt).
- `side_panel_right/rail.rs`: added `render_mode_button` (above dock toggle,
  NOT a PanelTab) + `render_mode_prompt` inline row (Да/Нет/Не спрашивать)
  shown while `workspace_mode::pending`. Click toggles Developer⇄Gamer; when
  pending, click is no-op (row handles it). Light accent tint, distinct from
  active-tab accent bar.
- Bar widget `workspace_mode` DELETED: file, `bar/widgets/mod.rs`,
  `instantiate`, `BUILTIN_NAMES`, default `right`, `right_widget_group` arm.
  `bar/mod.rs` + `layout_config.rs` tests updated (5 clusters, no
  workspace_mode in inventory/defaults). Migration tests retargeted to
  `keyboard_layout`.
- IPC `set-workspace-mode`/`toggle-workspace-mode` untouched (still live).
- Verified: `cargo test -p chronos --lib` (478 pass), `cargo build --release
  -p chronos` clean. Commit: `feat(right-rail): workspace mode switch on the right rail (T292)`.

## T285 — load ACP session on restore, not create_session (DONE, code)
- `crates/app/src/side_panel_left/tabs/chat.rs` (zone per task).
- Added pure `connect_session_action(restored_acp_id, cwd) -> enum
  ConnectSessionAction { Load{acp_id,cwd}, Create }` (tested, 4 cases).
- `ChatTab::new` spawn now: after `HermesClient::new`, looks up active restored
  thread's `acp_session_id`+`cwd`; `Load` → `run_load_session(replay_into_chat=
  chat.messages.is_empty()=false at startup, fallback_cwd=Some(project))`;
  `Create` → existing `create_session`.
- Extracted `run_load_session(client, acp_id, cwd, replay_into_chat,
  fallback_cwd, cx)` from `select_session` (shared replay consumer). When
  `replay_into_chat=false` the transcript is NOT mutated (binds session only —
  kills the "баннан twice" duplicate). `fallback_cwd=Some` makes a dead
  `load_session` (`Err`) fall back to `create_session` with explicit
  `warn "load_session failed, new session"`; SQLite transcript never wiped.
- `select_session` calls `run_load_session(..., replay_into_chat =
  chat.messages.is_empty(), fallback_cwd=None)` — Sessions-click path, silent
  error.
- Verified: `cargo test --lib -p chronos connect_action` 4 ok;
  `side_panel_left` 117 ok; `cargo build --release -p chronos` clean.
- Commit `f9cd9a2`. Report:
  `docs/orchestration/tasks/report/T285-acp-load-session-on-restore-report.md`.
- **Gate 8 live run NOT yet done** — needs shell restart on a project with a
  live thread: log shows `load_session replay complete`, no `create_session`
  after connect on this path, "что я просил запомнить?" returns this thread's
  word not Hindsight dump, transcript not duplicated.
- Next: live gate-8 run, then queue is open (T286/T287-A composer, T287-C chrome).

## Checkpoint #17
HEAD `f9cd9a2`. T285 code DONE; gate-8 live pending. Next: live run or T286.
