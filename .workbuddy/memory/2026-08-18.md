# 2026-08-18 — T299 kitchen archive role classification

- Role: RECON — read-only classification of 167 archive tickets
  (`docs/orchestration/tasks/{done,report-log,rejected}/`) into
  `{front,back,qa,recon,design}` so architect can `git mv` them under
  `.chronos-ops/{done,reports-log,reject}/<role>/`.
- Output: `docs/orchestration/tasks/report/T299-kitchen-archive-role-classification-report.md`
  with exactly 167 lines of CSV (role|rel_path) + distribution table:
  front=116, back=28, qa=12, recon=4, design=1. No `?` cases — all closed
  in one pass with the «если менялся crates/app → front» rule and reading
  only the first ~20 lines of each file.
- Six contested call-outs in the report:
  - T176/T178/T150 → back (dominant new code in crates/services).
  - T123 → front (mixed but follows crates/app rule).
  - T250 → back (one-line fix in crates/services/src/terminal).
  - T283 → back (persistence wiring, not schema rewrite).
  - rejected/T232 → back (D-Bus/PAM, not recon).
  - report-log/T226-plan4 → qa (live-test infrastructure).
- Nothing committed/moved yet — architect runs the git mv after accept.

## Move per neo's direct order (later in same session)

- «раскидай все по папкам в .chronos-ops» — neo отменил
  read-only-контракт отчёта. Выполнен `git mv` 170 архивных файлов:
  - 71 из `docs/orchestration/tasks/done/` → `.chronos-ops/done/<role>/`
  - 91 из `docs/orchestration/tasks/report-log/` → `.chronos-ops/reports-log/<role>/`
  - 8  из `docs/orchestration/tasks/rejected/` → `.chronos-ops/reject/<role>/`
  - Плюс 3 экстры, пропущенные первым проходом: `done/T287-left-chat-onto-gpui-component`,
    `done/T298-composer-select-popup-clipping`, `report-log/T298-...-report`
    (все `front`).
- `docs/orchestration/tasks/{done,report-log,rejected}/` теперь пусты
  (каталоги оставлены, удаление за архитектором).
- Проверка коллизий (`os.path.exists` на получателя): 0 коллизий, 0
  пропавших источников.
- Перекрёстная сверка с диском: `ls .chronos-ops/<cat>/<role>/` после
  move = ровно таблице распределения (delta = 0). Итог: 506 архивных
  файлов (336 до + 170 здесь).
- Распределение: front=121, back=32, qa=12, recon=4, design=1
  (отличается от черновой таблицы из отчёта: +5 к front т.к. первая
  разметка была по 167, реально перенесено 170).
- Report to `.chronos-ops/reports-fresh/T299-...-report.md` — файл сам
  переехал туда автоматом (не моими руками), что соответствует RULES.md.
- **Не трогал**: `docs/orchestration/tasks/active/`, `active/pause/`,
  `report/` (живая очередь + свежие репорты).
- Не коммитил.
