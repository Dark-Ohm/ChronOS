# Rust / GPUI build output
target/

# Nix build outputs
result*

# direnv
.direnv/

# Local reference checkouts (study material, not a dependency)
reference/

# Claude Code project state (settings, worktrees)
.claude/
System prompts/

# Subagent-driven-development scratch (progress ledger, task briefs/reports)
.superpowers/

# Git worktrees (isolated workspaces, not part of the repo)
.worktrees/
