# Rust / GPUI build output
target/
rust_out

# Nix build outputs
result*

# direnv
.direnv/

# Local reference checkouts (study material, not a dependency; gpui-shell is unlicensed — must not be committed)
reference/

# Claude Code project state (settings, worktrees)
System prompts/
.claude/

# Git worktrees (isolated workspaces, not part of the repo)
.worktrees/

# Session logs
log-dump/

# Agent orchestration runtime state (per-agent caches/config, not source).
# The human-readable briefs and reports live in orchestration/ and ARE tracked.
.autohand/
.cline/
.clinerules/
.mimocode/

# lean-ctx / codebase-context cache
contexts/
