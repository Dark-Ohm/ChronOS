# T306 — отчёт: `checkpoint/log/handoff.log` разрезан по месяцам

**Outcome:** `handoff.log` (3995 строк) удалён из дерева. Вместо него —
`handoff-2026-07.log` (2351), `handoff-2026-08.log` (1636) и `INDEX.md`
(320) в `checkpoint/log/`. Файла `handoff-undated.log` нет: после
наследования дат не осталось ни одного блока без даты. Коммит `ebc8310`,
`handoff : журнал разрезан по месяцам, добавлен INDEX`, без AI-трейлеров.

## Что как резалось (112 блоков)

- **Сегментация:** блок = маркер `**Обновлено:`/`**Чекпоинт` или заголовок
  `##`/`###`/`####` + весь текст до следующего такого же. 57 маркеров +
  55 заголовков = 112 блоков. Снапшот `# HANDOFF (содержимое на момент
  разреза 2026-08-18)` (исходные строки 23–191) оставлен **одним блоком** —
  это h1-секция с датой в собственной шапке; резать её по внутреннему
  маркеру (строка 25) значило бы оставить сиротой 2-строчную шапку,
  а архивная шапка исходника обещает «лежит ниже дословно, включая шапку
  того дня».
- **Даты:** из собственной шапки блока (`2026-0[78]-\d{2}`) — 88 блоков;
  у 24 блоков даты в шапке не было — унаследована от ближайшего
  вышестоящего датированного блока, все 24 помечены в INDEX.md
  (`## Даты унаследованы` с номерами строк исходника). **0 блоков без
  даты** → `handoff-undated.log` не создавался.
- **Порядок:** по дате, от старого к новому; внутри одного дня сохранён
  исходный порядок (устойчивая сортировка по `(дата, позиция в исходнике)`).
  Выпрямление внутри дня не делал: исходник «перевёрнут и местами
  перемешан», переворот внутри дня был бы догадкой, а бриф разрешает только
  перестановку целых блоков.
- **Архивная шапка** исходника (строки 1–22: что это, как грепать,
  приоритет источников, «ничего не удалено при разрезе») переехала в
  `INDEX.md` дословно по смыслу. Шапки новых файлов — по 7 строк каждая.

## Верификация (все четыре обязательные, на финальных файлах)

**1. Ни строки не потеряно — отсортированный diff пуст:**
```bash
$ tail -n +8 .chronos-ops/checkpoint/log/handoff-2026-07.log > /tmp/jul.body
$ tail -n +8 .chronos-ops/checkpoint/log/handoff-2026-08.log > /tmp/aug.body
$ cat /tmp/jul.body /tmp/aug.body | LC_ALL=C sort > /tmp/new.sorted
$ tail -n +23 .chronos-ops/checkpoint/log/handoff.log | LC_ALL=C sort > /tmp/old.sorted
$ diff /tmp/new.sorted /tmp/old.sorted
DIFF-EMPTY: OK
```
(шапка исходника — строки 1–22, контент — с 23-й; шапки новых файлов — 7 строк каждая)

**2. Счётчик строк:**
```
original total  : 3995
archive header  : 22            (строки 1–22, текст + хвостовой пробел)
original content: 3973
jul file        : 2351          (= шапка 7 + тело 2344)
aug file        : 1636          (= шапка 7 + тело 1629)
sum - headers14 : 3973          ✓ 3973 = 3973
```

**3. T-ID не потерялись:** `grep -oE '\bT[0-9]{3}' | sort -u` по исходнику
и по объединению файлов — списки идентичны (173 уникальных T-ID),
`T-ID-SETS-IDENTICAL: OK`. Карта T-ID → файл (227 токенов, грепом
`\bT[0-9]{3}[A-Za-z-]*\b`) в `INDEX.md`.

**4. Блоки не разорваны:** все 112 блоков присутствуют в результирующих
файлах целиком, контактно и без изменений (112/112, дублей 0); даты
заголовков в файлах не убывают: июль 65 блоков ✓, август 47 блоков ✓.

## Ссылки на `checkpoint/log/handoff.log` в других файлах (найдено грепом, не правил — перевешивает архитектор)

- `CLAUDE.md:68` — «`handoff.log` ~4000 строк, грепать по T-ID/дате/симптому, не читать подряд»
- `CLAUDE.md:116` — «Подробности — .chronos-ops/checkpoint/log/handoff.log 2026-07-22»
- `.rules:11` — «История сессий — `checkpoint/log/handoff.log` (архив)»
- `.chronos-ops/README.md:21` — «`HANDOFF.md` (состояние) + `log/handoff.log` (журнал, архив)»
- `.chronos-ops/checkpoint/HANDOFF.md:5` — «`checkpoint/log/handoff.log` (~4000 строк, …)»
- `.chronos-ops/checkpoint/HANDOFF.md:87` — таблица: «| `checkpoint/log/handoff.log` | журнал сессий (архив) |»
- `.chronos-ops/checkpoint/HANDOFF.md:137` — «Полный разбор двух причин — `checkpoint/log/handoff.log`, греп …»

## Что осталось открытым

- Все перечисленные ссылки указывают на удалённый путь — ждут перевода
  архитектором на `checkpoint/log/handoff-2026-08.log` (или INDEX.md).
- Внутридневный порядок в каждом месяце — исходный (не перевёрнут).
  Если нужен строгий внутридневный «старое → новое» — это одна строка
  сортировки, но я счёл это догадкой и не делал.
