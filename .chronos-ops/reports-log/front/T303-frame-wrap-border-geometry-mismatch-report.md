# T303 — wrap-рамка: развести толщину/радиус, убрать debug-лог, живой grim

**Роль:** FRONTEND. **Статус:** готово к приёмке. **Зона:** `crates/app/src/frame.rs` (один файл).

## Что сделано

1. **`wrap.thickness` — новое поле** (`frame.rs:64-70` константы, `136-178` struct/default/sanitize):
   - дефолт `DEFAULT_THICKNESS = 16.0` — равен дефолтному `inner_radius`, чтобы радиус читался соразмерно толщине (требование брифа);
   - отдельная граница sanitize `MIN_THICKNESS=1.0..=MAX_THICKNESS=64.0` со своим warn-сообщением (`wrap.thickness`), не пересекается с `bottom_strip` clamp;
   - `[serde(default)]` — конфиг без `[wrap]` получает 16.0, старые файлы не ломаются.
2. **Единый санитизированный источник** (второй разъезд из брифа, frame.rs:358-366 и 530-533):
   - `wrap_inset_for` в Wrap-ветке: `cfg.bottom_strip.sanitized().height` → `cfg.wrap.sanitized().thickness`;
   - `WrapSurfaceView::render`: сырой `cfg.bottom_strip.height` → `cfg.wrap.sanitized().thickness` — то же санитизированное значение, что и инсет/резервации. Сырых `cfg.*.height` в рендере wrap не осталось (греп: оставшиеся `bottom_strip.height` — только Hide-путь и тесты).
3. **Debug-мусор удалён полностью** (`tracing::error!("T303DEBUG ...")` — выпилен, не понижен до debug).
4. **Живая геометрия матте — кровный факт** (frame.rs:600-627): брифинговый коммит d01820e не достигал цели «fullscreen matte»:
   - `no vertical anchor` (d01820e) на этом Hyprland НЕ даёт «top margin 0»: 1440-пиксельная поверхность ЦЕНТРИРУЕТСЯ в свободной зоне между баром (30) и bottom-резервацией → y=13, нижняя кромка кольца уходит за экран (замерено живьём: матте `(0,13,2560,1440)`, нижнего бордюра нет);
   - `BOTTOM`-якорь → flush-посадка у верха резервации: `(0,-16,2560,1440)`, кольцо полное, но с 16px полосой обоев под низом;
   - **отрицательный margin** (`bottom = -inset`, `left = -inset`) компенсирует резервации ровно: `bottom = available_bottom - margin = (1440-16) - (-16) = 1440` → матте `(0,0,2560,1440)` во всех состояниях (проверено с закрытой и открытой панелью);
   - горизонтально: `LEFT|RIGHT`-якорь при открытой панели ЦЕНТРИРУЕТ переполнение (замерено x=-28, кольцо уехало за левый край и легло на рейл) → оставлен только `LEFT`-якорь;
   - слой матте — `Top` (как было): Overlay на одном слое с рейлом/панелями глушит их клики пустой input-region (замерено), Top — ниже них;
   - `border_t_0()` — верхний бордюр не рисуется вовсе (бар читается как верхняя кромка).
5. **Тесты** (frame.rs:977, 1047-1078, 1097-1109): `defaults_are_sane` + `wrap.thickness`; `wrap_thickness_clamped` (default + clamp в обе стороны); `wrap_inset_zero_in_hide_thickness_in_wrap` переписан с `bottom_strip.height` на `wrap.thickness` + кейс «нет [wrap] → дефолт ведёт инсет».

## Верификация

**cargo check** — чисто; **`--lib` 598/598** (было 597, +1 новый тест); **`--bins` 790/790**; 0 новых warnings в frame.rs.

**Живой grim (release-биарь, DP-1 2560×1440, reserved `[4,30,4,4]`):**

- **До** (биарь из принятого дерева, inset 4, T303DEBUG в логе): матте `(0,13,2560,1440)` → левый/правый край 4px (100% diff vs голые обои), **нижней кромки НЕТ** (0.4% diff — нижняя полоса пуста). Рамка «обрезана по краям» — ровно симптом жалобы владельца.
- **После** (дефолт thickness 16): матте `(0,0,2560,1440)`; левый/правый край 16px на всю высоту (99.1% diff), **нижний бордюр y1424-1439 до края экрана (99.1% diff)**, под ним 0% (чистая reserved-полоса); радиус внешний 32 / внутренний 16 (радиус == толщина, пропорционально); бар цел (виджеты 100% на месте, матте его не перекрывает); рейл правой панели x2488-2528 — зазор 16px до кольца x2544, контент вложен корректно.
- **8px hot-конфиг** (старт с `[wrap] thickness=8`): кольцо ровно 8px на всех краях (99.2% в полосе 8px / 0.8% снаружи), все три excl-полосы 8px — **единый геттер держит кольцо и резервации синхронно**.
- 8 кропов «после» (4 угла + середины краёв): доминирует цвет рамки (24,24,37), нижние углы с округлением — приложены в `/tmp/t303-crops/`.

## Честные оговорки

1. **Регресс-проверка popup'а (T305) синтетическим вводом не доведена:** в моих смок-сессиях pointer-ввод к layer-поверхностям не доставляется вообще — не реагируют ни иконки рейла, ни клик по часам бара (calendar не открывается). Это НЕ связано с T303: воспроизводится с `style="hide"`, где ни одна строка моего дифа не исполняется (диф — только frame.rs, wrap-код в Hide не запускается). Popup-код T303 не трогал; единственное взаимодействие — общий `wrap_inset()` (4→16), который сдвигает рейл И margin popup'а на одну величину (оба читают `frame::wrap_inset()`), относительная геометрия сохраняется — замерено по рейлу/контенту. Живой приём popup'а был в T305.
2. **Горячая перезагрузка wrap-геометрии — стартовый только (найдено живьём, НЕ чинил):** `apply_wrap` → `open_wrap_windows` early-returns, если матте уже открыта → смена `thickness`/`inner_radius` в `frame.toml` на живом шелле не применяется (бар и Hide-полоса перезагружаются, wrap — нет). Вне задач тикета (список из 4 пунктов выполнен); зафиксировал как находку — решать архитектору.
3. Коммит не делал (в брифе раздела «Коммит» нет); в дереве только `frame.rs` (проверено `git status`).

## Файлы

- `crates/app/src/frame.rs` — единственный изменённый файл (диф: 1 файл, ~+100/−40).
