# HANDOFF — состояние проекта для новой сессии Архитектора

**Это файл СОСТОЯНИЯ, не журнал.** Здесь только то, что верно прямо
сейчас. История сессий, разборы приёмок по дням, старые чекпоинты —
`checkpoint/log/` (разложен по месяцам 2026-08-19, T306: `handoff-2026-07.log`,
`handoff-2026-08.log`, точка входа `log/INDEX.md`) — грепать по
T-ID/дате/симптому, не читать подряд.

**Правило гигиены:** этот файл живёт в пределах ~200 строк. Всё, что
устарело — переносится в `checkpoint/log/`, а не копится тут. Разрез
2026-08-18 случился потому, что файл дорос до 3973 строк и его перестали
читать целиком — включая меня.

**Обновлено: 2026-08-21.** HEAD — `git log -1`.

---

## Где стоим

**T328 ПРИНЯТ 2026-08-21** (Sol). Вердикт НЕТ. Рама ок (апертура).
Тема/обои валят продукт. 36+42 кадра `dump/qa-ux/T328/`. B1 awww
сносит mpvpaper → **T338**. B2 Next на 34×mp4 молчит → **T339**. B3
Solarized чип 1.19:1 → **T340**. B4 blur ModuleMissing → **T341**. B5
alpha только бар → **T342**. F1 граница бара после height → **T343**.
Шов 1px — канон T284, не баг. Стол не восстановлен (waytrogen CLI
битый `-o`/`*`); чёрного нет. `bar.toml` height **20** (28.4 откатили
из бэкапа). 11 sha, panic/protocol 0. **QA слайсов нет.** T323 HOLD
(нарезан T324–T328). Front: T346(P0)+T332…T337+T339+T341–T343. Back: T330+T338.
**T331 HOLD.**

**T344 ПРИНЯТ 2026-08-21 по коду.** Три глифа левой панели
(`tool_card.rs:23` точка pending, `:74` шеврон, `chat_view.rs:117`
пустой чат) слезли с `interactive.active` на `text.muted` — 3 строки,
2 файла, grep чист (в `side_panel_left/` остались только `.bg(`),
610 + 28 тестов. Контрасты сверил своим прогоном проектного
`contrast_ratio`: Solarized `text.muted` = 5.61:1 на `bg.primary`,
4.86:1 на `bg.secondary`, старый цвет давал **1.00:1** (цвет в цвет с
фоном). После T340 отмечаю: здесь числа в отчёте честные. Уточнение:
чат-колонка фон не красит вовсе (корень панели `transparent_black`),
явный `bg.primary` — только у карточки инструмента.
**Живого кадра нет ни для одного из трёх глифов** — принял по коду как
исключение для трёхстрочной правки цвета под воротами T317, не как норму.

**T345 ПРИНЯТ 2026-08-21.** Корень оказался не в рендере пустого
состояния, а в том, что оно не наступало: перед стартом/повтором turn'а
в `chat.messages` пушится пустой агент-плейсхолдер (`segments: Vec::new()`,
`composer.rs:739`, `tabs/chat.rs:762`) — `has_messages` был true на пустой
сессии, «No messages yet» не рисовалось, а плейсхолдер вылезал одним
пустым пузырём. Правка: `has_messages` по содержанию + `continue` на
пустых в цикле. 1 файл, 11+/1−. Проверил сам: индексы `msg_idx` не
поехали (`continue` внутри `enumerate`, ключи `expanded_tool_calls`
целы), других push'ей с пустыми segments в дереве нет (все 9 сайтов
сверил), 610 + 28 тестов зелёные своим прогоном. Кадры исполнителя
разглядел глазами: «No messages yet» на Default и Solarized, на
`03-turn-done.png` видны шеврон `▸` у правого края карточки и точки
статуса (зелёная Done / красная Error) — **долг T344 по кадрам закрыт**.
Загадочный «чужой кадр» `solarized-chat-blank.png` объяснился: его снял
сам Hermes ИЗ панели (на кадре видно его reasoning «сделаем скриншот») —
улика не чужая, а рекурсивная.

**T346 заведён 2026-08-21 — P0, вперёд всей фронт-очереди.** Второй
симптом T345 исполнитель не воспроизвёл, **я воспроизвёл 4 раза подряд**:
после ПЕРВОГО закрытия левой панели любое следующее открытие даёт
мёртвую панель — рельс рисуется, слой `side_panel_left_content` создаётся
с верной геометрией (920×1395 @ 40,28 по `hyprctl layers -j`), лог чистый
(`opened both surfaces (pinned)`, `ACP client connected`), а пикселей
ноль: сквозь панель видно обои. Не лечится ожиданием, ховером, повторным
toggle. Моя версия из T344 («не выбрана вкладка») **неверна** — `active_tab:
LeftTab`, не `Option`, вкладка выбрана всегда. Ведущая гипотеза:
`with_animation("side-panel-left-enter")` + `apply_enter_from_left`
(`motion.rs:74`) при `delta = 0` даёт `opacity(0)`; на пересозданном окне
анимация, похоже, не тикает. Улика против гипотезы, которую надо
объяснить: рельс использует ту же функцию (`workspace_view.rs:427`) и
**виден**. Кадры — `dump/qa-ux/T346/arch-0{1,2,6,7}*.png`.
**Пока T346 не закрыт, живой смок левой панели невозможен.**
Побочно выяснено: `select-tab:<id>` по IPC левую панель НЕ переключает
(`classify_select_tab` знает только вкладки правой), а `ydotool click`
уводит курсор (20,106 → 1050,53) — после клика warp'ать заново.

**T340 ПРИНЯТ 2026-08-21** (`4606d9cd`, 2 файла). Выбранный чип настроек
— непрозрачная плита `interactive.active` + `text.primary` во всех
четырёх chip-билдерах (`seg_chip`/`preset_chip`/`theme_swatch_card`/
`onoff_chip`), акцентная рамка осталась сигналом. Solarized Dark:
`interactive.active` опущен с base04 `#839496` на base01 `#073642`
(родной highlight-фон). Ворота `selected_chip_passes_wcag_aa_in_all_schemes`
проверил СВОЕЙ мутацией по другой схеме (Mocha → 1.14:1, тест падает) —
цикл настоящий, не только якорь. Живьём Solarized: Top/Full/Soft/on/
Wrapped и имя карточки читаются, T328 B3 закрыт. **Все четыре числа
контраста в отчёте оказались неверны:** реально 6.68 / 9.24 / 12.05 /
**4.89** (Mocha) против названных 7.37/8.93/10.93/5.69 — вердикт тот же,
но запас у Mocha Mousse 0.39, а не 1.19; любая правка её палитры роняет
ворота. **Побочка → T344:** `interactive.active` красил ГЛИФЫ в левой
панели (`tool_card.rs:23,74` шеврон и точка статуса, `chat_view.rs:117`
«No messages yet») — на Solarized это #073642 на #002b36, ≈1.2:1.
Исполнитель сказал вслух и в чужую зону не полез. Чиним переводом на
`text.muted` (уже под воротами T317), токен не расщепляем.

**T329 ПРИНЯТ 2026-08-21.** Календарь получил плиту семьи Sound
(`surface_color(bg.alpha(0.82))` + `border.subtle` + `radius_lg` +
`elevation_popup` + blur-слой, `Calendar.border_0()`). Диф — один файл
`calendar_popup/view.rs`, Root остался прозрачным. 610 тестов, кадры
`dump/qa-ux/T329/frames/`. **Блокер отчёта опровергнут живьём:** попап
открывается тем же бинарником, который исполнитель объявил нерабочим;
никакой «canvas глотает клики» нет. Два методических провала
исполнителя, оба теперь записаны в `active/front/FRONTEND.md`:
(1) `ydotool mousemove --absolute` на этой машине промахивается —
`-x 2404 -y 10` уезжает в `3475, 39` (второй монитор); warping только
`hyprctl dispatch 'hl.dsp.cursor.move({x=..,y=..})'` (Lua-Hyprland
**0.56.2**, старый `movecursor 2404 10` — синтаксическая ошибка) и
позицию сверять `hyprctl cursorpos`; (2) якорный попап — xdg_popup, его
нет ни в `hyprctl layers`, ни в `clients`, проверять только `grim`.
Долг: поверх яркой карточки медиа плита просвечивает — ждём **T341**
(блюра в компоситоре нет вообще), альфу семьи крутить в T342, не в
календаре. **T332 разблокирован.** IPC-смок: сокет
`/run/user/1000/chronos.sock`, `socat` в системе НЕТ, слать питон-скриптом.

**T326 ПРИНЯТ 2026-08-21.** Левая панель прототип. T333 reselect.
**T325 ПРИНЯТ 2026-08-21.** Попапы → T332. **T324** бар → T329/T330/T331.

**T313 принят 2026-08-20** — пикер схем со свотчами вместо кнопки
Toggle и схема **Mocha Mousse** (тёмная, Pantone 17-1230). Код
исполнителя `8629bac6` + `4a960516`. Контрасты пересчитал сам, сошлись
до сотой (muted 5.43:1); тест на контраст проверен мутацией; клики по
карточкам — живой мышью через ydotool, в `theme.toml` меняется ровно
ключ `scheme`. Схем стало 4, все проходят WCAG-ворота T317
автоматически. По ходу живого разбора с владельцем добавлены четыре
правки архитектора: `d3ca6570` — ширина вкладки System settings
410 → **800** (при 410 весь респонсив T231 на странице был недостижим,
`GRID_BREAKPOINT` = 720); `30d1a3e5` — сегменты тянутся на всю строку,
Blur перестал наезжать на Elevation; `f786836d` — карточка настроек
тянется на всю высоту вкладки (под ней зияло ~85px фона; корень был не
в карточке, а в нерастянутом корне вкладки).

**T312 принят 2026-08-20** (`0e0ee38a`, +203/−325 — впервые удалено
больше, чем добавлено). Режимы рамы теперь `normal` / `wrapped`
(`FrameStyle::Normal/Wrapped`), старые `hide`/`wrap` — алиасы разбора,
**не выпиливать** (T268). Нижняя планка удалена как сущность:
`[bottom_strip]` парсится, но не читается; низ в `wrapped` — это
`wrap.bottom`, в `normal` низа нет вовсе и не создаётся ни одного слоя
`frame_wrap_*`. Живьём проверено пять циклов `wrapped↔normal` при обеих
открытых панелях — ноль протокольных ошибок (сценарий T321/T322 держит).
Эррата архитектора в том же коммите: гейт легаси-варна сжигал
одноразовость на первом `apply` до проверки условия — порядок условий
переставлен.

**T316 закрыт 2026-08-20 без кода** — апертуру со всех четырёх сторон
уже замкнуло кольцо `aperture_ring` из T318; живой промер на белых обоях
подтвердил радиус ≈10 одинаковым сверху и снизу и синхронный hot-reload
на 8 и 24. Правка бара из отчёта в дерево НЕ взята (лежит в
`git stash@{0}`): заявленный латентный баг «обои сквозь скруглённые
верхние углы бара при `appearance.radius > 0`» не воспроизвёлся на
чистом HEAD — угол подложен хромом wrap-поверхности, а цена правки —
молчаливое отбирание `appearance.radius` у пользователя в режиме wrap.
Урок в дисциплину: бриф T316 писался до приёмки T318 и пережил свою
предпосылку — сверять предпосылки тикета с деревом в момент ВЫДАЧИ.

**T309 принят 2026-08-19** (QA, живой UX-критик-аудит) — вердикт «не
дерьмо, но до показа как законченного продукта не дотягивает». Главная
находка: dock не показывает 3 pinned app (`no AppEntry`, сверено с
кодом дословно). Полировка (бар перегружен, empty states, light-тема
тяжёлая) — `checkpoint/TBD.md`, раздел «T309». **Найден и исправлен
методологический баг самого QA-процесса:** `awww restore` не работает
на этой машине (падает, тихо оставляет чёрный экран вместо обоев) —
верный путь `waytrogen --restore`; T309-бриф исправлен на будущее.

**Волна 2026-08-18/19 принята полностью:** T305 (`f326fc7`, control-center
popup), T303 (`c6df21a`, wrap matte-геометрия базовая), T301 (`96f713a`,
composer Select эллипсис), T307 (`601f8f0`, wrap hot-reload), T308
(`f2cacee`, wrap-матте `exclusive_zone: Some(px(-1.))` opt-out — не
съезжает при открытой панели), T306 (RECON, `fc1945e`, журнал разрезан
по месяцам), T309 (QA, критик-аудит). Детали — `MIGRATION.md`.

**Припарковано (`active/hold/`) — разобрано 2026-08-20, осталось три:**

- **T282** — упаковка (сборка без `../Source`, PKGBUILD, tarball, смок в
  чистом окружении). Живой, ждёт своей очереди; по собственному условию
  берётся, когда в `active/` пусто — а сейчас там пусто.
- **T224** — первый запуск и витрина репозитория. Идёт ПОСЛЕ T282; его
  раздел «Пакет» перекрыт T282 и в исполнение не идёт.
- **T281** — левая workspace Slice A. **Не закрыт и закрыться не может:**
  единственный оставшийся гейт (живой рестарт — лента И сессия агента
  восстанавливаются) упирается в T285 `load_session`, а тот STOP по
  апстриму. Это гейт владельца, миньону не выдавать.

Снято с парковки: **T277** (аудит T276) — предмет проверен позднее
T289/T302/T320/T321/T322 и живыми промерами, `done/qa/`; **T191** (смок
слайса 5) — отклонён как неактуальный, продукт ушёл в Agent Shell DE,
`reject/qa/`. Детали — `MIGRATION.md`.

**T285 (ACP `load_session`) — STOP по апстриму**, ветка
`feat/t285-acp-load-session` парковая. Причина — в `REJECTED.md`.

## Открытые долги

- **T281 гейт 8** — живой рестарт (владелец).
- **T266** — живой прогон попапов сделан T325. Хвост: click-away
  Sound/Calendar = T332; заливка календаря = T329.
- **T303** — живой grim после правки.
- Память-инфра после ребута не автостартует (см. «Окружение»).

## Кухня

`.chronos-ops/` под git — контрибьюторы берут задания отсюда.
`docs/orchestration/` закрыт и физически удалён 2026-08-18.

- Тикеты: `active/<role>/TNNN-slug.md` (+ `active/hold/`), точка входа
  роли — `active/<role>/<ROLE>.md` (тонкий указатель, не журнал).
- Отчёты: инбокс `reports-fresh/` → принятые `reports-log/<role>/`.
- Тикеты после приёмки: `done/<role>/`, на доработку `rework/<role>/`,
  отклонённые `reject/<role>/`.
- Реестр всех T-ID с вердиктами приёмки — `.chronos-ops/MIGRATION.md`.
- Что в какой каталог класть — `.chronos-ops/RULES.md`.
- Заметки и кадры-улики — `dump/notes/`, старые точки входа ролей —
  `dump/legacy-agents/`.

`skills/` вынесен из репо: единый вольт
`/home/neo/projects/chronos-ecosystem/skills/`, вне git, общий на всю
экосистему. В ChronOS каталога `skills/` нет; CI-job `skill-proofs` и
`scripts/git-hooks/pre-commit` удалены вместе с ним.

`docs/` — только продукт и сайт: `product/`/`style/`/`guides/` плюс
`index.html`/`.nojekyll`/`landing/` (GitHub Pages). Hyprland-правила
живут в `packaging/hyprland/` — **путь
`packaging/hyprland/40-windowrules-chronos.lua` менять нельзя**, живой
конфиг `~/.config/hypr/modules/40-windowrules.lua:4` жёстко его
`dofile`'ит, переезд сломает reload молча.

## Канон (при расхождении побеждают они)

| Файл | Что в нём |
|---|---|
| `checkpoint/ARCHITECT.md` | роль архитектора + живой список дисциплины |
| `checkpoint/ARCHITECTURE.md` | принятые архитектурные решения |
| `checkpoint/REJECTED.md` | что рассматривали и отклонили, почему |
| `checkpoint/HANDOFF.md` | **этот файл** — состояние сейчас |
| `checkpoint/log/INDEX.md` | журнал сессий (архив, по месяцам) |
| `checkpoint/TBD.md` | оперативные хвосты |
| `checkpoint/MEMORY.md`, `SOUL.md` | память и журнал сессий |
| `.chronos-ops/MIGRATION.md` | сквозная история T-ID |

## Кровные технические факты

Каждый оплачен живым багом. Не «стиль» — правила.

- **`cx.background_spawn(...)` без `.detach()` — баг.**
  `gpui_scheduler::Task` — `#[must_use]`, drop = cancel. Голый вызов или
  `let _ =` роняет Task сразу: быстрые футуры проскакивают, медленные
  (zbus/hyprctl/subprocess) дохнут молча. Симптом: `on_click` в логе
  есть, эффекта нет.
- **`AsyncApp::update` в форке возвращает `R`, не `Result`**
  (`Source/gpui/src/app/async_context.rs:163`). Значит `let _ =
  cx.update(...)` ничего не глотает — правка тут «снять `let _ =`», а не
  заворачивать в `if let Err`. Не переоткрывать (урок T271).
- **Никаких побочных эффектов в `render()`.** Он зовётся сотни раз в
  секунду (замер/лэйаут/пейнт + каждый сервисный сигнал, cava шлёт
  30 fps). Любое накопление — с гейтом по времени (≥1 с) и кэшем
  показанного; считать СКОРОСТЬ, а не дельту за неизвестный интервал.
  Юниты этого не ловят: они кормят синтетические дельты.
- **`tokio::task::spawn_blocking` вне tokio-runtime виснет.**
  `cx.background_spawn` — GPUI executor, не tokio: не паникует, просто
  не завершается. Для subprocess из GPUI-таски — `std::thread::spawn` +
  `tokio::sync::oneshot`. (zbus работает — сам поднимает runtime.)
- **`window.display(cx)` == None для layer-shell окон** (форк,
  `Source/gpui/src/window.rs:2293`): wayland-backend не заполняет
  `display_id`. «Попап на дисплее кликнутого окна» так не сделать;
  `display_id` в `WindowOptions` при `open_window` честен — роутить
  через конфиг пультового монитора.
- **`window.bounds()` для `center=true` окон врёт.** Wayland не отдаёт
  позицию toplevel'а: origin заморожен на запрошенном `(0,0)`. Живой
  `hyprctl` — единственный источник истины.
- **`KeyboardInteractivity::Exclusive` ЗАПРЕЩЁН** — вешает input-стек
  композитора.
- **Призыв левой панели = rail-only, и это дизайн, а не баг** (T220,
  подтверждено T302 2026-08-18). `toggle-side-panel-left` / `Super+A`
  открывают ОБА окна (`rail` + `content`), но `panel_width = RAIL_WIDTH`
  → `visible_content_width == 0`: видна только 40px полоса рейла, чат не
  выезжает. Контентное окно при этом висит прозрачным 920px `Overlay` над
  рабочим столом — клики сквозь него проходят, `content_input_region` при
  нулевой ширине пуст (`side_panel_left/state.rs:73`). Чат раскрывает
  клик по иконке таба на рейле или `chronos-ipc expand-left`. Так сделано
  потому, что призыв «сразу с чатом» прятал композер (баг T137). Плюс
  enter-анимация 260 мс стартует с `opacity 0` — `grim` в первые полсекунды
  даёт ровно ту же картину «пустая зона, сквозь неё обои».
- **`remove_window` на часто скрываемых layer-shell окнах шумит
  «window not found»** — soft-hide (display=None + пустой input region).
  Полный разбор двух причин — `checkpoint/log/handoff-2026-07.log`, греп
  `СИСТЕМНЫЙ БАГ`.
- **Композиторные события: «сменился» ≠ «список изменился».** Имена
  хендлеров в крейте `hyprland` генерируются макросом `events!` из
  вариантов enum — грепом `pub fn add_` не находятся, сверяться со
  списком в `event_listener/shared.rs`.
- **Lua-Hyprland:** диспатчи только Lua-формой в сокет (`hl.dsp.window.move`,
  не `hl.dsp.move`); голый `hyprctl dispatch` мёртв. Истина — живой
  сокет, не wiki.
- **Блюр (Lua-Hyprland 0.56.2):** глобальный `decoration.blur.enabled`
  обязателен, иначе корректный layer-rule не рендерит ничего;
  `ignore_alpha` в layer rule молча убивает блюр; `hl.layer_rule`
  идемпотентен по имени — правка файла требует рестарта Hyprland
  (`hyprctl reload` сбрасывает eval-глобалы).
- **Контент поверх заливки — через `chronos_ui::on_fill()`**, не
  `theme.text.*`: токены текста переворачиваются со схемой, заливки нет.
  `status.*` у схем РАЗНЫЕ (Mocha vs Latte).
- **zbus/D-Bus сверять с `busctl introspect`,** не с докой: UPower
  DisplayDevice = интерфейс `.Device`; `GetLayout` возвращает
  `(u(ia{sv}av))`, не `(uv)`.
- **gpui BGRA:** сырой RGBA-пиксмап свапать (0,2) перед `RenderImage`.
- Трей забивается безымянными `StatusNotifierItem` от Chromium/Vivaldi —
  фильтр безымянных + дедуп по bus-имени на нашей стороне.
- Бар перерисовывается ежесекундно: в `render()` виджетов ноль
  аллокаций и IO без кэша.

## Смоки: чем и как

**«Компилируется и тесты зелёные» для оконного/UX-кода — ничто.**

- Шелл: `cargo build --release -p chronos` → `pkill -x chronos` →
  `RUST_LOG=info ./target/release/chronos` → wpctl / notify-send /
  `chronos-ipc` → `grim`.
- UX-смоки ТОЛЬКО release. Кропы: `magick -crop WxH+X+Y -resize N%`.
- **Левая панель (T220):** призыв (`Super+A` / `chronos-ipc
  toggle-side-panel-left`) → `hyprctl layers -j | grep side_panel_left`:
  ждать `rail` 40px И `content` 920px, при этом видимая доля контента = 0.
  Раскрыть чат — клик по иконке таба на рейле или `chronos-ipc expand-left`.
  Снимать `grim` не раньше чем через ~0.5 с после раскрытия, иначе поймаешь
  enter-фейд и решишь, что панель пустая.
- `hyprctl clients -j` — обычные toplevel (лаунчер); `hyprctl layers -j`
  — layer-shell (bar/dock/osd/notifications/tray_menu). Проверять
  «открылось ли окно» надёжнее через `layers -j`, чем через grim-кроп.
- Тесты: `cargo test --workspace --lib --bins`; точечно —
  `cargo test -p chronos --bins <модуль>`.
- **ydotool** для кликов по попапам: юнита нет, `sudo ydotoold` руками +
  `chmod 666 /tmp/.ydotool_socket`; калибровка `hyprctl cursorpos` ⇄
  `ydotool mousemove -a` заново каждую сессию, формула плавает
  (на этой машине absolute ≈ экран/2), только одношаговые прыжки.
- `wf-recorder` для процессных багов (geometry-рецепт как у `grim -g`,
  останавливать `kill -INT`, не `-9`).

## Пользовательское окружение (не ломать)

- **DP-1** (Samsung, главный, слева) 2560×1440 @ 0,0; **HDMI-A-1**
  (Dell, справа) 1920×1200 @ 2560,0. Бар 2560×32.
- `hyprland.lua`: SUPER+equal/minus → микрофон ±5%; SUPER+L → лаунчер
  (сокет `$XDG_RUNTIME_DIR/chronos.sock`, payload `toggle-launcher`);
  автостарт easyeffects; `kb_layout = "us,ru,il"` (Alt+Shift).
- Пользователь работает в Vivaldi — процессы не трогать. Обои/мониторы
  дёргать только кратко в смоках и ВОЗВРАЩАТЬ как было.
- Память-инфра после ребута НЕ автостартует:
  `systemctl --user start app-9router@autostart.service` (:20128) →
  `podman start hindsight-embeddings hindsight-reranker hindsight` →
  health :8888. Hindsight склонен к OOM (exit 137) — рестарт. 401 =
  протух ключ провайдера в 9router, чинит владелец.
- Ядро CachyOS после обновления живёт без модулей до ребута — ломает
  podman-сеть и ydotool.
