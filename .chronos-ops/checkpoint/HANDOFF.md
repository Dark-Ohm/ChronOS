# ChronOS kitchen — HANDOFF

Канон оперативки пока `docs/HANDOFF.md`.

Очередь ещё в `docs/orchestration/tasks/`. Сюда тикеты переезжают
по одному, не пачкой. T287-B в поле — не трогать.

Снимок: `bash .chronos-ops/bin/kitchen-status.sh`
