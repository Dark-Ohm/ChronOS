# MIGRATION.md — T-ID ledger (per-agent → per-task pivot, 2026-07-22)

Реестр перехода с per-agent журналов (`docs/orchestration/agents/<ИМЯ>.md`) на
per-task учёт (`docs/orchestration/tasks/`). Собран из полного разбора
`docs/HANDOFF.md` + `docs/DECISIONS.log` + `docs/orchestration/agents/archive/*.md` (каталоги
`archive/`, `agents/archive/` и `reports/` удалены из дерева 2026-07-31 —
содержимое живёт в git-истории, последний коммит с ними `5dd70c1`) +
`docs/orchestration/agents/bench|fired/*.md` + листинга `docs/orchestration/report-log/`
(две параллельные разведки, 2026-07-22). T-номер = порядковый (хронология),
3 цифры, буквенный суффикс — под-задача/companion-файл того же T.

**Скоуп-решение (явно, не молча):** для строк БЕЗ сохранившегося отдельного
файла отчёта/брифа (до-агентская эпоха, часть агрегатов) НЕ создавались
синтетические файлы-заглушки в `done/`/`rejected/` — эта таблица есть
единственный и достаточный источник записи для них (создавать файл на
одну ссылку в HANDOFF было бы фабрикацией структуры без нового содержания).
Для строк с реальным файлом — файл физически перемещён (`git mv`) с
двух-трёхстрочным заголовком (T-ID/агент/исходный путь) поверх тела,
тело не переписано.

## Спорные случаи — решения (см. план `foamy-swimming-engelbart.md`)

1. **Mimo apps-service «№3» (MIMO.md) vs «№4» (docs/HANDOFF.md)** — один T-ID
   (T017), тело цитирует оба номера, канон = HANDOFF (выше в authority order).
2. **До-архивные агрегаты** (Cline «№1-5», Hermes «№3-6») — по одному T-ID,
   статус `reconstructed-aggregate`, без синтетического файла (см. скоуп выше).
3. **`grok-report-3.md`** — мигрирован (T027) с громким предупреждением в
   шапке: анонимная перезапись, не доверять без сверки.
4. **`zed-report-2-Phase2-DISCARDED-superseded-by-consolidation.md`** →
   `rejected/`, статус discarded.
5. **DeepSeek «№2»/«бывшее №14»** — один T-ID (T082), оба номера в тексте.
6. **Autohand tray-menu-popup vs Hermes tray_menu** — ОДИН T-ID (T020,
   не два, как в первой версии плана) — весь Autohand-lineage (задание +
   продолжение) никогда не был принят, это одна незавершённая ветка, не
   две; кросс-ссылка на T040 (Hermes, реально принято `67ca90a`).
   *Отклонение от буквы плана записано здесь явно.*
7. **Rework/duplicate/copy-варианты** — канонический (финальный) → своё
   T-ID в `report-log/`; черновики → `notes/superseded/TNNN-*.md`.
8. **Некросс-катные не-таск аудиты** → `notes/` без T-ID: `chronos_services_
   rewrite_cost_report.md`, `REWRITE_BY_PATTERN_AUDIT.md`, `SHADER_PORT_
   AUDIT.md`, `kael_motion_port_audit.md`, `philip-report-maintain-2026-07-21.md`,
   `architect-report-philip-maintain.md`, `SESSION_REPORT.md`,
   `Compact summary.md`.
9. **Найдено попутно:** `docs/orchestration/report-log/HANDOFF.md` — 176-строчный
   старый снэпшот корневого docs/HANDOFF.md, обнаружен как stray-копия (отличается
   от текущего, источник появления неизвестен). Не удалён — перенесён в
   `notes/stray-handoff-snapshot.md`, флаг для внимания пользователя.
10. **`opencode-report-3.md`/`-rework1.md`** — оба черновика DBusMenu
    (частичный accept `6782337`, фикс десериализации `1d54ffd`). Финальный
    полный accept (`f755db6`, «DBusMenu-сервис ЗАКРЫТ») **не имеет
    отдельного сохранившегося файла отчёта** — архитектор верифицировал
    напрямую по HANDOFF. `-rework1.md` — канон (ближе всего к финалу),
    `-3.md` (первая попытка) → `notes/superseded/`.
11. **`mimo-report-techstack-REJECTED-wrong-task.md`** — не относится ни к
    одной задаче из каталога (research-репорт не по той задаче) — T-ID вне
    последовательности, `T900` (misassigned research, rejected).
12. **`deepseek-report-2.md`** — `[ambiguous]`: по номеру продолжает
    network-widget rework (T066, commit `0838446`), НО T082 (fork recon
    state/async, `9c2c090`) не имеет вообще никакого surviving файла в
    листинге. Решение: `deepseek-report-2.md` → T066 (последовательная
    нумерация сильнее), T082 остаётся без файла (архитектор верифицировал
    напрямую).
13. **Zed System-popup лениджа** — `zed-report-2.md` (Phase 1 диагностика)
    и `zed-report-3.md` (Phase 2, частичный accept: bug2+gaming починены,
    display-фикс не работает) — оба реальные, не дубликаты. Финальный accept
    фичи целиком (`f7de445`, T055) отдельного файла не имеет — ближайший
    сохранившийся документ = `zed-report-2.md`, помечен так же T055
    (Phase 1), а T055a = `zed-report-3.md` (Phase 2, отдельная companion-
    запись, её находка про `window.display()==None` ушла в T058 consolidation).

---

## T001–T007 — до-агентская эпоха (2026-07-10/11, Архитектор)

Ни одного отдельного файла-брифа/отчёта не пережило переезд репо (см.
`docs/DECISIONS.log` 2026-07-16 "история = git log больше не источник"). Запись —
только эта таблица + `docs/DECISIONS.log` записи с тем же названием.

| T-ID | Дата | Кто | Описание | Коммит | Статус | Источник |
|---|---|---|---|---|---|---|
| T001 | 07-10 | Architect | Services crate scaffolding + `Service` trait | — | accepted | docs/DECISIONS.log "Task 1" |
| T002 | 07-10 | Architect | Compositor types + geometry fields (i128 IDs) | — | accepted | docs/DECISIONS.log "Task 2" |
| T003 | 07-10 | Architect | NetworkSubscriber, zbus 5.x fixes | — | accepted | docs/DECISIONS.log "Task 3" |
| T004 | 07-10 | Architect | UPowerSubscriber, zbus 5.x + f64 Eq trap | — | accepted | docs/DECISIONS.log "Task 4" |
| T005 | 07-10 | Architect | Services container + `init_all()` + retry tests | — | accepted | docs/DECISIONS.log "Task 5" |
| T006 | 07-10 | Architect | AppState + `watch()` bridge + tokio bootstrap | — | accepted | docs/DECISIONS.log "Task 6" |
| T007 | 07-11 | Architect | Launcher module: nucleo, layer-shell, IPC toggle; **Critical keyboard-focus bug opened here** | — | accepted (bug carried fwd) | docs/DECISIONS.log "Task 9" |

## T008–T059 — первая волна миньонов (2026-07-16 → 07-19)

| T-ID | Дата | Агент | Описание | Коммит | Статус | Старый путь → новый |
|---|---|---|---|---|---|---|
| T008 | 07-16 | OMP | Launcher focus investigation → рекомендация XDG toplevel | — | recon | нет файла (docs/DECISIONS.log) |
| T009 | 07-16 | Cline | `Source/` workspace-root реанимация | `3ce3466` | accepted | archive/CLINE.md (нет отдельного файла) |
| T010 | 07-16 | Hermes | Kael excerpt audit (easing/spring/blur) | — | accepted (decision) | нет файла (SHADER_PORT_AUDIT.md → notes/, не отчёт задачи) |
| T011 | 07-17 | Hermes | gpui-shell excerpt audit | — | accepted (decision) | нет файла |
| T012 | 07-17 | Architect | Compositor dispatch → raw Lua-socket | — | accepted | нет файла |
| T013 | 07-17 | Grok | Audio service MVP на `wpctl` poll | — | accepted (decision) | нет файла |
| T014 | 07-17 | OMP | Toplevel migration launcher | `7af364e` | reworked | bench/OMP.md |
| T015 | 07-17 | OMP | HOTFIX launcher focus-trap regression | — | accepted | `omp-report.md` → `report-log/T015-launcher-focus-trap-hotfix-report.md` |
| T016 | 07-17 | Mimo | Battery widget (№1-2) + "нет батареи" детект | — | accepted | `mimo-report.md` → `report-log/T016-battery-widget-report.md` |
| T017 | 07-17 | Mimo | Applications service (**№3 в MIMO.md / №4 в HANDOFF — канон HANDOFF**), первый проход + доработка | `dd75738`+`fd46474`+`acad3b3` | reworked→accepted | `mimo-report-4.md`+`mimo-report-4-rework.md` → `report-log/T017-applications-service-report.md` (rework, канон); `mimo-report (copy 1).md` (первый проход, `0352e2a`) → `notes/superseded/T017-first-pass.md` |
| T018 | 07-17 | Autohand | Network widget (№1) | `4bbc4fb` | rejected | нет отдельного файла (описано внутри autohand-report.md §Приёмка) |
| T019 | 07-17 | Autohand | Network widget fix (№2) | — | accepted w/ caveat | `autohand-report.md` → `report-log/T019-network-widget-fix-report.md` |
| T020 | 07-17→19 | Autohand | Tray context-menu popup (DBusMenu UI) — **никогда не принят**, вся ветка (задание + продолжение), superseded T040 | — | rejected/superseded | `autohand-report-3.md` → `rejected/T020-tray-menu-popup-report.md` |
| T021 | 07-17 | Cline | №1-5 агрегат (easing/spring, bar-мост, clock) | — | reconstructed-aggregate | нет файла (archive/CLINE.md preamble) |
| T022 | 07-17 | Cline | №6 настоящие иконки в tray | `b25dc97`+`8e7052a` | accepted | `cline-report (copy 2).md` (канон, оба коммита) → `report-log/T022-tray-icons-report.md`; `cline-report (copy 1).md` (черновик, 1 коммит) → `notes/superseded/T022-earlier-draft.md` |
| T023 | 07-17 | Hermes | №3-6 агрегат (notification daemon, popups+theme, workspaces, dispatch) | — | reconstructed-aggregate | `hermes-report.md` (surviving piece — №4 theme-крейт+popups) → `report-log/T023-hermes-n4-theme-notifications-report.md` |
| T024 | 07-17 | Hermes | №7 services follow-ups (`wired`, `has_battery`, flap fix) | — | accepted | `hermes-report (copy 1).md` → `report-log/T024-services-followups-report.md` |
| T025 | 07-17 | Hermes | №8 wallpaper service (awww MVP + multi-backend) | `de17aba`+errata `25a0e33` | accepted | `hermes-report-8-rework.md` (канон) → `report-log/T025-wallpaper-service-report.md`; `hermes-report-8.md` → `notes/superseded/T025-earlier-draft.md` |
| T026 | 07-17 | Grok | №2 OSD volume widget | folded into `f4edb88` | accepted | `grok-report.md` (канон) → `report-log/T026-osd-volume-widget-report.md`; `grok-report (copy 1).md` (дубликат) → `notes/superseded/T026-duplicate.md` |
| T027 | 07-17 | Grok | №3 audio dispatch + OSD errata | `6f24bb3`+`f4edb88` | accepted (реприманд) | `grok-report-3.md` **⚠ АНОМАЛИЯ** → `report-log/T027-audio-dispatch-osd-errata-report.md` (громкое предупреждение в шапке) |
| T028 | 07-17 | Grok | №4 volume widget | `d361ec2`+errata `b47f060` | accepted | `grok-report-4.md` → `report-log/T028-volume-widget-report.md` |
| T029 | 07-17 | Grok | №5 MPRIS service + widget | `d5a45ae`+errata `49b6fa5` | accepted | `grok-report-5.md` → `report-log/T029-mpris-service-widget-report.md` |
| T030 | 07-17 | — | **слито в T017** (Mimo apps-service rework, не отдельная задача) | — | merged→T017 | — |
| T031 | 07-17 | Mimo | №5 wallpaper control (IPC+cycler) | `e278a58` | accepted | `mimo-report-5.md` → `report-log/T031-wallpaper-control-report.md` |
| T032 | 07-17 | Mimo | №6 dock (pinned launcher panel) | `d646406` | **rejected** | `mimo-report-6.md` → `rejected/T032-dock-report.md` |
| T032a | 07-17 | OpenCode | №1/№2 tray widget + StatusNotifierWatcher service foundation (**найдено при миграции, не было отдельной строкой в исходной разведке**) | — | accepted (foundation, предшествует T033) | `opencode-report.md` → `report-log/T032a-tray-widget-service-foundation-report.md` |
| T033 | 07-17 | OpenCode | №3 DBusMenu service (2 захода + финал без отдельного файла) | `6782337`→`1d54ffd`→`f755db6` | reworked→accepted | `opencode-report-3-rework1.md` (канон) → `report-log/T033-dbusmenu-service-report.md`; `opencode-report-3.md` (первая попытка) → `notes/superseded/T033-first-pass.md` |
| T034 | 07-17 | Cline | №7 launcher закрывается от клика по себе | `3a692e4` | accepted | `cline-report-7.md` → `report-log/T034-launcher-self-click-bugfix-report.md` |
| T035 | 07-17 | Hermes | №9 notification popup обрезан снизу | — | rejected→superseded (T045) | нет отдельного файла (описано в archive/HERMES.md) |
| T036 | 07-18 | Grok | №6 `remove_window()` не убивает окно, Причина №1 | `3800d3a` (Source) | accepted | `grok-report-6.md` → `report-log/T036-remove-window-cause1-report.md` |
| T037 | 07-18 | Cline | №8 реальная причина ghost-window (реентерабельный close), Причина №2 | `0489c9c` | accepted | `cline-report.md` → `report-log/T037-launcher-ghost-window-cause2-report.md` |
| T038 | 07-18 | Cline | №9 launcher закрывается от движения мыши (debounce) | — (uncommitted) | **rejected** | `cline-report-9.md` → `rejected/T038-launcher-debounce-report.md` |
| T039 | 07-18 | Zed | №1 AUR/pacman helper (tray widget+badge) | `0fd2fb9` | accepted | `zed-report-1.md` → `report-log/T039-aur-widget-report.md` |
| T040 | 07-17→18 | Hermes | tray_menu context menu (перехвачено от Autohand) | `67ca90a` | accepted | `hermes-report-10.md` → `report-log/T040-tray-menu-report.md` |
| T041 | 07-19 | Architect | Launcher fix: no close-on-focus-loss (заменяет T038) | `fba8697` | accepted | нет файла (docs/HANDOFF.md "Живая приёмка") |
| T042 | 07-17→19 | Grok | №11 desktop_terminal spike | `b45cd07` | accepted (spike) | нет отдельного файла в report-log (archive/GROK.md) |
| T043 | 07-19 | Hermes | №11 notification popup обрезан (повтор) | — | superseded→T045 | нет файла |
| T044 | 07-19 | Hermes | №12 структурный фикс клипа попапов | `67f7d10` | accepted | `hermes-report-12.md`+companion `hermes-report-12b-confusion.md` → `report-log/T044-popup-clip-structural-fix-report.md` (оба объединены заголовком, тело не тронуто по отдельности — см. файлы) |
| T045 | 07-19 | Hermes | №13 визуальный паритет попапов | `8d74583` | accepted | `hermes-report-13.md` → `report-log/T045-popup-visual-parity-report.md` |
| T046 | 07-19 | Cline | №10 power-profiles-daemon реальная проводка | `2522018` | accepted | `cline-report-10.md` → `report-log/T046-power-profiles-report.md` |
| T047 | 07-19 | Cline | №11 workspace dots вместо номеров | `8457bbc` | accepted | `cline-report-11.md` → `report-log/T047-workspace-dots-report.md` |
| T048 | 07-19 | Mimo | №7 persistent pinned-list config | — | accepted | `mimo-report-7.md` → `report-log/T048-dock-persistent-config-report.md` |
| T049 | 07-19 | Mimo | №8 dock → в бар + Start-кнопка | `07df942` | accepted | `mimo-report-8.md` → `report-log/T049-dock-in-bar-report.md` |
| T050 | 07-19 | Grok | №12/№13 volume/mic popup (слайдер+пикер) | `66d66c3` | accepted | `grok-report-11.md`+`grok-report-12.md` → `report-log/T050-volume-mic-popup-report.md` |
| T051 | 07-19 | Grok | №13 cava audio-visualizer | `c519e2e`+`eb043fd` | accepted | `grok-report-13.md` → `report-log/T051-cava-visualizer-report.md` |
| T052 | 07-19 | Hermes | №14 notification history (bell+badge) | `f4ddd72` | accepted | `hermes-report-14.md` → `report-log/T052-notification-history-report.md` |
| T053 | 07-19 | Grok | №14 MPRIS multi-player | `a3d36ba` | accepted | `grok-report-14.md` → `report-log/T053-mpris-multiplayer-report.md` |
| T054 | 07-19 | Zed | №2 System popup (Phase 1 диагностика) | — | accepted (see T055a) | `zed-report-2.md` → `report-log/T054-system-popup-phase1-report.md` |
| T055 | 07-19 | Zed | №2 System popup финал (brightness+power-profile+gaming) | `f7de445` | accepted | нет отдельного финального файла — см. T054/T055a |
| T055a | 07-19 | Zed | System popup Phase 2 (companion к T054/T055) — bug2+gaming починены, display-фикс не работает | uncommitted | accepted-partial | `zed-report-3.md` → `report-log/T055a-system-popup-phase2-report.md` |
| T056 | 07-19 | Zed | "Phase 2" WIP — регрессия (откат `pult_display`→`window.display()`), поймана и отменена | uncommitted | **rejected/discarded** | `zed-report-2-Phase2-DISCARDED-superseded-by-consolidation.md` → `rejected/T056-system-popup-regression-discarded-report.md` |
| T057 | 07-19 | Mimo | №9 project switcher — briefed, заблокирован | — | open→closed by Architect (см. T085) | нет файла |
| T058 | 07-19 | Mimo | №10 consolidation: chrome на пультовый монитор | `0a99a67` | accepted | `mimo-report-10.md` → `report-log/T058-monitor-consolidation-report.md` |
| T059 | 07-19 | Zed | №4 gpui-form recon — НЕ выполнено (терминал сломан) | — | recon-failed, честно | `zed-report-4.md` → `report-log/T059-gpui-form-recon-failed-report.md` |

## T060–T089 — волна редизайна + vendor-recon (2026-07-20 → 07-21)

| T-ID | Дата | Агент | Описание | Коммит | Статус | Старый путь → новый |
|---|---|---|---|---|---|---|
| T060 | 07-20 | Cline | recon №1 gpui-form (перехвачено от Zed) | — | accepted (recon) | `cline-report-gpuiform.md` → `report-log/T060-gpui-form-recon-report.md` |
| T061 | 07-20 | Cline | recon №2 gpui-rsx | — | accepted (recon) | `cline-report-rsx.md` → `report-log/T061-gpui-rsx-recon-report.md` |
| T062 | 07-20 | Cline | vendor gpui-rsx в `Source/` | `99cab5e` | accepted | `cline-report-rsx-vendor.md` → `report-log/T062-gpui-rsx-vendor-report.md` |
| T063 | 07-20 | Grok | recon №18 gpui-animation | — | accepted (recon) | `grok-report-animation.md` → `report-log/T063-gpui-animation-recon-report.md` |
| T064 | 07-20 | Grok | vendor gpui-animation в `Source/` | `66cd816` | accepted | `grok-report-animation-vendor.md` → `report-log/T064-gpui-animation-vendor-report.md` |
| T065 | 07-20 | DeepSeek | №1 network widget → activity light (первая попытка) | — (uncommitted) | **rejected** (render() side-effect) | `deepseek-report-1.md` → `rejected/T065-network-widget-first-attempt-report.md` |
| T066 | 07-20 | DeepSeek | №1 rework: rate-over-time с time-gate | `0838446` | accepted | `deepseek-report-2.md` [ambiguous — см. п.12] → `report-log/T066-network-widget-rework-report.md` |
| T067 | 07-20 | Grok | №15 7 попапов на палитру docs/STYLE.md | `1d736da` | accepted | `grok-report-15.md` → `report-log/T067-popups-palette-sweep-report.md` |
| T068 | 07-20 | Mimo | №11 добивка эмодзи в баре (5 SVG+hover+CAVA) | `6723493` | accepted | `mimo-report-11.md` → `report-log/T068-bar-emoji-killed-report.md` |
| T069 | 07-20 | GLM | №1 Light C схема + `CHRONOS_THEME` | `0f0ee88` | accepted (эталонный отчёт) | `glm-report-1.md` → `report-log/T069-light-c-scheme-report.md` |
| T070 | 07-20 | Architect | `on_fill()` примитив + Latte-статусы + числовой badge | `009853f` | accepted | нет файла (мандат Архитектора) |
| T071 | 07-20 | Architect | Workspace dots динамические (fix subscribe-баг) | `608b584` | accepted | нет файла |
| T072 | 07-20 | Grok | №16 светлая тема: launcher tokens, power-profile on_fill | `3f6e165` | accepted | `grok-report-16.md` → `report-log/T072-light-theme-launcher-report.md` |
| T073 | 07-20 | GLM | №2 тема из config-файла + hot-reload | `5bb6c77` | accepted | `glm-report-2.md` → `report-log/T073-theme-hotreload-report.md` |
| T074 | 07-20 | Hermes | №16 tray decluttering (фильтр+дедуп+кап) | `7eada8b` | accepted | `hermes-report-16.md` → `report-log/T074-tray-decluttering-report.md` |
| T075 | 07-20 | Mimo | №12 upgrade-feedback (`UpgradeState`) | `79c8baa`+errata `b25452c` | accepted | `mimo-report-12.md` → `report-log/T075-upgrade-feedback-report.md` |
| T076 | 07-20 | Mimo | №13 upgrade-output в попап-хвост | briefed | open→заменено позже | нет файла |
| T077 | 07-20 | Architect | Fork-scroll retraction (`.overflow_y_scroll()` работает, нужен `.id()`) | — | decision (retraction) | нет файла (docs/DECISIONS.log) |
| T078 | 07-20 | Mimo | №14 recon state/async/executors | — | **не принято / отменено** | нет файла |
| T079 | 07-20 | Grok | №17 recon elements/styling/layout/scroll | `f4d2ebc` | accepted (recon) | `grok-report-17.md` → `report-log/T079-fork-recon-elements-report.md` |
| T080 | 07-20 | Hermes | №17 recon windowing/platform/layer-shell | `f7099e5` | accepted (recon, самая ценная зона) | `hermes-report-17.md` → `report-log/T080-fork-recon-windowing-report.md` |
| T081 | 07-20 | OpenCode | №4 recon examples corpus + gpui-component каталог | `cbfc197` | accepted (recon) | `opencode-report-4.md` → `report-log/T081-fork-recon-examples-report.md` |
| T082 | 07-20 | DeepSeek | №2 (бывшее №14) recon state/async/executors | `9c2c090` | accepted (recon) | нет файла [см. п.12 — не путать с T066] |
| T083 | 07-20 | Architect/Hermes | №15 token foundation (Catppuccin, font_mono, BAR_HEIGHT 30) | `3e04264` | accepted | `hermes-report-15.md` → `report-log/T083-bar-token-foundation-report.md` |
| T084 | 07-20 | Architect | Bar layout (clock right, MPRIS/CAVA reorder, separator) | `c7ccc02` | accepted | нет файла (мандат) |
| T085 | 07-20 | Architect | SVG icon infra (assets.rs, 8 иконок) | `f370618` | accepted | нет файла (мандат) |
| T086 | 07-20 | Architect | Project switcher (закрывает T057) | `6061736` | accepted | нет файла (мандат) |
| T087 | 07-21 | Hermes | №18 gpui-component compile recon | — | accepted (recon) | `hermes-report-18.md` → `report-log/T087-gpui-component-recon-report.md` |
| T088 | 07-21 | Hermes | №19 gpui-component pilot (замер цены) | branch `pilot/gpui-component-spike` | accepted (recon, число исправлено Архитектором) | `hermes-report-19.md` → `report-log/T088-gpui-component-pilot-report.md` |
| T089 | 07-21 | Hermes | ccf-gpui-widgets recon | — | accepted (recon), vendoring отложен | `hermes-report-widgets-recon.md` → `report-log/T089-ccf-widgets-recon-report.md` |

## T090–T101 — правая боковая панель v1+v2 (2026-07-21)

| T-ID | Дата | Агент | Описание | Коммит | Статус | Старый путь → новый |
|---|---|---|---|---|---|---|
| T090 | 07-21 | Cline | Task 1: `net_stats` shared module + widget | `dbce8ac` | accepted | `cline-report-1.md` → `report-log/T090-net-stats-report.md` |
| T091 | 07-21 | DeepSeek | Task 2: `Theme::font_ui` ("Inter") | `18c88f0` | accepted (нет потребителя) | нет файла в report-log |
| T092 | 07-21 | Grok+GLM | Tasks 3/4/5: `system_resources`+`power` backends | `bf5b683` | accepted (один коммит) | `grok-report-19.md`+`glm-report-3.md` → `report-log/T092-system-resources-power-report.md` |
| T093 | 07-21 | Zed | Task 6: per-app stream mute в audio | `984c799` | accepted (1 неточность в отчёте) | `zed-report-6.md` → `report-log/T093-audio-stream-mute-report.md` |
| T094 | 07-21 | Hermes | Task 7: `side_panel_right` window skeleton | `da744a2` | accepted | `hermes-report-7.md` (канон) → `report-log/T094-side-panel-skeleton-report.md`; `hermes-report-7-duplicate.md` → `notes/superseded/T094-duplicate.md` |
| T095 | 07-21 | Hermes | Tasks 8/9: hover-peek strip + MPRIS card | `8c05197` | accepted | `hermes-report-20.md` → `report-log/T095-hover-peek-mpris-card-report.md` |
| T096 | 07-21 | Hermes | Tasks 10/11: spectrum meters + power-row + geometry | `1e93209` | accepted | `hermes-report-21.md` → `report-log/T096-spectrum-power-geometry-report.md` |
| T097 | 07-21 | Architect | Panel/strip до низа дисплея + power-row 4-tile | `b120a3d` | accepted | нет файла (мандат) |
| T098 | 07-21 | Hermes | Track 1: Sidebar v2 pixel-по-мокапу на gpui-rsx | `7109860` | accepted | `hermes-report-22.md` → `report-log/T098-sidebar-v2-track1-report.md` |
| T099 | 07-21 | Zed | Track 2: udisks2 `DisksSubscriber` живые диски | `8c8ccb7` | accepted (ops-оговорка eject-whole-drive) | `zed-report-7.md` → `report-log/T099-udisks2-disks-report.md` |
| T100 | 07-21 | Grok | Track 3: MPRIS art/progress/timecode | `3d9b8b3` | accepted | `grok-report-20.md` → `report-log/T100-mpris-art-progress-report.md` |
| T101 | 07-21 | — | Медиа-видео закрыт на cover→idle (решение, не задача) | — | decision only | нет файла |

## T102–T106 — открытое сейчас (2026-07-22)

| T-ID | Агент | Описание | Статус | Путь брифа |
|---|---|---|---|---|
| T102 | не назначен | Task 12 — бар-триггер интеграции side_panel_right | **OPEN** | `docs/orchestration/tasks/active/T102-bar-trigger-integration.md` |
| T103 | Cline | Chronos-AUR порт, Трек A — движок aur-core | **WIP** | `docs/orchestration/tasks/active/T103-chronos-aur-track-a-engine.md` |
| T104 | Grok | Chronos-AUR порт, Трек B — shell-exec fish/zsh/bash | **WIP** | `docs/orchestration/tasks/active/T104-chronos-aur-track-b-shell-exec.md` |
| T105 | Hermes | Chronos-AUR порт, Трек C — GPUI-каркас aur-app | **WIP** | `docs/orchestration/tasks/active/T105-chronos-aur-track-c-app-shell.md` |
| T106 | Zed | Chronos-AUR порт, Трек D — порт страниц React→rsx | **WIP** | `docs/orchestration/tasks/active/T106-chronos-aur-track-d-pages.md` |

## T107–T109 — левая agent-панель (2026-07-23)

| T-ID | Агент | Описание | Статус | Путь брифа |
|---|---|---|---|---|
| T107 | Zed | Левая панель — скелет agent-панели (T107 план из 522232e) | accepted | `docs/orchestration/tasks/done/T107-left-agent-panel.md` |
| T108 | смешанный (миньоны + живой дебаг архитектора) | Agent switcher + ACP modes/models + task3 click-fix. п.9 resize (fbcadd6); #6 modes/models accepted; task3 dropdown click accepted 07-24. Долг: #7 jank, #8/#8-bis ghost-trail (fork), live round-trip после prompt | **accepted** | `docs/orchestration/tasks/done/T108-left-panel-agent-switcher.md`; `report-log/T108-*` (task1–3 + reviews) |
| T118 | не назначен (миньон) | Live upgrade output: stream stderr, UpgradeProgress, spinner/bar/line, staircase filter | **accepted with caveats** (07-24) | `done/T118-...`; `report-log/T118-*-report.md` + review; errata stdout null |
| T119 | не назначен (миньон) | Multi-select + Upgrade selected (`-S` not `-Syu`) + Check header | **accepted with caveats** (07-24, live PENDING) | `done/T119-...`; `report-log/T119-*`; commit `eac0591` |
| T109 | Zed | Agent Thread canvas: чат-часть по мокапу `docs/design/Agent Thread.dc.html` (unified composer, C-2 gpui-component TextInput blocked → homemade fallback, YOLO=bypass, тёмный send) | `10fa206` | **accepted** (живой смок архитектором 07-24) | `docs/orchestration/tasks/done/T109-agent-thread-canvas.md`; отчёт `report-log/T109-agent-thread-canvas-report.md` |

## T900 — вне последовательности (misassigned)

| T-ID | Агент | Описание | Статус | Старый путь → новый |
|---|---|---|---|---|
| T900 | Mimo | Tech-stack deep-research — не та задача (см. п.11) | rejected | `mimo-report-techstack-REJECTED-wrong-task.md` → `rejected/T900-techstack-research-misassigned-report.md` |

## Некросс-катные аудиты и артефакты без T-ID → `docs/orchestration/tasks/notes/`

`chronos_services_rewrite_cost_report.md`, `REWRITE_BY_PATTERN_AUDIT.md`,
`SHADER_PORT_AUDIT.md`, `kael_motion_port_audit.md`,
`philip-report-maintain-2026-07-21.md`, `architect-report-philip-maintain.md`,
`SESSION_REPORT.md`, `Compact summary.md`, `docs/HANDOFF.md` (stray snapshot, см.
п.9, → `notes/stray-handoff-snapshot.md`).

## T110–T112 — hot-reload bake-off + IDE-панель фундамент (2026-07-24)

| T-ID | Агент | Описание | Коммит | Статус | Путь |
|---|---|---|---|---|---|
| T110 | OpenCode | hot-reload bake-off, Трек A (`hot-lib-reloader` + `crates/hotview`) | `ea65be5`/`d0075ff` (спайк) → `b07eacd` (merge в master) | **accepted, победитель** | `done/T110-hot-reload-track-a-hotlibreloader.md`; отчёт `report-log/T110-hot-reload-track-a-hotlibreloader-report.md` |
| T111 | GLM | hot-reload bake-off, Трек B (`subsecond`) | нет (не собрался — `unsafe` API против workspace `unsafe_code=deny`, воспроизведено архитектором) | **accepted, проиграл по спеке (валидный исход)** | `done/T111-hot-reload-track-b-subsecond.md`; отчёт `report-log/T111-hot-reload-track-b-subsecond-report.md`; ветка `spike/hot-reload-track-b` архивирована, не удалена |
| T112 | DeepSeek | IDE-панель — фундамент таб-контейнера (rail + System + 9 заглушек) | `0e10e51` | **accepted** (правка сверх плана архитектором: rail → правый край экрана) | `done/T112-ide-panel-tab-container.md`; отчёт `report-log/T112-ide-panel-tab-container-report.md` |

## T120 — notifications history popup (2026-07-24)

| T-ID | Агент | Описание | Статус | Путь |
|---|---|---|---|---|
| T120 | не назначен | History popup: anchored + mockup UI + RemoveFromHistory/ClearHistory | **accepted with caveats** (`253f25b` errata) | `done/T120-...`; `report-log/T120-*` |

## T121 — volume popup (2026-07-24…25)

| T-ID | Агент | Описание | Статус | Путь |
|---|---|---|---|---|
| T121 | не назначен | Volume Sound: anchored + sliders + blur/anim + dual-marker fix | **accepted with caveats** (`54a54c0`/`1ad55c2`/`9597a10`) | `done/T121-...`; `report-log/T121-*` |

## T122 — dev shell CLI scripts (2026-07-25)

| T-ID | Агент | Описание | Статус | Путь |
|---|---|---|---|---|
| T122 | не назначен | chronos rebuild/reload/stop/start/debug | **accepted with caveats** (`23c5cda`) | done/T122-...; report-log/T122-* |

## T123 — audio drag coalesce (2026-07-25)

| T-ID | Агент | Описание | Статус | Путь |
|---|---|---|---|---|
| T123 | не назначен | volume drag coalesce + Context API errata | **accepted with caveats** (`5cad0bb`/`c6d7bee`) | `done/T123-...`; `report-log/T123-*` |

## T124 — ephemeral toast stack (2026-07-25)

| T-ID | Агент | Описание | Статус | Путь |
|---|---|---|---|---|
| T124 | не назначен | ephemeral toast stack | **accepted with caveats** (`813b3aa`) | done/T124-...; report-log/T124-* |

## T125 — system popup (2026-07-25)

| T-ID | Агент | Описание | Статус | Путь |
|---|---|---|---|---|
| T125 | не назначен | system popup anchored + brightness DDC debounce errata | **accepted with caveats** (`fc71215`/`2be1a91`/`87cab1e`) | `done/T125-...`; `report-log/T125-*` |

## T126 — left panel sessions sidebar + dock exclusive (2026-07-25)

| T-ID | Агент | Описание | Статус | Путь |
|---|---|---|---|---|
| T126 | не назначен | Sessions sidebar = bar (~36 collapsed); kill is_rail; chat overlay; Dock exclusive switch | **OPEN / errata in tree** (`f89e27d`); live smoke pending | `active/T126-...`; review `report/T126-*-review.md` |

## T127 — right panel rail exclusive + content dock (2026-07-25)

| T-ID | Агент | Описание | Статус | Путь |
|---|---|---|---|---|
| T127 | не назначен | Tab rail = bar (exclusive); content overlay; Dock full; Super+G IPC | **OPEN** (supersedes “always exclusive” draft) | `active/T127-right-panel-exclusive-resize.md` |

## T128–T132 — visual depth wave (2026-07-25)

| T-ID | Агент | Описание | Статус | Путь |
|---|---|---|---|---|
| T128 | не назначен | Elevated surface + blur tokens; apply popups/panel chrome | **OPEN** | `active/T128-elevated-surface-blur-tokens.md` |
| T129 | — | Panel/popup enter-exit (scale+opacity springs) | **QUEUED** (after T128) | MEMORY/HANDOFF |
| T130 | — | Toast enter/exit motion | **QUEUED** (after T129 pattern) | MEMORY/HANDOFF |
| T131 | — | Fork spike: 3D scene primitive + gpui example | **QUEUED** (after shell polish) | MEMORY/HANDOFF |
| T132 | — | Wire one 3D demo surface in ChronOS shell | **QUEUED** (after T131) | MEMORY/HANDOFF |

## T133 — wallpaper / waytrogen integration (2026-07-25)

| T-ID | Агент | Описание | Статус | Путь |
|---|---|---|---|---|
| T133 | не назначен | Optional waytrogen gallery + IPC/UI; no UI rewrite | **OPEN** | `active/T133-wallpaper-waytrogen-integration.md` |

## T137–T143 — ACP left panel revive (2026-07-26 … 2026-07-27)

Спека: `docs/superpowers/specs/2026-07-26-acp-panel-revive-design.md`.

| T-ID | Агент | Описание | Статус | Путь |
|---|---|---|---|---|
| T137 | не назначен | Chat must work (Phase A) | **ACCEPTED** (`af54fb0`, user chat) | `done/T137-*`; report `report-log/T137-*` |
| T138 | не назначен | Multi-agent registry + agents.toml + shared .env | **ACCEPTED w/ caveats** (`82405c3`) | `done/T138-*`; report `report-log/T138-*` |
| T139 | не назначен | ChronOS density/identity левой панели | **ACCEPTED w/ caveats** (`66a86f5`) | `done/T139-*`; report `report-log/T139-*` |
| T140 | не назначен | Permission auto-approve (YOLO) | **ACCEPTED w/ caveats** (`36e8399`) | `done/T140-*`; report `report-log/T140-*` |
| T141 | не назначен | Tool cards + reasoning UI | **ACCEPTED w/ caveats** (`36e8399`) | `done/T141-*`; report `report-log/T141-*` |
| T142 | не назначен | Model picker | **ACCEPTED w/ caveats** (`36e8399`); живой список моделей пуст → D5 в T143 | `done/T142-*`; report `report-log/T142-*` |
| T143 | **Hermes** | Честный ClientCapabilities (D0), живучесть turn'а, достоверность тул-карточек | **ACCEPTED** (заходы 3-4) — стриминг живой, 975 событий, `turn END (reason=ok)`. P0-лайвлок оказался не в зоне Hermes: coop-бюджет tokio на главном потоке, фикс архитектора `44ba823` | `done/T143-*`; report `report-log/T143-*` |
| T144 | **Hermes** | Непустой селектор модели в панели | **ACCEPTED, закрыта 2026-07-28 после захода 4** (`a44e9bd` — `max_h` + `overflow_y_scroll` на дропдаунах). Живой прогон архитектора (ydotool+grim): список раскрывается и не уезжает, четыре смены модели подряд, у агента `model switched to …` и следующий turn на выбранной. История заходов: З.1 (`89b44e0`) перехват `session/new`; з.2 (`ea6a0c7`) `SharedModels` вместо глобала + `#301`; з.3 (`b5116ee`) D6 — смена модели уходит `session/set_model` через `UntypedMessage`, замерено архитектором (на проводе `session/set_model`, у агента `model=anthropic/claude-opus-4`). Отчёты з.1 и з.3 отклонены: з.3 правил `docs/HANDOFF.md` вне зоны и вписал «D6 закрыто» при непроверенном смоуке. Осталось: раскрытый дропдаун на кадре `grim` (нужен клик) | `done/T144-*`; отчёты `rejected/` (з.1, з.3), `report-log/` (з.2, з.4) |
| T145 | **Hermes** | Бамп `agent-client-protocol` 0.11.1 (альфа) → 2.0.0 (релиз) | **КОД ПРИНЯТ, ОТЧЁТ ОТКЛОНЁН** — бамп настоящий (lock 2.0.0, `-tokio` вычищен, 42+176 тестов), живой смоук сделал архитектор: 12-мин ход, 10/10 тулов закрыты, 8 файлов на диске. В отчёте три выдумки: несуществующая ветка, несуществующий PR в чужой орг, и `Live ACP tool calls ✅` при нуле `session/prompt` на проводе | `done/T145-*`; отчёт `rejected/T145-*` |
| T146 | **Hermes** | Эрраты E1-E5 после лайвлока: traceback-эскалация, потолок хода, таймаут панели, немые стоки | **ACCEPTED w/ erratum** — E1/E3/E5 чисто; E2 «абсолютный» дедлайн проверялся только по тишине → вынесен в T147. E4 оказался багом адаптера агента, пропатчен вне ChronOS (10/1 → 19/18 апдейтов) | `done/T146-*`; report `report-log/T146-*` |
| T147 | **Hermes** | Настоящий потолок хода: перенести проверку в начало цикла, поднять до 30 мин, имя = поведению | **ACCEPTED** (`729c440`) — проверка первой строкой цикла, 1800 с на обоих контурах (`read_turn` закрыт в `89b44e0`), 42+176 тестов сверены архитектором. Живой лог срабатывания снял архитектор (константа урезана до 10 с, стриминговый ход на 159 событий, `extensions=0` → `absolute deadline hit`, `$/cancel_request` агенту). В отчёте снова выдумана ветка — как в T145 | `done/T147-*`; report `report-log/T147-*` |
| T148 | **FRONTEND** | Транскрипт агента: тулы наверх, живое размышление со сворачиванием, ответ отдельным блоком снизу | **ACCEPTED** (`9a765a2`) — порядок `tool_cards → reasoning → content`, свой скролл у размышления, `collapsed_reasoning` + авто-разворот во время стрима. Живой кадр архитектора `notes/T148-order-live.png`: 6 тул-карточек сверху, reasoning под ними, ответ снизу; рост подтверждён двумя кадрами с интервалом. Ручной клик по шапке — PENDING (синтетический клик не улика) | `done/T148-*`; report `report-log/T148-*` |
| T149 | **FRONTEND** | Строка поиска в списке моделей (288 штук) | **OPEN** — фильтрация вводом, Enter=первый результат, Esc чистит; сейчас любая клавиша при открытом дропдауне его закрывает | `active/T149-model-picker-search.md` |
| T150 | **BACKEND** | Хранилище тредов: SQLite `~/.local/share/chronos/threads/` + ACP `session/list` и `session/load` | **OPEN** — решение в docs/DECISIONS.log 2026-07-28: содержимое разговора остаётся за агентом (Hermes умеет load/list/resume), у нас метаданные + кэш | `done/T150-thread-store.md` — ЗАКРЫТА 29.07: SQLite-хранилище + типизированные session/list и session/load; приёмка со второго захода (Cargo.lock, rusqlite 0.40, expect, типы ACP) |
| T151 | **FRONTEND** | UI тредов: настоящий список, возобновление через `session/load`, переименование/пин/архив, поиск | **OPEN, разблокирована 29.07** — T150 закрыта | `active/T151-thread-list-ui.md` |
| T152 | **FRONTEND** | Иврит/RTL в панели: глифы, выравнивание по содержимому, композер | **OPEN** — выросла из предложения агента (`agent-suggestions/`), проверено архитектором: факты верны, но API направления в форке НЕТ (только `text_align`), а «тофу» — гипотеза (в системе DejaVu с ивритом есть, Noto нет). Порядок: сначала замер кадром, потом код | `done/T152-hebrew-rtl-render.md` — ЗАКРЫТА 29.07 с четвёртого захода: d8920c1 (word-chars) + de62111 (перенос при убывающих x) + 86701db (позиция RTL-строки у визуального конца) |
| T153 | **FRONTEND** | Флоу транскрипта: лента сегментов (размышление/тул/ответ) вместо двух буферов | **OPEN** — продолжение T148; сейчас чанки склеиваются в `content`/`thought`, хронология теряется. Новый сегмент открывается при смене вида чанка; тул стоит там, где случился | `active/T153-transcript-flow-segments.md` |
| T154 | **FRONTEND** | Композер как настоящее текстовое поле: каретка, выделение, Ctrl+C/V/X, drop файлов, индексы по символам | **OPEN** — жалоба пользователя «copy paste, drag and drop, that blinking stick». Эталон в дереве: `Source/gpui/examples/input.rs` (778 стр., каретка/выделение/IME/клипборд), `drag_drop.rs`, `ExternalPaths`. Развилка A (модуль в app) vs B (виджет в crates/ui) — рекомендация B вторым коммитом | `active/T154-composer-real-text-input.md` |
| T155 | **FRONTEND** | Перенос `gpui-component` целиком (фичи + проводка + замер за один заход) | **ЗАМОРОЖЕНА** — заход убит двумя вещами: фичи объявлены без `#[cfg]` в коде (10 × E0432) и компонент добавлен членом `Source/Cargo.toml` без снятия его `[workspace]` → `multiple workspace roots`, любой `cargo` в `Source/` падал. Всё откачено; снесённый `LICENSE-APACHE` восстановлен. Разбита на T156/T157/T158 | `done/T155-gpui-component-wiring.md` — закрыта 29.07, поглощена T156–T158 |
| T156 | **FRONTEND** | `gpui-component`: cfg-гейты и матрица фич в отдельном worktree, без проводки | **ЗАКРЫТА 2026-07-29** — фичи `markdown`/`html`/`time`/`chart`/`lsp` опциональны и размечены; `lsp` тянет `markdown` (LSP-поповеры зовут `TextView::markdown`); `input::Position` развязан от `lsp_types`; ловушка инспектора закрыта `all(any(inspector, debug_assertions), lsp)`. Приёмка архитектора: `cargo clean -p` → 7 × `check` + release `--no-default-features` — 0 ошибок. Первый отчёт объявлял `--all-features` зелёным при 23 × E0308 — отклонён эрратой, исправлено. Косметика `cargo fmt` вынесена во второй коммит по требованию. Worktree `Source-wt-component`, ветка `component/feature-gates`, коммиты `6118382` + `06ace12`. Дальше T157 (проводка и замер, база 22 475 648 байт) | `done/T156-gpui-component-feature-gates.md` |
| T157 | **FRONTEND** | `gpui-component`: проводка в ChronOS и замер со шлюзом | **ЗАКРЫТА 2026-07-30** (пять заходов). Итог: **+2 058 432 байта (+1.96 MiB)** за `Input+Table+VirtualList`, из них 91 % — сам `Input` (+1 844 288). `Table` +199 168, `VirtualList` +14 976 (почти бесплатен — `v_virtual_list` по сути макрос). Гейты T156 подтверждены в живом графе: `lsp-types`/`html5ever`/`markdown` отсутствуют. Приёмка архитектора: `stat` базы и финала совпал до байта, размер кадров 560×1410 совпал с геометрией слоя из `hyprctl layers` до пикселя, живой ввод доказан дописыванием текста (`T157 real input` → `T157 real inputT157 round5 live`), лог без паник. Заход 4 отклонён за галочку на непроверенном кадре — исправлено заходом 5. Три находки в T158: `Root` обязателен (иначе `Input` паникует на `window.root()`), нужен `KeyboardInteractivity::OnDemand`, `num-traits` приезжает через `rust-i18n → serde-saphyr`, а не от фичи `chart`. Плюс баг: `side_panel_right/mod.rs:187` ставит `state.width = RAIL_ONLY_WIDTH` ПОСЛЕ `cx.open_window` — `CHRONOS_SMOKE_SIDE_PANEL` открывает панель полоской | `done/T157-gpui-component-wiring-and-measure.md`, отчёт в `report-log/` |
| T158 | **FRONTEND** | `gpui-component`: усыновление — проверка премиссы обрезки, `Root`/`OnDemand` в постоянную проводку, баг ширины smoke-флага | **ЗАКРЫТА 2026-07-30** — принята с эрратой. **Премисса обрезки мертва:** вырезан модуль `setting` (1930 строк) → бинарь изменился на **128 байт** (24 578 112 против 24 577 984). `lto`+`strip` уже всё выбросили; резать исходники компонента ради размера бессмысленно, вопрос закрыт навсегда. `Root` и `OnDemand` оформлены постоянной проводкой с комментариями «почему»; баг ширины починен (`window_options` читает `state.width`, сброс в rail-only перенесён ДО `cx.open_window`, smoke-путь раскрывает заранее). 179 тестов зелёные (прогнал сам). **Эррата:** §4.2 отчёта заявлял кадр с введённым текстом, а в кадре стоял старый смоук-текст `T157 real input` — координаты `ydotool` взяты полные (2131) вместо половинных, о которых прямым текстом написано в отчёте T157 часом ранее. Дозакрыто архитектором живьём: калибровка по `hyprctl cursorpos`, `-x 1132 -y 89`, кадр `/tmp/t158-verify-typed.png` содержит `T157 real inputT158 live input` — `OnDemand` доказан. Код в `master` черри-пиком `2e42b36` | `done/T158-gpui-component-adoption.md`, отчёт в `report-log/` |
| T159 | **RECON** | Разведка под слайс 1 Shell-IDE: иконки, токены темы, перерисовка бара, прецедент нескольких `on_click` в виджете бара | **ЗАКРЫТА 2026-07-30** — принята с эрратой. Ответы: `code.svg`/`gamepad.svg` **не существуют** (36 файлов в каталоге), берём `rail-editor.svg`+`bolt.svg` с TODO; все 7 токенов темы на месте; `cx.refresh_windows()` достаточно, бар читает глобал прямо в `render()` (`bar/mod.rs:68`), watch не нужен; **прецедента трёх независимых `on_click` в одном виджете бара НЕТ** — плашка предложения будет первой, риск event bubbling назван заранее. Счётчики обработчиков сверил сам — совпали полностью. **Эррата:** номера строк в Q2 восстановлены по памяти и промахнулись на 8–17 строк (`BgColors` заявлен ~87, фактически 70) — при стандарте роли «цитата с путём и строкой или это не факт». Плюс отчёт лёг в `report/` вместо `notes/`, два протёкших иероглифа, раздел F5.1 сам себя снимает. План слайса 1 обновлён по итогам | `done/T159-workspace-mode-recon.md`, результат в `notes/` |
| T160 | **BACKEND** | `workspace_mode`: глобал Developer/Gamer, конфиг `workspace.toml`, env-оверрайд, IPC `toggle-workspace-mode`/`set-workspace-mode:<mode>`, логика предложения смены | **ЗАКРЫТА 2026-07-31** — принята с эрратой. Состояние, персистентность, env-оверрайд и контракт предложения (`PromptPref{Ask,Never}`, `should_prompt`, `request_switch` не переключает) сделаны чисто; зона соблюдена безупречно (5 файлов, ни одного из `bar/**`); 14 тестов в `workspace_mode`, 193 по бинарю — прогнал сам. Отступление по `let _ = cx.update(...)` обосновано верно: в форке `AsyncApp::update` возвращает `R`, не `Result` (`async_context.rs:163`) — **ошибка была в моём плане**. **Дефект:** ветка диспетча в `ipc/service.rs::accept_loop` не написана — канал проложен end-to-end, арм в `mod.rs` ждёт, а отправлять некому; пейлоад проваливался сквозь `else if` и терялся. Компилятор кричал `unused imports: classify_set_workspace_mode, is_toggle_workspace_mode`, отчёт списал это на ствол. Юнит-тесты не ловят (проверяют чистые функции `messages.rs`). Исправлено архитектором, эррата `ddedf0a`, 6 строк. Живьём после фикса: set/toggle/мусор, персистентность через рестарт, env перебивает конфиг не перезаписывая его, 0 паник | `done/T160-workspace-mode-state-and-ipc.md`, отчёт в `report-log/` |
| T161 | **FRONTEND** | Переключатель режима в правом кластере бара + плашка предложения смены | **ЗАКРЫТА 2026-07-31** — код чистый, зона безупречна (3 файла), 194 теста, токены через `Theme::global`, уникальные `.id()` на четырёх кликабельных. Живьём: виджет в правом кластере между project-пилюлей и громкостью, обе иконки рисуются, часы остались крайними справа, клик переключает Developer⇄Gamer с записью в `workspace.toml`, 0 паник. **Оформление:** работа была НЕ закоммичена (три файла в рабочем дереве, ноль коммитов на заявленной ветке) — оформил сам, `93998a0`, плюс ребейз на актуальный master. **Находка уровня плана → T163:** `BarLayoutConfig::load()` читает `~/.config/chronos/bar.toml`, и при наличии файла `Default` не применяется никогда — новый виджет невидим у всех, кто трогал раскладку. Плашка предложения и event bubbling не проверены (нечем вызвать `request_switch`) → T162 | `done/T161-workspace-mode-bar-switcher.md`, отчёт в `report-log/` |
| T163 | **FRONTEND** | `bar.toml`: новый builtin-виджет не доезжает до существующего пользователя | **ЗАКРЫТА 2026-07-31** — принята с ДВУМЯ эрратами. Вариант B (поле `known`, двухфазность) выбран и обоснован верно: на первом взгляде «никогда не видел» от «удалил сознательно» не отличить, поэтому пишем текущий набор и добавляем только появившееся позже; цена — два рестарта для виджета из того же релиза. **Дефект 1:** bootstrap заполнял `known` в памяти и возвращал `false`, а `load()` персистит только при `true` — поле не попадало на диск, фаза 2 не наступала НИКОГДА; починка де-факто требовала ручного сохранения через edit-mode, то есть ровно того, ради чего задача заводилась. Доказано живьём: два рестарта, конфиг не тронут. **Дефект 2:** якорь вставки искался только среди успешников — на пользовательском кластере, начинающемся с `separator`, виджет уезжал в позицию 0 вместо места за `project`; `separator` вдобавок повторяется и якорем не годится. Оба починены архитектором (`7b54ba2`) + два теста на связку фаз и на якорь. Живьём после фикса: рестарт 1 пишет `known`, рестарт 2 вставляет виджет сразу за `project`, удалённый `volume` НЕ воскрес, 201 тест зелёный. Работа была не закоммичена и велась прямо в главном дереве на `master` | `done/T163-bar-layout-new-builtin-migration.md`, отчёт в `report-log/` |
| T162 | **QA** | Живой смок слайса 1: восемь пунктов + доказательство, что режим не переключается сам | **ЗАКРЫТА 2026-07-31** — принята **8/8 PASS, без эррат**, первый отчёт слайса без единой правки. Главное доказано: **режим не переключается сам** — статика (`set`/`toggle` зовутся только из `ipc/mod.rs:145,148` и клика `bar/widgets/workspace_mode.rs:59`; `request_switch` в проде не зовёт никто — мёртвая точка входа под будущий детектор, и это правильно) плюс живой soak на `mode = "gamer"` с fullscreen-окном. Приёмка архитектора сверена по дереву: греп вызовов, лог `/tmp/t162/chronos-smoke.log` (ровно 5 `switched` — все от IPC/кликов; мусорный пейлоад `set-workspace-mode:мусор` даёт `accept_loop payload` **без** `received`/`switched`, процесс жив; `prompt silenced app_id=smoke_app_t162`), `bar.toml` (виджет после `project`, `clock` крайний, `cava` центр, `known` заполнен), иконки `rail-editor.svg` (477 B) + `bolt.svg` (218 B) на диске, `code.svg`/`gamepad.svg` не существуют — как решено в T159. Зонд P7 (временный `request_switch` под `CHRONOS_SMOKE_PROMPT`) откатан полностью, продуктовый код не менялся — проверил грепом. **Отступления, принятые осознанно:** soak 91 с вместо запрошенных 5 мин (при нуле вызовов из не-пользовательских путей статика сильнее таймера; вернуть таймерный soak, когда появится детектор); клик «Да» на плашке отдельно не гонялся (`accept_prompt` зовёт тот же `set`, доказанный IPC и кликом пилюли, а «Не спрашивать» покрыт живьём как более жёсткий контракт). **Косметика №1 отчёта снята архитектором:** «слипшиеся пробелы» на плашке — артефакт vision-чтения 12px, глазами на кадре `18-prompt-banner-right.png` пробелы на месте, тикет не заведён. Подтверждено третий раз: **`ydotool` absolute = screen / 2** на этой машине | `done/T162-workspace-mode-smoke.md`, отчёт в `report-log/` |

| T164 | **BACKEND** | Scene-модель: состояние вне панелей, персист `~/.config/chronos/scenes.toml`, восстановление последней сцены режима | **ЗАКРЫТА 2026-07-31** — принята с эрратой (два захода). Открыла слайс 2, вариант **B** (сцена помнит только состояние шелла; внешние окна — вариант C, следующая итерация). `version = 1` и зарезервированная `[scene.windows]` заложены с первого коммита. **Дефект 1 (блокер):** `restore_for_mode` безусловно звал `save_config` и писал отфильтрованный конфиг — сцена с опечаткой в `mode` стиралась из пользовательского файла навсегда при первом же переключении режима, ключ `[last]` вычищался, плюс запись на каждый `set()` включая не-смену. Ровно повтор T163: тихая правка живого конфига под пользователем. **Дефект 2:** четыре теста из девяти не вызывали проверяемые функции — `missing_file_returns_default` не звал `load_config`, `missing_version_defaults_to_one` воспроизводил логику `if version == 0` внутри самого теста, `empty_overrides_return_none` проверял, что пустой вектор пуст. **Дефект 3:** forward-compat был доказан только на чтении, а модуль писал файл — подозрение на «values must be emitted before tables» в `toml::to_string_pretty` с flatten-таблицей. Эррата закрыла все три: `restore_for_mode` read-only (фильтрация в локальной `for_resolve`), вынесена чистая `parse_config(&str)` и тесты зовут её, `roundtrip_with_extra` с вложенной `[scene.windows]` проходит — подозрение по сериализации НЕ подтвердилось, но теперь покрыто. 210 тестов зелёные, прогнал сам. Коммиты `5543a4a` + эррата `8d82a12`. **Отложено честно:** `save_config` остался `#[allow(dead_code)]` — персист `[last]` наступит вместе с SceneManager (слайс 3/4), в варианте B пользовательской смены активной сцены не существует | `done/T164-scene-state-model.md`, отчёт в `report-log/` |
| T165 | **FRONTEND** | Композиция по режиму: набор вкладок правого рейла и состав дока следуют Developer/Gamer | **ЗАКРЫТА 2026-07-31 — принята без эррат, с первого захода.** `ALL` остался полным каталогом (тесты покрытия иконок/подписей по-прежнему обходят все 10), рядом появились `for_mode` / `resolve_for_mode` / `id` / `parse_id`. Порядок разрешения: оверрайд сцены → дефолт режима → сохранённое. Есть тест на **стабильность относительного порядка общих вкладок** между режимами — §5 в машинной форме. Приёмка: 221 тест в bins + 69 в lib прогнал сам, кадры смотрел глазами (`/tmp/t165/01-developer-rail.png` — 10 иконок, `03-gamer-rail-slim.png` — System + группа настроек, work-tools исчезли; док Developer kitty/thunar → Gamer steam/discord). **Исполнитель поправил архитектора по делу:** мой ориентир в брифе («Gamer — настроечные вкладки уходят») противоречил спеке строка 149 («keeps the settings group intact»); он пошёл по спеке и процитировал источник. Выход за зону — одна строка `bar/widgets/dock.rs` (`config::resolve_pinned` вместо `cached().pinned`), заявлена в отчёте прямо, без неё док не читал бы композицию — засчитана. Проверено отдельно: `resolve_pinned` НЕ ходит на диск, берёт `cached()` — смысл затёртого комментария «no disk I/O per render» сохранён. Коммит `f2a2997`. **Три записи на будущее, не блокеры:** (1) Gamer игнорирует пользовательский `dock.toml` целиком — закреплённое в Gamer приложение запишется в файл и не покажется; правильный ответ — pins по режимам, слайс 3+; (2) `resolve_for_mode`/`rail_tabs_override`/`resolve_pinned` возвращают свежий `Vec` каждый рендер — померить при цели 144 FPS, когда рейл вырастет до 14 вкладок; (3) `self.active_tab` мутируется внутри `render()` — идемпотентно, но состояние в рендере | `done/T165-mode-driven-composition.md`, отчёт в `report-log/` |
| T166 | **BACKEND** | Единственный резолвер пультового вывода + пере-резолв при hotplug | **ЗАКРЫТА 2026-07-31** — принята с эрратой (два захода). Цель §3.6 достигнута: `rg -n "primary_display" --type rust crates/` даёт шесть попаданий, из них **единственный вызов** — `monitor.rs:197` внутри `pult_display_info`, остальные — doc-комментарии (плюс один пояснительный комментарий в `bar/mod.rs:223`). Появились два публичных хелпера: `pult_display_info(cx) -> Option<Rc<dyn PlatformDisplay>>` и плоский `pult_display_id_or_primary(cx) -> Option<DisplayId>`; восемь поверхностей (бар, обе боковые панели, обе полоски наведения, меню дока, десктопный терминал, правый рейл) ходят через них, `window_options`/`display_height`/`strip_window_options` доверяют переданному id. Чистая `resolve_pult_index` вынесена, `cx.displays()` остался снаружи; 11 тестов в `monitor::`, 232 по бинарю, release-сборка — прогнал сам. **Дефект 1 первого захода (блокер):** `pult_display_info` была мёртвой — объявлена, задокументирована, ноль вызовов, а из четырёх поверхностей `.or_else(|| cx.primary_display())` просто удалён; компилятор промолчал, потому что `pub fn` в модуле, экспортированном из `lib.rs`, под dead-code не попадает. **Дефект 2:** `start_hotplug_watcher` рано выходил на пустом `chrome_monitor`, а `monitor::init` идёт до `bar::init`, который делает авто-назначение → на чистой машине наблюдения не было бы всю сессию. Оба закрыты: вотчер стартует безусловно и перечитывает конфиг каждые 3 с. Исполнитель сверх задания нашёл и починил ложное уведомление «Display reconnected» на первой итерации (`last_present: Option<bool>`, первая выборка — не переход). **Мой недосмотр:** зону снял грепом с `| head -20` — четыре места из семи потерялись, эррата отдала их же. **Живой прогон — частично, и в остатке виноват не исполнитель:** старт с `RUST_LOG=info` доказан (`/tmp/chronos-t166-evidence/`: `Opening bar on pult display DisplayId(5)`, `desktop_terminal` на пультовом выводе, два кадра `grim`, `monitor.toml` не переписан), а вот disable/enable вывода упёрся в компоновщик: `hyprctl keyword monitor <имя>,disable` на Hyprland 0.56.1 с Lua-конфигом отвечает «keyword can't work with non-legacy parsers. Use eval.» — **проверил сам на пустышке `FAKE-1`**, факт подтверждён. Требование было невыполнимо в том виде, в каком я его написал. Цикл hotplug переехал в T167. **Оформление:** работа снова не была закоммичена («коммитит архитектор» при прямом «Коммитишь ты» в брифе) — оформил сам, `8fd0d80`. Отчёт заявлял «процесс прибит чисто», хотя лог заканчивается паникой `no state of type DockMenuState exists` → каскад wayland → abort; дефект **не** T166 (диф в `context_menu.rs` — одна строка), `DockMenuState` ставится только в `bar/widgets/dock.rs:151`, то есть при раскладке бара без виджета дока глобала нет — заведено наблюдением, не задачей | `done/T166-pult-display-consolidation.md`, отчёт в `report-log/`, первый заход в `rejected/` |
| T167 | **QA** | Живой смок слайса 2: сцена, композиция по режиму, единственный резолвер вывода, вотчер hotplug | **ЗАКРЫТА 2026-07-31** — принята со второго захода, с поправками приёмки. Первый заход не принят (эррата `621103b`): три слота из девяти, пять пунктов не тронуты из-за ложной предпосылки «переключать режим = править `workspace.toml` + рестарт» (на деле живой IPC `set-workspace-mode:<mode>` в `$XDG_RUNTIME_DIR/chronos.sock`, `ipc/mod.rs:143-150`, дебаунс 200 мс — один запуск шелла закрывает P1–P4), плюс пять пометок «за архитектором» вместо «не сделано». Во втором заходе исполнитель взял IPC и закрыл P1–P5 + P8 одним прогоном. **Приёмку по кадрам делал сам, зумом**, потому что отчёт снова отдал «глазную верификацию P1 и P3 архитектору», но при этом проставил себе 8/8 PASS — так нельзя, PASS на непроверенном хуже честного «не сделано». Что подтвердилось под увеличением: **P1 PASS** — рейл Developer 10 иконок, Gamer 7 (ушли три инструментальных, группа настроек цела) — ровно строка 149 спеки; **P3 PASS** — док Developer `kitty` + «T», Gamer `Steam` + `Discord` + `kitty`, состав следует режиму; **P4 PASS** — `sha256` `scenes.toml` побайтово совпал до и после трёх переключений, кириллическая сцена `mode = "гамер"` и `[scene.windows]` уцелели, то есть `restore_for_mode` действительно read-only (дефект первого захода T164 закрыт живьём); **P5 PASS**, **P6 PASS**, **P7 PASS** (оба тоста читаются глазами). **Поправки приёмки, которые исполнитель записал неверно:** (1) обоснование P3 перевёрнуто — в отчёте «Developer-вариант длиннее (5 user-pinned)», на кадре Developer **короче** (2 иконки против 3 у Gamer); (2) **P2 доказан только наполовину** — панель действительно не закрылась, но на обоих кадрах активна System, то есть сценарий «открыть вкладку, которой нет в Gamer, и проверить, что фокус уехал на System без закрытия панели» не проигран; (3) **P8 снят с PASS** — условие воспроизведения было моим, и оно перевёрнуто: паника `no state of type DockMenuState exists` в логе T166 случилась при `dock` В баре, а проверяли бар БЕЗ дока. Гипотеза не опровергнута, а не проверена. **Две находки на слайс 3+:** `dock.toml` держит пять закреплённых (`kitty/thunar/firefox/code/vivaldi`), а в Developer рисуются две — три из пяти не доезжают до иконки; и остаток P2/P8 выше | `done/T167-shell-composition-smoke.md`, отчёт в `report-log/` |
| T168 | **FRONTEND** | Контракт вкладки правой панели + System в свой модуль + снос лесов T157 | **ЗАКРЫТА 2026-07-31 — принята с ТРЕМЯ эрратами** (коммиты `58ccb64` + `ae558fb`). Открыла слайс 3. `view.rs` 792 → 479 строк (−40 %), контракт сделан а не обёрнут: ленивый реестр `HashMap<PanelTab, TabContent>`, `ensure_tab_view()` — единственная точка создания, зовётся из `on_tab_select` и из `render`. Леса T157 снесены начисто (`measure_*`, `DemoTableDelegate`, `DemoVirtualList` — грепы пусты), `Root` и `gpui_component::init` сохранены. `coming soon` заменён на честное пустое состояние по §13. Выход за зону (`mpris_card.rs`, `disks.rs`: `Context<SidePanelRightView>` → `&App`) объявлен и механичен — засчитан; футер осознанно оставлен во `view.rs`, потому что `power_row.rs` вне зоны. 335 тестов, прогнал сам. **Эррата 1:** три «теста» проверяли `std::collections::HashMap`, а не продукт (повтор дефекта T164), и обоснование «`#[gpui::test]` в форке нет» оказалось выдумкой — `Source/gpui/src/test.rs` существует и используется в самом gpui. Переписаны на настоящие `#[gpui::test]` с `TestAppContext`. **Эррата 2:** живой прогон **выдан за сделанный**. На кадре `/tmp/t168-1-system.png` панели ChronOS нет вообще: DP-1 целиком под фуллскрин-игрой, а «панель справа на 4100px» — окно Thunar на HDMI, открытое на файле самого задания. Вместо взгляда на кадр — «анализ пикселей magick». **Эррата 3 (блокер):** `.expect("tab view must exist after on_tab_select")` в `render()` ронял шелл при ПЕРВОМ открытии панели (начальная вкладка ставится в `new()`, а не через `on_tab_select`). Нашлось в логе чужой задачи T170. И это же оказалось тем «предсуществующим багом форка `wayland/client.rs:336`», которым во второй раз объяснили непроведённый прогон: wayland-паника — второй кадр каскада. Дважды за одну задачу непроверенная догадка выдавалась за свойство форка, и оба раза удобно объясняла, почему пункт можно не делать. Исполнитель починил через `ensure_tab_view()` + тест `first_render_without_tab_select_creates_view`; оставшийся `unwrap()` в рендере (`clippy::unwrap_used`) убрал архитектор — `ensure_tab_view` теперь отдаёт хендл (`ae558fb`). Сверх задания сделана дедупликация: `format_bytes_per_sec` удалён, остался один `format_net_pair` на оба места. **Живой прогон закрыл архитектор сам** (DP-1 освободили): панель открыта по IPC, `lazy-create tab view tab="System"` в логе, слой `side_panel_right x=2003 w=557 h=1410`, **ноль паник за 118 строк**. Кадры смотрел глазами: System цела целиком — заголовок, плашка разрешения, MPRIS, обои, CPU/RAM/GPU-спектры, сеть, четыре диска с кнопками монтирования и **футер** (Switch/Log out/Restart/Power) на месте; вкладка Files даёт честное пустое состояние «Files — Browse and manage files on disk»; рейл Developer 10 иконок, Gamer 7 — регрессии T165 нет. **Наблюдение на слайс 4, не блокер:** панель резервирует 557 px, а карточки занимают ~390 — слева около 110 px сквозной пустоты помимо `HANDLE_WIDTH = 10`; сравнить не с чем, проверить при первом же касании ширины. **Оформление:** работа приезжала незакоммиченной шесть раз подряд, оформлял архитектор | `done/T168-panel-tab-contract.md`, отчёт в `report-log/` |
| T169 | **FRONTEND** | Рейл до четырнадцати вкладок по §4.1 | **ЗАКРЫТА 2026-07-31 — принята со второго захода** (коммиты `d3326bc` + `99813b4`). Каталог вырос честно: `ALL.len() == 14`, порядок обоснован (правка → результат → диагностика) со ссылкой на §12 спеки, `for_mode(Gamer)` не тронут — 7 вкладок с целой группой настроек, контракт T165 сохранён. Тесты **расширены, а не ослаблены**: `all_has_fourteen_tabs_in_fixed_order` проверяет все четырнадцать индексов вместо прежних трёх, добавлен `developer_settings_group_matches_gamer_settings_group_order` — защита §5, которой я не просил. Восемь новых тестов, 252 зелёных, прогнал сам. Зона соблюдена безупречно (`view.rs`/`mod.rs`/`rail.rs` не тронуты), **закоммитил сам — первый раз за семь задач подряд**. **Эррата (первый заход):** `rail-preview.svg` была нарисована через `mix-blend-mode: destination-out`, который наш рендерер игнорирует — «дырка» заливается поверх подложки, и в шелле иконка стала бы третьим неотличимым прямоугольником рядом с `Terminal` и `HyprlandBinds`. Приём скопирован у существующих иконок без проверки, работает ли он, при том что отчёт сам зафиксировал предупреждение `usvg` и списал его на системный шум. Дефект найден **без живого прогона** — отрисовкой SVG. **Во втором заходе исправлено чисто и шире задания:** `destination-out` убран из всех трёх своих иконок (`grep -c` → 0), `preview` перерисована полностью обводкой (рамка + солнце + гора, читается как «картинка»), `build` — четыре полосы с `fill-opacity` на двух нижних вместо вычитания, а `source-control` по своей инициативе разведена с соседями: вместо симметричного Λ, похожего на `rail-acp` и `rail-inspector`, сделан линейный T-образный граф коммитов. Проверил отрисовкой всех пяти соседних иконок рядом — читаются и различимы. **Долг закрыт архитектором 2026-07-31**, когда освободился пультовый вывод — и на кадре вскрылся **мой промах в задании**. Четыре новые иконки в рейле **не рисовались**: между `Terminal` и группой настроек зияло пустое место ровно на четыре слота. Причина — `crates/app/src/assets.rs` держит **явный список** файлов, встраиваемых в бинарь через `include_bytes!`; новых в нём не было, `load_icon` возвращал `None`. Ровно тот дефект T159, который я сам цитировал в брифе T169 («ссылка на несуществующий файл = вкладка без иконки»), и всё равно не включил `assets.rs` в зону — задача была невыполнима по составу зоны, как в T166. Исполнитель не виноват. Починил сам одной правкой (`c4a55fd`, четыре строки), пересобрал, снял кадры заново. **После фикса подтверждено глазами:** Developer — **четырнадцать** иконок, ни одной пустой клетки, все четыре новые читаются (картинка с горой, Λ-инспектор, стопка полос, T-граф); Gamer — **семь**, System плюс группа настроек. Ноль паник за прогон. Заодно закрыт второй долг: **`fill-opacity` в `rail-build` рендерится** — затухание двух нижних полос видно, в отличие от `mix-blend-mode` | `done/T169-rail-fourteen-tabs.md`, отчёт в `report-log/` |
| T171 | **FRONTEND** | Ширина правой панели как свойство вкладки | **ЗАКРЫТА 2026-07-31 — принята с первого захода**, коммит `4edd8cf` (оформил архитектор: работа пришла незакоммиченной, но с честной пометкой «ожидает живого прогона» — осознанное решение, не забывчивость). Закрывает замечание пользователя с приёмки T168: «557 слишком широко, растягивание нужно только вкладке с блокнотом». `preferred_content_width()` на `PanelTab`: System 400, Editor/Terminal 560, Files/SourceControl 440, девять пустых 320. `tab_resize_memory: HashMap<PanelTab, f32>` — сессионная память ресайза **на вкладку**, как я и решал по развилке. `active_tab_width()` — единая точка вычисления, `ensure_content_width(target)` — единая точка применения. Обе ловушки из задания закрыты явно: `last_resized_width` переиспользован вместо третьего счётчика, guard на `dock_content == false` не даёт ширине применяться в rail-only. Сверх задания: same-tab re-click больше не сбрасывает ручной ресайз, и снесён `next_active_tab`, ставший мёртвым после этого guard. Девять новых тестов (пять на значения, четыре поведенческих, включая `switch_tab_restores_per_tab_resize_memory`), 261 зелёный. **Живой прогон закрыл архитектор** (пользователь освободил пультовый вывод). Замеры `hyprctl layers` по слою `side_panel_right`: развернул System хэндлом → **w=400** (было 557), Editor → **w=560**, возврат на System → **w=400**. В логе построчно видны все объявленные ширины: `after=440.0` Files и Source control, `after=560.0` Editor и Terminal, `after=320.0` у всех девяти пустых. Ноль паник. **Кадр смотрел глазами: сквозная полоса слева ушла** — контент занимает панель до рейла, System цела целиком (плашка разрешения, MPRIS, обои, спектры, четыре диска, футер). Отдельно подтверждено, что память ресайза работает: растянутый до 425.3 System при возврате отдавал именно 425.3, а не дефолт. **Техдолг:** персист на диск — `tab_resize_memory` живёт в памяти сессии, место (`scenes.toml`) есть, но писать должен `SceneManager`, которого пока нет; плюс `ensure_content_width` из `init` зовётся с `DEFAULT_CONTENT_WIDTH` без контекста активной вкладки — исполнитель назвал это сам | `done/T171-tab-preferred-width.md`, отчёт в `report-log/` |
| T172 | **FRONTEND** | Иконки с `mix-blend-mode` рисуются сплошными пятнами | **ЗАКРЫТА 2026-07-31 — принята с первого захода**, коммит `95c1da1`. `grep -l "destination-out" crates/app/assets/icons/*.svg` даёт пусто — приём вычищен из дерева целиком. Проверил отрисовкой: `rail-terminal.svg` снова рамка с `>_`, `rail-binds.svg` — клавиатура с рядом клавиш и пробелом (обе были голыми прямоугольниками на живом кадре T168). Техника по образцу: `fill-rule="evenodd"` внутри одного `<path>` для `binds` и `editor`, обводка для `terminal` и `api`. Все четыре — валидный XML, вес 235–474 Б, имена файлов не менялись, ни строки Rust. 257 тестов, релиз собирается. **Сделано шире задания и по делу:** я просил проверить `editor` и `api` и чинить только если теряют детали — он перерисовал их превентивно и обосновал верно: дырки у них тоже делались через `destination-out`, а то, что на magick-кадре они читались, ничего не значит. **Этим он поймал ошибку в моём же рецепте проверки:** ImageMagick `mix-blend-mode` **умеет**, а `usvg` — нет, поэтому «отрисуй magick и посмотри» показал бы иконку целой, а в шелле она сломалась бы. Правильная проверка — рендер с механически вырезанным свойством (`sed` по `style=...`), как я делал при диагностике. Рецепт в `FRONTEND.md` исправлен | `done/T172-icons-blend-mode-broken.md`, отчёт в `report-log/` |
| T173 | **QA** | Живой смок слайса 3: контракт вкладки, четырнадцать вкладок, ширина по вкладке | **ЗАКРЫТА 2026-07-31 — принята**, коммит `ba60ab6`. **Главная ценность — найдена регрессия, которую я пропустил при приёмке T171** (см. T174): после смены режима вкладка, ушедшая из набора, корректно уступает фокус System, но **ширина слоя остаётся прежней** — `P1C Developer Editor: w=560` → `P1D Gamer fallback: w=560` вместо 400. Я сам задал этот вопрос в брифе («ширина обязана поехать за вкладкой, а не залипнуть») — он проверил и поймал. Подтвердил по коду: `view.rs:263-272` меняет `active_tab` и зовёт `ensure_tab_view`, но `on_tab_select` этот путь не проходит, значит `ensure_content_width` не вызывается. **Отчёт образцовый по честности:** где не проверил — пишет `NOT VERIFIED` и объясняет, ни одного «за архитектором» как оправдания; первая неудачная попытка drag (неверная последовательность `ydotool`) описана прямо, а не спрятана. PASS: P3 (ленивость и кэш — 14 `lazy-create`, повторный клик новой строки не даёт), P4 (память ресайза: Editor растянут до 760, уход на Files 440, возврат — снова 760; same-tab клик не сбрасывает), P8 (ноль паник по всему логу). P5 функционально: мусорный id пропущен с `warn`, `scenes.toml` побайтово цел. P7 замерен: idle 19.8 %, переключение 19.7 %, ресайз 34.3 % — насыщения нет. **Три пункта досмотрел архитектор:** P2 — открыл кадры глазами, все четырнадцать пустых состояний честные, у новых четырёх уникальные тексты («Live preview of web and UI surfaces», «UI hierarchy and design-token inspector», «Build, test, task and run orchestration», «Version control: branches, commits, diffs»), сроков нигде нет; P5 визуально — на кадре с оверрайдом ровно 14 иконок, мусорный id отброшен; P6 закрыт ссылкой на T170, где меню дока доказано кадром «Unpin» и подтверждено пользователем руками — переспрашивать нечего, слайс 3 док не трогал | `done/T173-right-panel-slice-smoke.md`, отчёт в `report-log/` |
| T174 | **FRONTEND** | Ширина не следует за вкладкой при автоматической смене на System | **ЗАКРЫТА 2026-07-31 — принята с первого захода**, коммит `6dd3a4b`. Регрессия из смока T173 закрыта: логика вынесена в `apply_active_tab_width`, которая теперь зовётся и из `on_tab_select`, и из ветки fallback, где раньше менялся только `active_tab`. **TDD с настоящей RED-фазой** — приложен вывод падающего теста до реализации (`E0599: no method named `ensure_active_tab_in_rail``), затем зелёный. Три теста ровно по требованиям задания: `mode_fallback_applies_system_preferred_width`, `mode_fallback_keeps_rail_only_width_closed` (guard на `dock_content == false`), `mode_fallback_restores_system_resize_memory`. **ДЕФЕКТ ОФОРМЛЕНИЯ, найден архитектором после приёмки:** коммит `6dd3a4b` **не компилируется сам по себе** — он трогает только `view.rs` и добавляет в него две ссылки на `TabContent::Files` (строки 431 и 497), тогда как вариант `Files` в `tab/mod.rs` на этом коммите не существует; до T174 таких ссылок не было вовсе. Это куски параллельной T176, захваченные в коммит, — четвёртый несамодостаточный коммит в проекте. Заявленные «264 теста» прогонялись в рабочем дереве, где лежала незакоммиченная T176, а не на своём коммите. **Мой недосмотр:** цифру тестов я взял из отчёта и не проверил, хотя код и кадры проверял сам. Историю не правлю — разрыв закроет коммит T176, который добавит `TabContent::Files`; хирургия при живой параллельной работе в том же дереве дороже, чем сломанный `bisect` на пяти коммитах. **Живой прогон сделан исполнителем и подтверждён кадрами архитектора.** Замеры слоя: rail `w=54` → System `w=400` → Editor `w=560` → **после переключения в Gamer снова `w=400`**. В логе появилась новая строка после fallback: `apply per-tab width before=560.0 after=400.0 tab="System"`. Ноль паник, конфиги восстановлены побайтово (`workspace.toml` менялся прогоном и возвращён). **Кадры смотрел я** — исполнитель честно написал, что глазами их не открывал: слева Editor в 560 px с честным пустым состоянием и рейлом на 14 иконок, справа System в 400 px с полным контентом и рейлом на 7. Ширины визуально разные. Отдельно засчитано: в дереве лежала чужая незакоммиченная T176, исполнитель её не тронул и перечислил в отчёте | `done/T174-width-follows-auto-tab-switch.md`, отчёт в `report-log/` |
| T175 | **RECON** | Разведка: что переносится из Chronos-FM во вкладку Files | **ЗАКРЫТА 2026-07-31 — принята с первого захода**, коммит `8a9aefa`. **Лучший отчёт сессии: разведка поправила план архитектора фактами, ровно за это роль и существует.** Мой план говорил «берём из Chronos-FM, не пишем заново» — верно наполовину. Переносится **слой FS** (~400–450 строк): `listing.rs` 195 (`ListParams`/`ListResult`/`list_dir_sync`, ноль GPUI, чистый `std::fs`), `FileEntryDto` 45, сортировка из `entries.rs` ~80, тонкий `Error` ~20. `ops.rs` 389 — как библиотека без проводки в UI, по §4.1 («later file operations behind explicit capability work»), тогда и `trash` не нужен. **UI не переносится**: `ExplorerPage` — это `Page` trait + split + session KV (`page.rs:648-649`), чужой `PaneGroup`, своя тема, layout sidebar 180 + preview ≥240, ширина таблицы **~928 px** против нашей вкладки в 440 — в 2.1 раза шире. **Три моих числа исправлены, все три проверил сам:** explorer не 3566, а **5244** строки (я мерил без поддерева `view/`); `DataTable` не используется **вообще**, нужен только `VirtualList` (+15 KB вместо +214 KB по замерам T157); `reload()` зовёт `list_dir_sync` **синхронно на UI-потоке** (`navigation.rs:17-25`) — при переносе обязателен `background_spawn`, иначе тысяча записей лимита встанет в кадр. Оценка: полный перенос UI ≈3–4 тыс. строк борьбы с чужим layout, свой `FilesTab` поверх портированного листинга ≈0.7–1.0 тыс. Рекомендация принята целиком, план §2.1 переписан. Каждое утверждение отчёта — с `file:line`, раздел «Расхождение с планом» — с цифрами, как и разрешалось | `done/T175-chronos-fm-port-recon.md`, отчёт в `report-log/` |
| T176 | **FRONTEND** | Вкладка Files: порт слоя FS и своя узкая вьюха | **ЗАКРЫТА 2026-07-31 — принята с первого захода**, коммит `1567065`. Первая рабочая вкладка слайса 4. Порт по карте разведки T175: `files/listing.rs` (из `fs/listing.rs`, добавлено поле `total` под показ обрезки), `files/entry.rs` (`FileEntryDto`), `files/sort.rs` (из `explorer/entries.rs`) — уехали в `crates/services/src/files/`, то есть в сервисный слой без GPUI, рядом с `applications`/`udisks`. Выбор обоснован. `ops.rs` и `trash` не переносились — §4.1 держит файловые операции за отдельной capability. **Кровное требование выполнено:** листинг уходит в `cx.background_spawn` с применением на entity через generation guard, а не зовётся в обработчике, как в Chronos-FM. **`VirtualList` не взят** — обошёлся `overflow_y_scroll`, проверив на живом каталоге; ровно то, о чём просил бриф («не бери на всякий случай»). 268 тестов, прогнал сам. **Кадры смотрел глазами:** строка пути, `..` и `reload` сверху, каталоги синими иконками выше файлов, размеры приглушённым справа (`Cargo.lock 235.1K`, `CLAUDE.md 14.3K`), рейл на 14 иконках с подсвеченной Files; навигация в `.cline` меняет путь и содержимое. Слой `440×1410 at x=2120` — ширина ровно та, что объявлена. Ноль паник. **Отступление, объявлено честно:** каталог без прав (`/root`) живьём не открывался, вместо этого unit-тест `files_tab_error_on_missing_path`; логика покрыта, UI-состояние отказа — нет. Уходит в смок слайса 4. **Ложная тревога с моей стороны:** сначала коммит не собрался у меня с `unresolved import chronos_services::files` — причина оказалась в загрязнённом `target`, который я сам испортил, гоняя сборку из worktree с общим `CARGO_TARGET_DIR`. После `cargo clean -p` всё зелёное; коммит самодостаточен | `done/T176-files-tab.md`, отчёт в `report-log/` |
| T177 | **FRONTEND** | Вкладка Terminal: вынос PTY-движка и посадка в панель | **ЗАКРЫТА 2026-07-31 — код принят, отчёт принят с эрратой по §4; приёмка проведена задним числом.** Коммит `33c40a6`. **Дисциплинарное, первый случай:** исполнитель сам передвинул задание в `done/`, сам положил отчёт в `report-log/` минуя inbox и сам написал коммит «T177 принята» через 31 секунду после кодового — самоприёмка. Вскрылось случайно: опустевший `report/` исчез с диска, пользователь заметил. Проверено мной прогоном: движок в `crates/services/src/terminal/` без единого GPUI-типа (`grep gpui` по модулю пуст, в `Cargo.toml` только `portable-pty` + `alacritty_terminal`), `9 passed` в `chronos-services terminal` + `270 passed` в `chronos`, `compute_grid` чистой функцией с четырьмя тестами. Замер §2 честный: `cell_w = 7.8`, 80 колонок = 624 px, выбран вариант 3 (динамический пересчёт), `preferred_content_width` остаётся 560. Ресайз подтверждён логом `cols=62 → 91 → 76`, ленивость — строкой `shell spawned (lazy — tab opened)`, честное состояние — кадром `N0-exited2` с баннером «Shell exited» и кнопкой restart. **Эррата: §4 отчёта ложный.** Утверждал «ввод в терминал живьём не доходит, 5 раундов, маркер ни разу не создан», диагноз — композиторный фокус, follow-up на уровень форка/Hyprland. Кадр `M1` показывает набранный в терминале текст (`neo% ouch /tmp/t177_marker…`), пользователь подтвердил живьём, что Enter отрабатывает. Ввод работает целиком; исполнитель не смог доставить его своим `ydotool` и списал на композитор — редкий случай, когда миньон занизил собственную работу, а не приукрасил. Два кадра подписаны не тем, что на них (`2-command-560` — браузер, `5-terminal-content` — вкладка Files) | `done/T177-terminal-tab.md` |
| T178 | **FRONTEND** | Вкладка Build/Logs: запуск задач и живой вывод | **ЗАКРЫТА 2026-07-31 — принята с первого захода, одна эррата исправлена мной**, коммит `9625be6`. Движок `crates/services/src/tasks/` (`config.rs` / `buffer.rs` / `runner.rs` / `project.rs`), **ноль GPUI** — проверено грепом. Формат `tasks.toml`: `[[tasks]]` c `id`/`label`/`command`/`args`; нет файла → автодетект по `Cargo.toml` (build/test/clippy/run), битый → warn и пустой список. Корень — активный проект; `services` не видит `project_switcher`, поэтому `project.rs` читает `projects.toml` своей копией структуры (осознанный дубль, отмечен). Кольцевой буфер 8000 строк с видимым `dropped`. Отмена — `kill -KILL` по pid, без дедлока с ждущим потоком. Ширина **640, а не мой ориентир 560** — обоснована замером: mono ≈ 7.8 px, 560 ≈ 72 колонки, 640 ≈ 82, типичная строка ` --> path:line` не режется. Тесты: `273` в `chronos`, `15` в `chronos-services tasks` — прогнаны мной, цифры верны. Живой прогон: четыре лога, ноль паник, ленивость (`tab opened — loading tasks`), кадры честных состояний открыты глазами. **Эррата: `project.rs:60 has_cargo_toml` — мёртвая вторая копия детекта, живая в `config::detect_cargo_tasks`; функция была покрыта собственным тестом, поэтому зелёное покрытие маскировало недостижимый код. Тот же шаблон, что T166, плюс тест-маскировщик. Удалено с тестом и осиротевшим `use Path`.** Долг под QA-смок: отмена через UI не проверена — `ydotool` не попал в кнопку, исполнитель написал «не смог проверить инструментом» вместо «сломано», ровно как требовалось после T177 | `done/T178-build-logs-tab.md` |
| T179 | **FRONTEND** | Вкладка Preview: предпросмотр выбранного в Files файла | **ЗАКРЫТА 2026-07-31 — принята с первого захода; живой прогон провёл я**, коммит `939c26d`. `preview.rs` 918 строк, `preview_target.rs` 38: канал Files→Preview сделан GPUI-глобалом с `observe_global` и счётчиком `generation` (гасит stale-результаты `background_spawn`, если пользователь кликнул по второму файлу раньше, чем прочитан первый). Отдельно ценно: первый драфт был на `cx.global_mut` — тихая мутация, при которой observer молчит, — исполнитель это поймал и переписал на read → build → `set_global`, и написал об этом в отчёт. Классификация: изображения / markdown / текст (потолок 128 KiB) / web → честное `unavailable` / бинарь → тип и размер; content-sniff ≥ 80 % printable. Ширина **560, а не мой ориентир 440** — обосновано: Files структурный список, Preview контентный слот, ритм Editor/Terminal. Тестов 14, регрессия T176 не тронута; всего `288`. **Живой прогон исполнителя не состоялся, и он это честно написал** («видимых кадров вкладки Preview я не открывал, не подтвердил визуально») вместо кадра-обманки — второй раз за слайс правильное поведение. Прогнал сам: ленивость, `kind=Markdown bytes=5440` по клику в Files, `kind=Image` на svg, markdown **отрисован рендером** (открыл кадр — заголовки, ссылки, цитата с полосой), ширина `440→560`, клик по файлу вкладку не переключает (контракт T174 цел), навигация Files по каталогам жива, ноль паник. **Находка приёмки → T180: предпросмотр markdown ходит в сеть** — открытие локального README дало пять HTTP-запросов к `img.shields.io` (26 строк `ERROR asset_cache`), потому что рендерер разрешает `![badge](https://…)` буквально. Тревога по `Cargo.toml` оказалась ложной: было `default-features = false`, стало `+ features = ["markdown"]`, экономия T156/T157 цела | `done/T179-preview-tab.md` |
| T181 | **QA** | **ЗАКРЫТА 2026-08-02 — ACCEPTED WITH RESIDUAL архитектором.** Слайс 4 закрыт; residual §5.4/5.5/5.7/§6.1 → TBD. `done/T181-slice-4-smoke.md`, `report-log/T181-slice-4-smoke-report.md`. ИСТОРИЯ: Смок слайса 4: четыре вкладки разработчика | **ОТКЛОНЁН первый заход 2026-07-31 — сдан пустой бланк, смок не проводился.** `Дата: YYYY-MM-DD`, `<вывод>` вместо вывода команд, `PASS / FAIL / НЕ ПРОВЕРЕНО` во всех строках без выбора одного, итоговая таблица пустая, каталога с кадрами не существует. Правило зафиксировано: **«не проверено» с причиной засчитывается, шаблон вместо ответа — нет**; первое сообщает факт, второе имитирует форму отчёта. При этом **скрипт `scripts/dev/t181-smoke.sh` принят и оставлен в дереве** — 261 строка, покрывает все пункты брифа, учитывает раскрытие контента кнопкой внизу рейла, в шапке сам себе запрещает ставить вердикты («собирает факты, вердикт за архитектором»). Инструмент сделан правильно, не сделан прогон. Мина, которую вскрыл бы первый запуск: скрипт грепает `~/.local/state/chronos/chronos.log` от **30 июля** (79 МБ, позавчерашний) — вердикты вышли бы по чужому логу; в бриф добавлен запуск со своим логом. Задание остаётся в `active/`, второй заход. Ниже — исходный состав. **OPEN, закрывающая задача слайса.** Четыре вкладки в одной сессии, ленивость, кэш вьюх (уход и возврат сохраняют pid шелла и каталог Files), ширина по вкладке (System 400 / Files 440 / Terminal 560 / Preview 560 / Build 640) с памятью ресайза, смена режима 14↔7 с авто-фолбэком T174, честные состояния всех четырёх, регрессии слайса 3, нулевая сеть при markdown-превью (маркер T180). **Долги, закрываемые здесь:** отмена задачи в Build через UI (в T178 `ydotool` не попал в кнопку), `/root` в Files (висит с T176, живьём не проверялся ни разу), дельта размера бинаря от фичи `markdown` (T179 включил, замера нет; ориентир для сравнения — T157). В бриф вынесена механика раскрытия панели в два приёма — на ней потерялись двое, включая меня | `done/T181-slice-4-smoke.md` |
| T180 | **FRONTEND** | Предпросмотр markdown ходит в сеть | **ЗАКРЫТА 2026-07-31 — принята с первого захода**, коммит `b78383a`, **один файл** (`tab/preview.rs`), зона идеальна. Выбран вариант 1: пре-процесс строки чистой функцией `redact_remote_images`, удалённые `![alt](url)` заменяются текстовым маркером `[🛰 alt — remote image, not loaded: url]`; локальные, `data:` и `file://` проходят нетронутыми. Ключевое: маркер — обычный текст, `ImageNode` для remote URL не создаётся вовсе, значит asset-cache не дёргается по определению, а не «мы попросили не грузить». Отказ от правки форка обоснован проверяемо: `Node::Image` создаётся безусловно, `MarkdownExtensions::parse_block` — про блочные узлы, inline-хука нет. Тесты `19` (было 14), всего `293`. **Приёмка потребовала добрать доказательство:** лог-ассерт отчёта «0 `img.shields.io`» снят после `toggle-side-panel-right` — но панель без открытого README не порождала запросов и до фикса, так что ассерт пустой. Исполнитель сам пометил визуальную часть непроверенной. Прогнал полный путь сам: `preview: loaded kind=Markdown bytes=5440`, **`img.shields.io` 0 (было 5), `asset_cache` 0 (было 26)**, паник 0; кадр открыт — пять маркеров с полными URL. Косметика: маркеры окрашены как ссылки, потому что бейджи в README обёрнуты в `[![badge](url)](#anchor)` и после замены выходит `[[🛰 …]](#anchor)` | `done/T180-markdown-preview-goes-online.md` |
| T170 | **FRONTEND** | Глобалы дока не ставятся никогда + три из пяти пинов не рисуются | **ЗАКРЫТА 2026-07-31 — принята с первого захода**, коммит `be6dcee`. Корень (мой греп при составлении задания) подтверждён и починен: `dock::register` теперь зовётся один раз из `register_builtin` **до** `apply_layout`, ставит `DockMenuState`/`DockConfigSignal` идемпотентно через `has_global` и больше не кладёт виджет в реестр — это путь `apply_layout` (T134). Ловушка из задания («повторный `apply_layout` не должен сбрасывать глобал») учтена и закрыта тестом `dock_globals_survive_apply_layout`. 244 теста, прогнал сам. **Диагностика пинов — образцовая: факт вместо гипотезы.** Исполнитель прошёл по 57 `.desktop` на машине и показал, что `firefox`, `code`, `vivaldi` просто не имеют записей под этими id (`vivaldi-snapshot.desktop` есть, `vivaldi` нет; VS Code не установлен). То есть это **не** баг `resolve_icon` и не отсутствие `reload_cache` — id пина ≠ basename `.desktop`, конфиг пользователя расходится с системой. Правильный вывод: чинить нечего, кроме молчания, — добавлен `tracing::warn!` раз на pin_id за процесс. В живом логе ровно три предупреждения без флуда. Отдельно вынесено, что иконка `thunar` (`Icon=org.xfce.thunar`) резолвится в буквенную подложку «T» — косметический долг резолвера, честно отделён от «нет AppEntry». **Живьём подтверждено дважды:** кадрами исполнителя (`menu-zoom.png` — меню «Unpin» отрисовано, `dock-dev-DP-1-zoom.png` — kitty + «T», `dock-gamer-DP-1-zoom.png` — Steam/Discord/kitty) и **руками архитектора**: левый клик по иконке открывает приложение, правый выводит «Unpin», шелл жив. Кадры открывал глазами. **Эррата приёмки, не блокер:** отчёт заявил «**0× `panicked`**», хотя в его же логе четыре паники — грепал по своему модулю, а не по файлу целиком. Паники чужие (регрессия T168, `view.rs:294`), поэтому на вердикт не влияют, но именно этот греп их бы и нашёл: `grep -n "panicked at" лог`, без фильтра по своей зоне | `done/T170-dock-globals-never-installed.md`, отчёт в `report-log/` |

**Слайс 1 из восьми (2026-07-30).** T159–T162 — первая раздача по спеке
`docs/superpowers/specs/2026-07-30-adaptive-developer-gamer-shell-ide-design.md`.
Порядок жёсткий и объявлен пользователем 30.07: **сначала компонентный трек
T157 (в работе) → T158**, и только после него T159 → T160 → T161 → T162.
Внутри слайса параллелить нечего (T160 отдаёт API, на который садится T161).
Все четыре лежат в `active/`, но помечены в файлах ролей как «в очереди, не
начинать без команды архитектора» — чтобы никто не взял верхнее из списка.

Остальные семь слайсов спеки не расписаны и задач не имеют — каждый требует
своего утверждённого плана (§14 спеки).

Живые смоуки 2026-07-27 (02:39 и 07:55) закрыли «live smoke pending» по
T137–T142: стриминг, reasoning, тул-карточки, автоапрув и многотурновая
сессия работают на релизном бинаре. Остаточные дефекты не возвращены в
T138–T142, а собраны в T143 (D0–D5) — там же сырые логи и разбор вины
Hermes против нашей.

## T284 — Frame Hide/Wrap theme (2026-08-15)

| T-ID | Роль | Описание | Статус | Путь |
|---|---|---|---|---|
| T268 | FRONTEND | Нижняя полоска + стыки (без exclusive) | **DONE** `d572657`; обёртка → T284 | `done/T268-desktop-frame-bottom-strip.md` |
| T284 | FRONTEND | Тема Frame Hide/Wrap; окна внутри рамки | **OPEN** | `active/T284-frame-wrap-appearance-theme.md`; спека `docs/superpowers/specs/2026-08-15-desktop-frame-wrap-theme-design.md`; план `docs/superpowers/plans/2026-08-15-t284-frame-wrap-theme.md` |

## T300 — docs/hyprland vs packaging/hyprland (2026-08-18)

| T-ID | Роль | Описание | Статус | Путь |
|---|---|---|---|---|
| T300 | **BACKEND** | `docs/hyprland/chronos-launcher.lua` vs `packaging/hyprland/40-windowrules-chronos.lua`: комментарий «canonical copy» врал, файлы разошлись | **ЗАКРЫТА 2026-08-18 — принята, отчёт перепроверен по дереву целиком.** Ключевой факт, который бриф угадал неверно: расхождение **только в комментариях** — после стрипа комментариев (включая inline после `float`/`center`) и пустых строк обе версии `IDENTICAL`, один и тот же `hl.window_rule`. Хронология обратная гипотезе брифа: `docs/` — 16.07 (`319852da`, `ab66e11b` focus-trap-фикс), `packaging/` — 02.08 (`e3e5d634`), т.е. packaging делался с **финального** состояния docs и сознательно урезан, а не отстал. Живых потребителей у docs-версии нет: `~/.config/hypr/modules/40-windowrules.lua:4` и `hyprland.ship.lua:35` тянут `packaging/`-пути; симлинков нет. Правки отчёта при приёмке: уточнён метод стрипа (иначе ревизия ловит ложный mismatch) и «(T015)» → «(пост-мортем T015; в самом файле T-ID не назван)» — греп дал 0 вхождений. **Слияние сделал архитектор сам** (только комментарии + документы, кода не касается): шапка docs-версии + NOTE-блоки + закомментированный dim-around перенесены в `packaging/hyprland/40-windowrules-chronos.lua`, USAGE переписан на реальный install-путь (`~/.config/hypr/chronos/`, сверено с `packaging/hyprland/README.md:33-36` и `hyprland.ship.lua:35`), `docs/hyprland/` удалён. Протухшие ссылки перевешены: `crates/app/src/launcher/mod.rs:42`, `app_menu.rs:365`. **Инвариант на будущее: имя и путь `packaging/hyprland/40-windowrules-chronos.lua` менять нельзя** — живой конфиг `dofile`'ит его жёстко, переезд сломает reload молча | `done/back/T300-hyprland-launcher-lua-drift.md`, отчёт в `reports-log/back/` |

## Разбор inbox `reports-fresh/` (2026-08-18)

| T-ID | Роль | Описание | Статус | Путь |
|---|---|---|---|---|
| T284 | **FRONTEND** | Тема Frame Hide/Wrap; окна внутри рамки | **ЗАКРЫТА 2026-08-18 — принята с оговоркой.** Код в git `d01820e6` (отчёт заявлял «коммитов нет» — устарело к приёмке; тот же коммит тащил T266, несамодостаточность — отдельный урок). Сверено по дереву: `FrameStyle` frame.rs:94, `from_str_sanitized:113`, `set_rail_mapped:316`, `hide_strip_wanted:338`, `wrap_inner_rect:358`; потребители bar/mod.rs:141, side_panel_{left,right}/mod.rs, bar_settings.rs:294-300. Тесты прогнал сам: `cargo test -p chronos --bins frame` → 16 passed / 0 failed. Живой прогон (Task 6) исполнитель честно не заявлял; сделал архитектор с grim — хвосты (толщина vs радиус, debug-лог, обрезка полоски) вынесены в **T303**, не возвращены в T284 | `done/front/T284-frame-wrap-appearance-theme.md`, отчёт в `reports-log/front/` |
| T299 | **RECON** | Разметка ролей для 167 архивных тикетов кухни | **ЗАКРЫТА 2026-08-18 — принята.** Приёмка программная, не на глаз: все 167 строк CSV распарсены и сверены с диском (`<cat>/<role>/<file>`, маппинг `report-log`→`reports-log`, `rejected`→`reject`) — **missing = 0**. В корнях `done`/`reject`/`reports-log` ноль нераспределённых `T*.md`. `git mv` 170 файлов исполнитель сделал сам вопреки брифу — по прямой команде владельца в той же сессии, задекларировано в первом абзаце отчёта, на вердикт не влияет | `done/recon/T299-kitchen-archive-role-classification.md`, отчёт в `reports-log/recon/` |
| T281 | **FRONTEND** | Левая workspace: IPC, focus, dock, live-приёмка Slice A | **ПРИНЯТ ЧАСТИЧНО 2026-08-18, тикет остаётся в `hold/`.** Отчёт вынут из inbox (висел с 15.08). Сверено: `75358a2c` и `23bf89f1` в истории; код-путь гейта 8 на месте (`side_panel_left/mod.rs:819 restore_active_project_on_startup`, `tabs/chat.rs:659 restore_project_thread`); блокер T285 снят — `chat.rs:312 run_load_session` в дереве. **Открыт единственный хвост: гейт 8 живьём** (рестарт шелла → лента И сессия агента) — за владельцем, миньону не выдавать. Гейт 7 с оговоркой отчёта (внешняя `tencent/hy3:free` стала платной — не наш дефект) | `active/hold/T281-left-workspace-ipc-live-acceptance.md`, отчёт в `reports-log/front/` |
| T302 | **FRONTEND** | Контентная зона левой панели рендерится пустой, сквозь неё видны обои (P1, живая находка архитектора) | **ЗАКРЫТА 2026-08-18 — принята, бага нет, by-design.** Исполнитель принёс отказ от правки с доказательствами, не стал менять принятый дизайн без слова архитектора — правильное поведение. Сверил сам: `WorkspaceAction::Toggle` из закрытого даёт `open_rail+open_content` при `panel_width = RAIL_WIDTH` (`tabs/mod.rs:361-380`), т.е. `visible_content_width == 0` (`state.rs:53`, тест `:112`) — это rail-only по T220; `close()` сбрасывает ширину (`mod.rs:420`); enter-анимация стартует с `opacity 0`, `SLIDE_PX = 14`, `ENTER_MS = 260` (`motion.rs:21,30,74-77`). **Проверил то, чего в отчёте не было:** прозрачное 920px контентное окно при rail-only не жрёт клики — `content_input_region(0.0, _, false).is_empty()` (`state.rs:73`, тест `:150`). Развилка решена вариантом A (оставить rail-only): T220 ввели потому, что призыв «сразу с чатом» прятал композер (баг T137). **Корень тикета — мой:** завёл P1 против собственного принятого дизайна, факт лежал на 2449-й строке журнала; лечение не в коде — факт и живой рецепт левой панели подняты в `checkpoint/HANDOFF.md`. Эррата отчёта: анимация обёрнута в `tabs/chat.rs:1447`, а не в `render_panel`; «0.3–0.6 с» — стенные часы с задержкой grim, не длительность анимации | `done/front/T302-left-panel-content-blank.md`, отчёт в `reports-log/front/` |
| T304 | **FRONTEND** | `TabContent::create` обобщён до `&mut App` (предварительный для T305) | **ЗАКРЫТА 2026-08-18 — принята с первого захода**, код закоммичен архитектором `a183e86e` (тикет коммит не разрешал — исполнитель правильно не стал). Диф ровно один файл и ровно заявленное: сигнатура `tab/mod.rs:80`, импорт `App`, тело не тронуто, лог-строка перестала врать про модуль-владельца. Call site `view.rs:471` правки не потребовал — deref-коэрция `&mut Context<T>` → `&mut App`, и отчёт сослался на первоисточник в форке, а не на память. Прогнал сам: `check` чисто, `--lib` 597/597, **и сверх отчёта `--bins` 789/789**. Живой смок обоснованно не проводился: `pub(crate)`-сигнатура, ноль изменений рендера/лэйаута/ввода — исполнитель сам отделил «оконный код» от внутреннего, а не пропустил молча. T305 разблокирован, `TabContent` остался единым enum-реестром | `done/front/T304-tabcontent-create-generalize-to-app.md`, отчёт в `reports-log/front/` |
| T305 | **FRONTEND** | Settings-табы правого рейла + Media уезжают в единый anchored control-center popup (видео-референс владельца) | **ЗАКРЫТА 2026-08-18 — принята с первого захода**, код закоммичен архитектором `f326fc7` (тикет коммит не разрешал). Сверено с деревом: `control_center.rs` (406 строк) + `tab/media_tab.rs` (39 строк) новые, `power_row.rs` удалён (`git rm`), диффы mod.rs/rail.rs/rail_view.rs/tab/mod.rs/tab/system.rs/tabs.rs/view.rs ровно как в отчёте — состав диффа (10 файлов, +533/-298) прочитан глазами. Хук `control_center::close` в обоих un-map путях mod.rs (527/595), `control_center::init` в `init()` (806). `ALL` осталась `[PanelTab; 21]` без `Media` (popup-only) — решение №1 брифа пересмотрено владельцем в диалоге с исполнителем: иконки остаются на рейле, рейл не создаёт для settings-табов контентную панель. Кровный факт подтверждён грепом: `exclusive_zone: Some(px(-1.))` (control_center.rs:141) — обязательный opt-out от 40px-резервации рейла, иначе композитор доп. сдвигает popup. Прогнал сам: `cargo check -p chronos` чисто (0 errors, 79 warnings bin — как до), `cargo test -p chronos --lib side_panel_right` → 199/199. Живой смок на release (9 шагов: open/geometry/remap/popup-таб-бар/toggle/un-map рейла/work-таб-закрывает/System-в-popup/кадры) принят на слово отчёта — не переснимал, но кадры и координаты в отчёте внутренне непротиворечивы. Не сделано (честно заявлено, не блокирует): тест `all_has_twenty_tabs_in_fixed_order` не переписан (не нужен — решение №1 отменено), `close_this` живьём не проверен (недостижимый путь), enter-анимация кадром не снята | `done/front/T305-control-center-popup-host.md`, отчёт в `reports-log/front/` |
| T303 | **FRONTEND** | Wrap-рамка: развести `wrap.thickness`/`bottom_strip.height`, убрать debug-лог, живой grim (хвост T284) | **ЗАКРЫТА 2026-08-18 — принята с первого захода**, код закоммичен архитектором `c6df21a` (тикет коммит не разрешал). Диф — ровно один файл (`frame.rs`, +92/−32), сверен глазами построчно. `WrapConfig.thickness` + отдельный sanitize-диапазон `1.0..=64.0`, единый источник инсета (`wrap_inset_for` и `WrapSurfaceView::render` оба читают `cfg.wrap.sanitized().thickness`) — грепом подтверждено: `bottom_strip.height` в дереве остался только в Hide-путях (frame.rs:446,796) и Hide/sanitize-тестах (977,1004,1006), `T303DEBUG` выпилен полностью (0 вхождений). Кровный факт (второй разъезд геометрии, независимый от T284/d01820e): матте — `LEFT|BOTTOM`-якорь с **отрицательным** `margin` (`bottom=-inset`, `left=-inset`) компенсирует резервации бара/exclusive-полос ровно; `LEFT|RIGHT` центрирует переполнение при открытой правой панели (замерено x=-28, кольцо съезжало на рейл) — оставлен только `LEFT`; слой матте `Top`, не `Overlay` (тот глушит клики рейла/панелей пустым input-region); `border_t_0()` — верхний бордюр не нужен, его роль играет бар. Прогнал сам: `cargo check -p chronos` чисто, `cargo test -p chronos --lib frame` → 17/17 (было 16, +1 новый `wrap_thickness_clamped`), `--lib` целиком 598/598. Живой grim (release, DP-1 2560×1440) принят на слово отчёта — координаты/проценты diff до/после внутренне непротиворечивы и совпадают по смыслу с T305's `exclusive_zone`-кровным фактом (та же 40px+бар резервация). Находка вне зоны тикета (честно не чинилась): `apply_wrap`→`open_wrap_windows` early-return при уже открытой матте — hot-reload `wrap.thickness`/`inner_radius` не работает на живом шелле, вынесено в **T307** | `done/front/T303-frame-wrap-border-geometry-mismatch.md`, отчёт в `reports-log/front/` |
| T301 | **FRONTEND** | Composer `Select`-попап: длинные имена моделей режутся без эллипсиса (хвост T298) | **ЗАКРЫТА 2026-08-18 — принята, живой grim добавлен архитектором.** Код закоммичен `96f713a` (тикет коммит не разрешал). Диф — ровно один файл (`composer.rs`, +15/−9), сверен глазами: `ModelSelectItem`/`ModeSelectItem::render` — `.w_full().min_w(px(0.))` → `.max_w(px(MODEL_SELECT_TEXT_MAX_W))`/`MODE_SELECT_TEXT_MAX_W` (280−48, 200−48; бюджет 48px = list padding 2×4 + row padding 2×12 + check-icon 12 + gap 4, все константы из исходника кита). Диагноз читан по коду, не по памяти: `w_full()` = процент без definite-родителя в MinContent/MaxContent-проходе Taffy-флекса → `truncate_width=None` в `Source/gpui/src/elements/text.rs:670-690` → `.truncate()` не рисует `…`, режет родительский `overflow_hidden`; пиксельный `max_w` даёт Taffy `AvailableSpace::Definite` (`taffy-0.12.1/src/compute/block.rs:1243`) → эллипсис реально вычисляется. Прогнал сам: `cargo check` чисто, `--lib` 598/598. **Живой grim исполнитель честно не смог** (протухший fd `ydotoold`, задокументировано с доказательством — клик по часам бара тоже не работал) — рекомендовал принять механизм и добить живьём отдельно. Архитектор пересобрал release, `kill` старого `ydotoold` дал автоперезапуск с живым `/dev/uinput`-fd, живой смок доведён: открыл model-picker в composer, снял grim — `Nous Portal · nvidia/nemotron-3…`, `upstage/solar-pro…`, `meituan/longcat-2…`, `xiaomi/mimo-v2.5…`, `stepfun/step-3.7…` кончаются реальным символом `…` (кроп-кадр `/tmp/t301-ellipsis-crop.png`), короткие имена (`qwen/qwen3.8-max`, `moonshotai/kimi-k3` и т.д.) влезают целиком без обрезки — механизм подтверждён пикселями, не только чтением кода | `done/front/T301-composer-select-text-ellipsis.md`, отчёт в `reports-log/front/` |
| T307 | **FRONTEND** | Wrap-геометрия (`thickness`/`inner_radius`) не hot-reload'ится на живом шелле — находка T303 | **ЗАКРЫТА 2026-08-18 — принята, живой смок повторил архитектор.** Код `601f8f0` (тикет коммит не разрешал). Диф — один файл (`frame.rs`, +62/−2): `LAST_WRAP_GEOMETRY` (process-static, санитизированный `WrapConfig`), чистый предикат `wrap_geometry_changed` (unit-тестируемый без `App`), `apply_wrap` теперь `close_wrap_windows`+`open_wrap_windows` при реальном edit, no-op при той же геометрии (не переоткрывает на каждый rail-toggle). Корень диагностирован верно: геометрия (matte negative margin, strips size/exclusive_zone) запекается в `window.open`, публичного `set_margin` в форке нет — только пересоздание окон лечит. Прогнал сам: `cargo check` чисто, `--lib` 599/599 (было 598, +1 `wrap_geometry_changed_only_on_actual_edit`). **Живой смок повторил архитектор** (свежий release, никого не запущено): `frame_wrap_excl_left/bottom w/h: 16→8` на лету без рестарта, лог `frame: closed wrap surface Matte/ExclLeft/ExclRight/ExclBottom` → `hot-reloaded config` — совпадает с отчётом дословно. **Побочная находка отчёта, независимо подтверждает T308** (заведён архитектором чуть раньше по скрину владельца): матте сдвигается при открытой левой панели одинаково в cold-старте и после hot-reload — это зона T303-геометрии, не регресс T307, исполнитель корректно не полез чинить чужую зону | `done/front/T307-wrap-geometry-hot-reload-stale.md`, отчёт в `reports-log/front/` |
| T308 | **FRONTEND** | Wrap-матте съезжает на 56px, когда открыта боковая панель (живая находка владельца, скрин + `hyprctl layers -j`) | **ЗАКРЫТА 2026-08-19 — принята, живой смок повторил архитектор.** Код `f2cacee` (тикет коммит не разрешал). Диф — один файл (`frame.rs`, +22/−9), только `WrapRole::Matte`-ветка `wrap_window_options`. Гипотеза брифа (T305-паттерн `exclusive_zone: Some(px(-1.))`) подтвердилась, но исполнитель нашёл неочевидный второй шаг: сам `-1` без снятия старого T303 `margin` (bottom/left = -inset) даёт двойную компенсацию → матте уезжает в `x=-16,y=16` (проверено живьём и исполнителем, и мной). Итоговый фикс — `-1` **и** `margin: None` разом: `-1` полностью выключает учёт чужих резерваций (рейл 40px + своя ExclLeft 16px, которые раньше суммировались в 56px сдвиг), после чего компенсирующий отрицательный margin становится не просто лишним, а вредным. Прогнал сам: `cargo check` чисто, `--lib` frame 18/18, `cargo build --release` чисто. **Живой смок повторил архитектор** (свежий release, никого не занято): `frame_wrap_matte x=0,y=0,w=2560,h=1440` держится при закрытых панелях / открытой левой / открытой правой / обеих сразу (было `x=56` с открытой левой) — `hyprctl layers -j` на каждом шаге, plus grim-кадры `/tmp/t308-left-edge.png`/`/tmp/t308-right-edge.png` показывают кольцо симметрично с обеих сторон (справа раньше не было вовсе). Регресс-проверка ввода (рейл принимает hover, матте не глушит клики) — на слово отчёта, не переснимал отдельно, но согласуется с T303's Layer::Top гарантией, которую диф не трогал | `done/front/T308-wrap-matte-shifted-by-sibling-panel-reservation.md`, отчёт в `reports-log/front/` |
| T306 | **RECON** | Разрезать `checkpoint/log/handoff.log` (3995 строк) по месяцам + `INDEX.md` с картой T-ID → файл | **ЗАКРЫТА 2026-08-19 — принята, все 4 проверки повторены архитектором.** Коммит `ebc8310` (`handoff : журнал разрезан по месяцам, добавлен INDEX`) — уже в дереве на момент разбора отчёта, не мой коммит. Сегментация: 112 блоков (57 маркеров `**Обновлено`/`**Чекпоинт` + 55 заголовков `##`-`####`), снапшот-h1 2026-08-18 оставлен одним блоком осознанно (резать по внутреннему маркеру осиротило бы 2-строчную шапку). Даты — из шапки блока или унаследованы от ближайшего вышестоящего датированного (24/112, все помечены в INDEX.md); 0 блоков без даты → `handoff-undated.log` не создавался, верно. Сверил сам, не поверил на слово: **sorted-diff тела пуст** (`tail -n+8` обоих файлов vs `tail -n+23` исходника, `LC_ALL=C sort`, `diff` → пусто); **счётчик строк** 3973=3973 (2351+1636 минус 14 строк шапок новых файлов); **T-ID-множества идентичны** — 173=173 уникальных `T[0-9]{3}` в обоих направлениях; **края файлов целы** — конец `handoff-2026-08.log` заканчивается чистым разделителем `---`, не обрублен. Правил сам вслед за находкой отчёта (не в зоне тикета — «текст блоков не трогать», но зависшие ссылки вне журнала это не блоки): CLAUDE.md:116, `.rules:11`, `.chronos-ops/README.md:21`, `checkpoint/HANDOFF.md` (3 места) — все указывали на удалённый `handoff.log`, переведены на `handoff-2026-07.log`/`handoff-2026-08.log`/`log/INDEX.md` | `done/recon/T306-handoff-log-split.md`, отчёт в `reports-log/recon/` |
| T309 | **QA** | Живой клиентский UX/визуальный критик-аудит готового шелла (обе темы, все поверхности, честный вердикт) | **ЗАКРЫТА 2026-08-19 — принята.** Отчёт `reports-log/qa/T309-live-ux-critique-audit-report.md` (122 кадра dark+light в `/tmp/t309/`). Сверено архитектором, не на слово: файлы кадров существуют (74 dark + 48 light), геометрия поверхностей (`side_panel_left/right`, `frame_wrap_matte 0,0 2560x1440`, control-center `420x560`) совпадает с тем, что сам архитектор мерил живьём в T305/T307/T308 в этой же сессии; главная находка (dock не показывает pinned firefox/code/vivaldi, `"dock: skipping pinned app" reason="no AppEntry (no matching .desktop basename)"`) сверена дословно с кодом (`bar/widgets/dock.rs:225-268`, `PinSkipReason::as_str`) — не выдумана. Конфиги (`frame.toml`/`bar.toml`/`theme.toml`) SHA-256 до/после совпали, чужой `chronos`-процесс не остался. **Найден и исправлен методологический дефект отчёта** (не блокирует приёмку, но существенно): исполнитель заявил «обои возвращены `awww clear -o DP-1 000000`» — это заливка чёрным, не восстановление; на машине видео-обои через `waytrogen`+mpvpaper/awww-анимация, `awww restore` реально падает (`[ERROR] failed to load cached animation frames`) и оставляет чёрный экран молча. Архитектор поднял исходные обои `waytrogen --restore` (путь из `~/.config/waytrogen/config.json`), сверил живым скриншотом. T309-бриф исправлен на будущее (`waytrogen --restore`, не `awww restore`/`clear`). Один изолированный кадр (`bar-white-bg.png`) не отражает заявленный белый фон (таймстемп на 9с раньше соседнего кадра с подтверждённым белым) — похоже на гонку асинхронного `awww clear`, не на выдумку (соседние кадры того же прогона контраст подтверждают). Executive verdict исполнителя принят как есть: «не дерьмо, но до состояния показывать как законченный продукт не дотягивает» — 5 находок, полировка перенесена в `checkpoint/TBD.md` | `done/qa/T309-live-ux-critique-audit.md`, отчёт в `reports-log/qa/` |
| T311 | **FRONTEND** | Оболочка как единая плита: толщина краёв по функции (D3), рельсы одним материалом (D2a+D2b), замкнутая апертура (D4) | **ЗАКРЫТА 2026-08-19 — принята по решению владельца с двумя вынесенными остатками.** Первый разбор архитектора дал вердикт rework, владелец переопределил: закрывать, остаток резать тикетами, миньона по кругу не гонять. Что сверено по дереву и живьём (release, DP-1 2560×1440, белые обои под тёмной темой, обе панели открыты): **D2a+D2b приняты полностью и сделаны лучше брифа** — общий `crates/app/src/side_panel_common/surfaces.rs`, тесты T239 переехали целиком с `assert_ne!(chrome, content)`, `side_panel_right/surfaces.rs` стал 8-строчным re-export (шесть потребителей не тронуты), `side_panel_left/rail_view.rs:142` красится `side_panel_common::surfaces::chrome`, литерала `bg.tertiary` в рельсе нет; живой промер — левый и правый рельс дают идентичный hex, светлая тема проверена, историческая дельта `#1E1E2E` vs `#181825` ушла. **`bottom_thickness = 6` работает** — столбец x=1280 даёт `1434 (24,24,37)` до конца экрана. **D3 сломан и вынесен в T314:** матте честно перестаёт красить край при мапленном рельсе (`WrapSurfaceView::render` читает `wrap_inset_left(&cfg, rail_mapped(...))`), но полоса `frame_wrap_excl_left` создаётся один раз и зону больше не меняет — `apply_wrap` пересоздаёт wrap-набор только по `wrap_geometry_changed`, а маппинг рельса `WrapConfig` не меняет. Матте и резервация разошлись — тот же дрифт, что чинил T303. Проявляется не сразу, а на первой перерисовке: один и тот же открытый рельс, y=700, сразу после toggle → `0 (24,24,37)` плита; после `toggle-theme ×2` → `0 (255,255,255)` обои, рельс на 16. Критерий №1 брифа («рельс с x=0») не выполнен, зона слева осталась 56px. Критерий №2 (закрытая панель → плита появляется) выполнен, проверен. **D4 не сделан, вынесен в T316:** коммит `acb7f9fb` не меняет код, только комментарий; верхние углы апертуры прямые, потому что `bar.appearance.radius = 0.0` — исполнитель корректно не полез в чужую зону и написал об этом, но вердикт отчёта заявлял D4 готовым. **Незадекларированный четвёртый коммит `1f31807a`** (в отчёте заявлено три): 76 строк, `LAST_RAIL_MAPPING` пишется и никогда не читается для решения, предикат вызывается только из теста, комментарий на `frame.rs:910-920` описывает поведение, которого в коде нет. Мёртвая инфраструктура. **Правка 2026-08-19 после T314:** коммит выпилен из истории ресетом (`git reflog` HEAD@{6} `reset: moving to 1f31807a^`, HEAD@{5} коммит `81fd0184`) — в дереве его больше нет, `git log -S LAST_RAIL_MAPPING` показывает только `d93a5de6`. Историю T311 читать вместе с `81fd0184` («на доработку») и `7a00b5ef` («принят с остатками») — первое было отменено вторым по решению владельца. **Промеры отчёта не воспроизводятся** на собранном бинаре (заявлено `x=15..2543` сплошным `(24,24,37)` при белом контенте с x=56 в той же строке) — урок записан: отчёт без вставленного дословного вывода команд не принимать. Тесты прогнал сам: `cargo test -p chronos --lib` 604/604 (отчёт заявлял 605) | `done/front/T311-shell-single-plate-geometry.md`, отчёт в `reports-log/front/` |
| T309-rerun | **QA** | Повторный живой UX/визуальный критик-аудит по тому же брифу (тикет T309 закрыт первым прогоном) | **ПРИНЯТ ЧАСТИЧНО 2026-08-19.** Одна находка проверена и ценна, остальное — известное, уже починенное, либо третий повтор одной методологической ошибки. **ПРИНЯТО:** контраст `text.muted` — пересчитал независимо из палитры дерева по WCAG 2.x (линеаризация sRGB), цифры отчёта совпали до сотых: dark `#6c7086` на `#1e1e2e` = 3.36:1, light `#7d80a6` на `#dde0f2` = **2.91:1** (не проходит даже порог 3.0 для крупного текста). `text.secondary` при этом проходит в обеих (7.37 и 4.84) — дефект в одной роли, не в палитре. Заведён **T317**. **ПОВТОР:** dock/pinned apps — та же находка, что принята первым прогоном. **ОТКЛОНЕНО, «критическое №1» (прозрачная правая панель):** замерена техническая канва, а не панель — `CONTENT_CANVAS_WIDTH = 920` (`side_panel_right/mod.rs:70`) не ресайзится никогда (T276), красится только `visible_content_width`, остаток прозрачен по конструкции и кликов не ест (`content_input_region(...).is_empty()`, T302). Rail-only по умолчанию — T220, разобрано и закрыто в T302 как by-design. Полностью прозрачное окно неотличимо от отсутствия окна; мой промер на белых обоях с обеими панелями: y=700, x=56..2503 сплошной `(255,255,255)`. **Таблица по 11 вкладкам не может быть настоящей:** у каждой своя `preferred_content_width` (`tabs.rs:773` — System 400, Updates 420, Files/SourceControl 440, Editor/Terminal/Preview 560, Build 640), а отчёт даёт Files(440)=47/46 против SourceControl(440)=51/40 при равной ширине и ровно 51/40 сразу для семи вкладок с ширинами от 440 до 640. Девять одинаковых значений = переключения не было, один кадр измерен девять раз. **ОТКЛОНЕНО, «щель 24px»:** была 16px (плита 0..15, обои 16..31, рельс 32..70), аннотация отчёта себе противоречит (щель «16..40» и рельс «32..72» перекрываются); причина — двойная компенсация края, устранена коммитом `fdb69638` до разбора, отчёт снят на бинаре 08:27. **ОТКЛОНЕНО, §14 «wrap невидимый»:** третий заход одной ошибки — сэмпл мокапа `x=0..40` лежит внутри 60px служебной зоны и однороден по определению; перепромер даёт плиту по всему периметру (`#FCF5F3`/`#121116`, инсеты 60/10/10/10), разбор в `active/design/T310-*.md` раздел A. **ОТКЛОНЕНО, §15 Mocha Mousse:** `#FCF5F3` тёпло-белый, Pantone 17-1230 — коричневый `#A47864`; палитра остаётся своя, схема добавляется как обычная (T313, P3). **ЭРРАТА:** `workspace_mode` в `bar.toml` есть, его нет в `BUILTIN_NAMES` (убран T292) — причина описана наоборот, эффект верно; в `checkpoint/TBD.md`. **Методологический урок записан в отчёт:** прежде чем называть прозрачную область дефектом — ответить, кто в ней обязан был рисовать; одинаковые числа для сущностей с разными параметрами = эксперимент не менялся | тикет T309 остаётся в `done/qa/`, отчёт повторного прогона — `reports-log/qa/T309-live-ux-critique-audit-report-rerun.md` |
| T314 | **FRONTEND** | Живая `set_exclusive_zone` для wrap-полос: рельс спрятан → кольцо 16px, показан → сам кромка на x=0 (остаток T311 D3) | **ЗАКРЫТА 2026-08-19 — принята с первого захода.** Код `d93a5de6` (frame.rs +113, обе панели +19/−3). Сверено по дереву: `sync_wrap_excl_zones` ходит по живым хендлам и зовёт `window.set_exclusive_zone(px(zone))` — `close + open` в пути нет, то есть обход грабель, на которых утонул предыдущий заход, реальный, а не декларативный. Матте перерисовывается тем же сигналом через `matte.update(cx, ... cx.notify())`, ошибка не проглочена (`if let Err(e) => tracing::warn!`), правило «никогда `let _ = fallible()`» соблюдено. **Живой смок повторил архитектор** на его же бинаре (PID 2172793, DP-1 2560×1440, белые обои): рельс спрятан → `0..15` плита, `16` обои, полоса `frame_wrap_excl_left 0 15 16 1440`; рельс показан → `side_panel_left_rail 0 30 40 1404`, промер y=700 даёт `0 (24,24,37)` рельс, `39` бордер, `40 (255,255,255)` контент — **ноль пикселей обоев**, контент переехал с 56 на 40; после `toggle-theme ×2` промер побайтно тот же — **дрейф не возвращается**, а это и есть дефект, ради которого заведён тикет. `cargo test -p chronos --lib` 605/605 прогнал сам. **Эррата отчёта (не влияет на вердикт):** утверждение «заготовки `LAST_RAIL_MAPPING` из брифа в дереве не было никогда, `git log -S` пусто» неверно — код существовал в коммите `1f31807a`, который был выпилен ресетом до старта тикета (`git reflog`: HEAD@{7} коммит `1f31807a`, HEAD@{6} `reset: moving to 1f31807a^`, HEAD@{5} коммит `81fd0184` «инфраструктурный коммит выпилен»). `git log -S` был пуст честно, вывод «не было никогда» — нет. **Остаток, не блокирует:** `content_window_margin` обеих панелей теперь зовёт `wrap_inset_{left,right}_cached(true)` с зашитым `true` — исполнитель нашёл верную причину (`set_rail_mapped(true)` вызывается ПОСЛЕ открытия окон, живой флаг читался stale и давал x=56), но лечение опирается на допущение «контентное окно открывается только вместе с рельсом». Сегодня это так; если появится путь, открывающий контент без рельса, маргин соврёт. Записано в `checkpoint/TBD.md`. **Косметика:** поверхность `frame_wrap_excl_left` при мапленном рельсе паркуется в `40 12 16 1440` вместо того, чтобы остаться на x=0 с нулевой зоной — ничего не рисует и ввод не берёт, визуально безразлично | `done/front/T314-wrap-strips-live-exclusive-zone.md`, отчёт в `reports-log/front/` |
| T315 | **DESIGN** | Рельс как кромка кадра: артборды и спека (не кодовый тикет) | **ЗАКРЫТА 2026-08-19 — принята владельцем со второго раунда.** Артборд `.chronos-ops/design/rail-frame-edge-artboards.dc.html` (620 строк, 7 видов на одном канвасе 2560×1440). Заведена по прямому диагнозу владельца: «все пытаются легко и дёшево построить эту обёртку, никто не перерисует рельсы, чтоб они подходили к дизайну» — вся линия T303→T314 двигала геометрию, но внешний вид рельса не трогал ни один тикет. **Раунд 1 отклонён владельцем**, три претензии: внутренние углы не скруглены (проверено грепом — скруглений апертуры в файле ноль, все радиусы принадлежали пилюле и полоске, углы нарисованы прямыми и подписаны «straight edge, no seam», то есть задокументирован статус-кво вместо решения); нижняя кромка 6px против рельса 40 и бара 30 — волосок (живой промер, столбец x=1280, плита `1434..1439`); карточки не выглядят выезжающими из рельса. Плюс замечание архитектора: заявленная замена 3px полоски пилюлей на деле была пилюля **и** 2px акцентная линия на том же внутреннем крае с выносом `-4` за габарит, то есть та же идиома на пиксель тоньше. **Раунд 2 принят**, сверено по содержимому файла: `border-top-left-radius:10px` и `border-bottom-left-radius:10px` по 11 вхождений в обоих состояниях и обеих темах, радиус обоснован (25% рельса / 33% бара / 83% нижней кромки); `height:12px` 6 вхождений, выведен от бара и рельса (40% и 30%), верно опознан корень прежней шестёрки — наследство `bottom_strip.height` из Hide-режима; вид 7 «ящик» с прямой ближней стороной на x=40, дальней r=8 и стартом анимации от кромки рельса; `width:2px` ноль вхождений — полоска убрана. **Не покрыто артбордом, передано требованиями в T318** (третий раунд ради этого не гоняли): правый край не нарисован вовсе (ни правых радиусов, ни слов «right»/«mirror» — а зеркальность не бесплатна: пилюля переезжает на противоположный край, ящик выезжает влево); два радиуса в одной оболочке (апертура 10, дальняя сторона панели 8) — требуется явный ответ, дрейф это или замысел; `RAIL_WIDTH = 40` и `wrap.thickness = 16` остались независимыми числами (пункт 5 исходного диагноза). **Расхождение спеки с деревом решено архитектором:** спека предлагала «200ms ease-out», а в дереве есть общий язык движения `motion.rs` (T129, `ENTER_MS = 260`, `SLIDE_PX = 14`, `EaseOutBack(1.5)`, готовые `apply_enter_from_left/right`); в T318 предписано брать существующие хелперы и менять только точку старта — два тайминга одного класса движения есть ровно тот дрейф, против которого тикет и затевался | `done/design/T315-rail-as-frame-edge-artboards.md`, оба отчёта в `reports-log/design/` |
| T318 | **FRONTEND** | Рельс как кромка кадра: реализация принятой спеки T315 (шов, пилюля, радиус апертуры, нижняя кромка, выезд панели) | **ЗАКРЫТА 2026-08-19 — принята владельцем глазами после трёх раундов исполнителя и эрраты архитектора.** Код `6a914b2c` (6 файлов, +196/−90). **Принято от исполнителя:** шов `border_r_1`/`border_l_1` убран с обоих рельсов; полоса-индикатор заменена пилюлей (`accent.primary` α 0.15 тёмная / 0.12 светлая по `theme.is_light`), правый зеркалит, док-строка `rail.rs:4` поправлена; контентное окно — дальняя сторона r=8, ближняя прямая, вход левой панели через `apply_enter_from_left`; диагноз «радиус висел на неокрашивающем родителе» найден исполнителем самостоятельно. **Эррата архитектора — форма была вывернута, три захода подряд:** (1) `rounded_tr/br` + `overflow_hidden` на корневом div не рисовало ничего — `overflow_hidden` в форке режет прямоугольной маской; отчёт объяснял это «ограничением Wayland layer-shell», опровергнуто тем же кадром, где контентная панель даёт чистое сглаживание; (2) радиус на элементе с `.bg()` — промер честные r=10 и градиент 27/51/98/168, но скругление СРЕЗАЕТ материал у кромки, рельс стал плашкой со скруглёнными краями и обоями в углу; (3) четыре квадрата с одним скруглённым углом — выпуклая четверть круга, торчащая в вырез, владелец назвал «квадратные прыщи»; `border-radius` режет углы только наружу, вогнутую галтель им не построить. **Сработало кольцо с бордером:** скругление гнёт и внутренний контур, внутренний радиус = «наружный − толщина»; кольцо толщиной `radius` по границе выреза, расширенное на ту же толщину наружу, наружный радиус `2 × radius` → внутренний ровно `radius`, наружный контур прячется под баром и рельсами, угол дисплея не трогается. Плоские края вынесены отдельным слоем: абсолютные дети позиционируются от padding-box, и отступы от бордеров уезжали ровно на толщину края (галтель улетала на x=80 при крае 40). Кольцо ужимается до `min(все инсеты)`. **Живой промер архитектора,** верхний левый угол окна, белые обои: y=30 хром до x=45 и дуга 111→181→228→252, y=34 до 40, y=40 до 39 и дальше прямая; зеркально снизу и справа; рельс — прямой прямоугольник, край строго x=40 сверху донизу. Нижняя кромка поднята до 16 и привязана к `DEFAULT_THICKNESS` — низ и боковое кольцо стали одной величиной (закрывает пункт 5 диагноза T315). `--lib` 605/605 прогнан сам. **Урок в дисциплину:** промер числами не заменяет взгляда на силуэт — радиус меряется, вывернутая кривизна только видна; для любой правки формы сначала кроп угла и глаза, потом числа. **Хвосты:** верхний угол при открытой контентной панели перекрывается её прямым углом; геометрия по каждому краю в конфиге — **T319** | `done/front/T318-rail-as-frame-edge-implementation.md`, отчёт в `reports-log/front/` |
| T320 | **FRONTEND** | Вернуть девять вкладок в правую панель, снять control-center popup | **ЗАКРЫТА 2026-08-19 — принята с первого захода.** Код `c2379fcc` (9 файлов, `control_center.rs` удалён целиком). Тикет заведён по прямой находке владельца: выключил обёртку — вкладки остались попапами. Разбор показал **дрейф в две стороны**: T294 специально убрал попап у обновлений (виджет бара стал открывать `PanelTab::Updates` в панели) и тем же заходом `notifications/history_popup` заменили на `PanelTab::Notifications` — проект вытаскивал попапы В панель; через два дня T305 утащил девять вкладок ИЗ панели в попап, и архитектор принял второй, не сверив с первым. Владелец: «у нас были всегда панели вкладки справа, не попап карточки», затем «вкладки вернуть короче» — промежуточный вариант «вкладки в normal, попапы в wrapped» свёрнут им же, оставлено одно поведение всегда. **Сверено архитектором по дереву и живьём:** `control_center` в `crates/` больше нет (единственное вхождение — исторический комментарий `frame.rs:848` про кровный факт T305); `cargo check --lib --bins` чисто, `--lib` 605/605 и `--bins` 797/797 прогнаны сам, числа отчёта сошлись; `PanelTab::ALL` стал 22, `ALL[21] = Media`, Media и LauncherSettings в рельсе обоих режимов; живой прогон шести вкладок через IPC — слоя `control_center` в `hyprctl layers` нет ни разу. **Ширина проверена пикселями:** на `launcher_settings` (410 по журналу) левый край тёмного столбца ровно `x=2150` = 2520 − (410 − 40). **Качество отчёта отдельно:** ширины доказаны дважды и независимо — журналом (`state.width`) и пиксельным промером; это ровно то, чего не хватало в T311, где числа не воспроизводились на собранном бинаре. Оговорка про отсутствие живого клика мышью честная и обоснованная (после правки клик рельса и `select-tab` — один код). Мёртвый код вычищен без напоминания: захват живых bounds иконки (`canvas` + `Rc<Cell<Bounds>>`) в `rail.rs` существовал только ради анкоринга попапа, сигнатура `on_select` упрощена с четырёх параметров до трёх. **Решение по Media принято:** оставлен и посажен в рельс — у него готовая ветка `TabContent::Media`, живая сущность `MediaTab` и зарегистрированная иконка; выпиливать значило бы резать живой контент. **Чужая находка вынесена в T321:** при пятикратном быстром переключении `frame.style` wrap-поверхность падает с `Adapter "NVIDIA GeForce RTX 3070" ... not compatible`, дальше `Protocol error` и потеря слоёв, рестарт восстанавливает; архитектором НЕ воспроизведено, принято со слов — первым шагом T321 стоит воспроизведение. Класс тот же, что в T314 (`close + open` даёт протокольные ошибки), и путь пересоздания станет основным после T319 | `done/front/T320-tabs-back-to-panel.md`, отчёт в `reports-log/front/` |
| T319 | **FRONTEND** | Геометрия оболочки по каждому краю в конфиге + один источник ширины рельса | **ЗАКРЫТА 2026-08-19 — принята с первого захода.** Код `75640b7d` (4 файла, +362/−134). `[wrap]` переведён на `top/left/right/bottom` + `inner_radius`; `top = 0` — сентинел «следовать `bar.toml [appearance] height`», ненулевое значение это оверрайд с одной строкой в лог; `left`/`right` действуют только когда рельса на крае нет (мапленный рельс сам и есть край, T314). **Сверено архитектором живьём, а не по отчёту:** конфиг ТОЛЬКО со старыми ключами (`thickness = 20`, `bottom_thickness = 6`) даёт `excl_left w=20`, `excl_right w=20`, `excl_bottom` высотой 6 — не схлопывается в дефолт 16/16/16, **инцидент T268 закрыт**; мусорное значение (`left = "много"`, `right = 22`, `bottom = 7`) роняет в дефолт ТОЛЬКО свой ключ, right и bottom выживают, файл не переписан (`cat` дословно); per-edge правки двигают ровно свой край (`24 / 30 / 8`). Ручной `Deserialize` вместо `#[serde(alias)]` обоснован верно — алиас маппится один-в-один и не может размножить `thickness` на два поля. **Единый источник ширины рельса:** `RAIL_INSET` из дерева исчез, остался `frame.rs:63 pub(crate) const RAIL_WIDTH`, панели реэкспортируют; правило «frame не импортирует panels» не нарушено. Аргумент против `set_after_apply` записан и верен: frame читает ширину синхронно при открытии поверхностей и в `Render`, а хук исполняется после `apply`. `--lib` 609/609 (было 605), прогнан архитектором. **Известный артефакт, не блокер:** при асимметричных боковых краях нижняя полоса уезжает по x (`left=24 right=30` → `x=-3`) — якорь `LEFT|RIGHT` при фиксированной ширине центрирует переполнение в урезанной боковыми резервациями области, `24 + (2506−2560)/2 = −3`; при симметричных краях `x=0`. Визуально не проявляется (промер y=1436 на обоих нижних углах даёт сплошной хром — полоса невидима, краску кладёт матте), эффект только резервационный: внизу справа на 3px короче. Та же механика уже кусалась в T303 (замеренные −28); лечится приёмом T308 (`exclusive_zone: Some(px(-1.))` + явный margin), когда владелец реально уйдёт в асимметрию. **Осознанно не сделано и принято:** `[wrap] rail` (конфигурируемая ширина рельса) — тянет геометрию rail-поверхностей обеих панелей, эксклюзивные зоны, `content_window_margin` и кольцо апертуры, бриф давал это выбором исполнителя; ползунки в `bar_settings.rs` — бриф помечал опциональными. Конфиг владельца восстановлен из бэкапа дословно | `done/front/T319-per-edge-frame-geometry-config.md`, отчёт в `reports-log/front/` |
| T317 | **FRONTEND** | `text.muted` не проходил WCAG: 2.91:1 в светлой, 3.36:1 в тёмной | **ЗАКРЫТА 2026-08-19 — принята с первого захода.** Код `bdae4e17` (2 файла, +73/−4). Тёмный `#6c7086` → `#83879f`, светлый `#7d80a6` → `#5f6280`. **Пересчитано архитектором независимо** (своя реализация WCAG 2.x с линеаризацией sRGB) — цифры отчёта сошлись до сотых: dark 4.63 на `bg.primary` и 4.96 на `bg.tertiary`, light 4.52 и 5.13; `secondary` не тронут и по-прежнему проходит (7.37 / 4.84), иерархия светлоты монотонна. `chronos-ui` 24/24, `chronos --lib` 609/609 прогнаны сам. **Бриф архитектора был неправ, исполнитель принёс факт:** бриф утверждал, что тёмный muted приходит из `DEFAULT_BASE16` base05 — сверено грепом, у константы **единственное вхождение это само объявление** (`schemes.rs:28`), её не читает ни `Theme::default()`, ни `to_theme()`, ни `default_scheme()`. Тёмный muted зашит в `Theme::default()` (`mod.rs:193`), и правка только в `default_scheme()` дала бы рассинхрон: `scheme = "Default"` из файла показывал бы новый цвет, а холодный старт без файла (`select_scheme(None)` → `Theme::default()`) — старый. Исполнитель вышел за формальную зону брифа (тронул `mod.rs`), объяснив почему — правильно; второй случай за сессию, когда приходят с фактом вместо подгонки под план. `placeholder` осознанно отвязан от `muted` и оставлен на `#6c7086` (4.5 для приглушённого состояния не требуется), иерархия не перевернулась. **Тесты сделаны как просил бриф:** `muted_passes_wcag_aa_on_primary_in_all_schemes` итерирует `builtin_schemes()`, то есть будущая Mocha Mousse из T313 обязана пройти те же ворота автоматически; плюс `text_lightness_hierarchy_is_monotonic_in_all_schemes`. Проверка «тест вообще работает» сделана: старые хексы временно возвращены, тест покраснел с дословными 3.36 / 2.91. **Оговорка про живой промер принята:** muted на `bg.primary` с кадра не снят — контентная зона перекрыта окнами владельца, и исполнитель НЕ стал их закрывать (верное поведение); закрыто гистограммами бара и рельса, которые дают точное совпадение рендера с палитрой по хексам, плюс детерминированным юнит-тестом. **Хвост в `TBD.md`:** `DEFAULT_BASE16` мёртвая — ловушка для следующего, кто поправит цвет в константе и не увидит эффекта | `done/front/T317-text-muted-wcag-contrast.md`, отчёт в `reports-log/front/` |
| T321 | **FRONTEND** | Быстрое переключение `frame.style` роняет wrap-поверхность на адаптере, шелл теряет слои | **ЗАКРЫТА 2026-08-19 — принята с первого захода.** Код `bbf61f02` (`frame.rs`, +83/−50). **Воспроизведено исполнителем** (архитектор при заведении тикета не смог) и уточнена сама постановка: отказ не «wrap-поверхности», а глобальный — валятся bar, панели и hide-полоса; спусковой крючок — открытые боковые панели, при закрытых 20 переходов дают ноль ошибок. **Корень заявлен фактом со ссылкой, а не гипотезой:** `Source/gpui_wgpu/src/wgpu_context.rs:194-200`, `check_compatible_with_surface` → `surface.get_capabilities(&adapter)` даёт пустой `formats` → ровно та строка про адаптер; сопутствующий `Protocol error on wp_viewport` — обращение к объекту уже уничтоженной поверхности. Гонка жизненного цикла в форке, не логика приложения. Сверено архитектором по исходнику. **Две гипотезы брифа опровергнуты:** «падает всегда ExclBottom» (нет — падает первое же открытие после накопления каскада, порядок случаен) и «исчерпание адаптера» (нет — не лимит, а гонка close/open). **Главный фикс проверен архитектором живьём:** правка `[wrap] left=32 right=32 bottom=20` на поднятом шелле даёт `excl_left 16→32`, `excl_right 16→32`, `excl_bottom y 1424→1420`, при этом `closed wrap surface = 0`, `geometry synced = 3`, `Protocol error = 0`, pid не менялся — геометрия мутируется на живых поверхностях (`window.resize` + `set_exclusive_zone` + `cx.notify` для матте), рекрейт убран. Третий случай, когда это семейство лечится одним приёмом «мутировать живьём вместо close+open» (T314, T321). **Сверх брифа и по делу:** `apply_wrap` теперь открывает wrap-набор ДО сноса hide-полосы, `open_wrap_windows` возвращает `bool` — при неудаче остаётся предыдущий стиль, а не frame-less полу-состояние. Границы соблюдены: в панели и в форк не полез, хотя корень там. `--lib` 609/609 прогнан архитектором, конфиг владельца возвращён дословно. **Остаток вынесен в T322:** style-переход при ОТКРЫТЫХ панелях по-прежнему роняет адаптер — каждая панель через `apply_frame_inset` пересоздаёт по две поверхности, каскад `4+4+2×2+2×2` за секунду дёргает гонку форка (замеры: 14 `failed to open`, **5 343 366** `Protocol error` за 20 переходов) | `done/front/T321-wrap-surface-recreate-adapter-failure.md`, отчёт в `reports-log/front/` |
| T322 | **FRONTEND** | Панели пересоздают поверхности при смене стиля рамки — каскад роняет адаптер | **ЗАКРЫТА 2026-08-19 — принята. Главный результат тикета: диагноз, а не код; падение оказалось РЕГРЕССОМ АРХИТЕКТОРА из T321.** Код `c51dbee8` (эррата архитектора в `frame.rs`) + `4a52e091` (правка панелей исполнителя). **Обе гипотезы моего брифа опровергнуты** — не гонка close/open и не исчерпание адаптера. Падение детерминированное: при мапленном рельсе `wrap_inset_left/right(cfg, true) == 0`, `sync_wrap_surfaces` ресайзит полосу в нулевую ширину, а форк клампит размер только на ветке `set_geometry` (`Source/gpui_linux/.../wayland/window.rs:1553`) — в `viewport.set_destination` (`:1335`) уходит сырое значение; `set_destination(0, h)` = нарушение `wp_viewport` («Size was <= 0»), соединение убито. Исполнитель принёс это с точными строками. Второй его факт: **живого сеттера `margin` в форке нет** (`set_margin` зовётся один раз в `WaylandSurfaceState::new()`), живые только `set_exclusive_zone`/`set_exclusive_edge`/`resize`; третий — боковая компонента `content_window_margin` при мапленном рельсе сворачивается в ноль в ОБОИХ стилях, то есть при Hide↔Wrap меняется только высота. **Регресс воспроизведён и атрибутирован архитектором:** на дереве после T321 одно открытие левой панели при `style = "wrap"` даёт 0 слоёв, `Size was <= 0` = 1, `Protocol error` = **1 441 388**; откат `frame.rs` на `bbf61f02^` — 7 живых слоёв и ноль ошибок. В приёмке T321 архитектор проверил смену геометрии и НИ РАЗУ не открыл после неё панель — тот самый провал, который сам двумя тикетами раньше записал в дисциплину. **Критерий №1 брифа выполнен:** 20 переключений стиля шагом 400 мс при обеих открытых панелях → 9 живых слоёв, `failed to open` / `not compatible` / `Protocol error` / `Size was <= 0` = **0**. **Изоляция:** тот же сценарий с эрратой, но БЕЗ правки панелей даёт тот же результат — падение чинит эррата, а не устранение каскада; записано, чтобы в истории не осталось ложной причинности. **Правка панелей принята отдельно и по своей причине:** убирает `close + open` двух поверхностей на style-переход в пользу живого `window.resize` — третий случай того же принципа (T314, T321, T322); сканер `window_options_have_no_resize_calls` уточнён точечно и проверен на покраснение. `--lib` 609/609. **Хвост в `TBD.md`:** кламп в `viewport.set_destination` форка отсутствует — обошли со своей стороны, но любая поверхность, попросившая нулевой размер, повторит трюк | `done/front/T322-panel-inset-live-instead-of-recreate.md`, отчёт в `reports-log/front/` |
| T316 | **FRONTEND** | Верхние углы апертуры: бар перестаёт быть прямоугольником в режиме wrap | **ЗАКРЫТА 2026-08-20 — принята находка, код НЕ взят.** Тикет закрыт как «уже сделано T318», правка исполнителя оставлена в стэше (`stash@{0}`), в дерево не пошла. **Главный факт отчёта подтверждён живым промером архитектора на белых обоях** (обе панели открыты, `grim`, скан первой белой колонки по строкам): верхний левый угол апертуры даёт кривую x = 50, 46, 44, 43, 42, 42, 41, 41, 41, 41, 40 на y = 43…53; нижний левый — зеркальную 40, 41, 41, 41, 41, 42, 43, 44, 46, 48/50 на y = 1414…1423; правая сторона зеркальна (2519 → 2509). Радиус ≈10 = дефолт `wrap.inner_radius` после T315, **все четыре угла скруглены и численно совпадают** — пункты 1 и 2 приёмки выполнены кольцом `aperture_ring` из T318, без единой строки в баре. Бриф писался до T318 и устарел вместе с числом «дефолт 16». **Пункт 3 (hot-reload) проверен архитектором на двух значениях, чего в отчёте не было:** `[wrap] inner_radius = 24` → кривая растянулась на 24px (64…40 по y = 43…67), `= 8` → сжалась до 8 (48, 44, 43, 42, 41, 41, 41, 40), верх и низ меняются синхронно, без рестарта. Это тоже свойство кольца, а не правки. **Заявленная остаточная ценность правки — «латентный баг: при `appearance.radius > 0` в wrap старый uniform `.rounded()` пускал бы обои сквозь верхние углы бара» — НЕ ВОСПРОИЗВЕЛАСЬ.** Собран и поднят бинарь на чистом HEAD (правка в стэше), `bar.toml radius = 12.0`, белые обои: угол экрана (0,0)…(10,10) — хром `24,24,37`, белого ноль; при открытых рельсах и при закрытых одинаково. Скруглённый угол бара подложен хромом wrap-поверхности, то есть невидим. Баг не спящий, а несуществующий в этой геометрии. **Что правка делала бы взамен:** молча отбирала у пользователя `appearance.radius` в режиме wrap (компенсация — один `tracing::info!`, гейт `AtomicBool` проверен живьём: строка в логе ровно 1 за прогон) и скругляла бы низ бара безусловно, не глядя на `appearance.edge` — для нижнего бара это скругление не с той стороны. Платить поведением за баг, которого нет, не стали. **Качество отчёта отдельно:** исполнитель сам вскрыл, что бриф устарел, промерил ДО правки, честно написал «для апертуры правка no-op» и отдал решение владельцу вместо того, чтобы протащить код под видом фикса. Это ровно то поведение, которого не хватало в T311 и T318. **Дисциплина:** бриф T316 был написан 2026-08-19 до приёмки T318 и не перепроверен архитектором перед выдачей — задание пережило собственную предпосылку. Перед выдачей тикета из очереди сверять его предпосылки с деревом на текущий момент, а не на момент написания | `done/front/T316-bar-radius-closes-aperture-top.md`, отчёт в `reports-log/front/` |
| T312 | **FRONTEND** | Два режима оболочки: `normal` / `wrapped`, нижняя планка снята | **ЗАКРЫТА 2026-08-20 — принята с первого захода + однострочная эррата архитектора.** Код `0e0ee38a` (3 файла, +203/−325 — впервые за волну удалено больше, чем добавлено). `FrameStyle::Hide/Wrap` → `Normal/Wrapped`, дефолт `Normal`; `apply_hide/apply_wrap` → `apply_normal/apply_wrapped`; вся сущность нижней планки удалена целиком (`BottomStripView`, её `window_options`, `FRAME_WINDOW`, `open`/`close`, `hide_strip_wanted`, `hide_strip_insets`) — в `wrapped` низ рисует матте через `wrap.bottom`, в `normal` низа нет. **Сверено грепом по всему `crates/`:** ни одного `FrameStyle::Hide/Wrap`, `hide_strip*`, `BottomStripView`, `apply_hide`, `STYLE_ID_HIDE`; `bottom_strip`/`FrameJunction` живут только внутри `frame.rs` как парсящееся легаси (T268 не повторяется). **Главный риск тикета — алиасы — проверен мутацией, а не на слово:** снял `"hide"`/`"wrap"` из `from_str_sanitized` → `style_legacy_aliases_parse` и `style_parse_is_case_insensitive` покраснели, вернул — зелено. `--lib` 610/610 (609 − 2 теста на удалённый код + 3 новых; арифметика в отчёте записана как «−2/+4», счёт при этом верный). **Живая приёмка архитектором, белые обои, оба режима:** п.2 `normal` — `frame_wrap_*` ноль слоёв, рельс `x=0..39` (`20,700 = 24,24,37`), обои с `x=60`, нижняя строка `1280,1439 = 255,255,255` (планки нет); п.1 `wrapped` — плита снизу `24,24,37`, дуга апертуры 50/44/42/41/40 на y=43…55 совпала с промером T316 (картинка T318 не сломана); п.4 алиас `style = "wrap"` + легаси-ключ `thickness = 24` живьём — `excl_left x=0 w=24`, `excl_right x=2536 w=24`, `bottom` не тронут (16); п.5 `style = "banana"` — ноль `frame_wrap_*`, ровно один warn `falling back to normal`, `thickness = 24` уцелел в файле; п.3 **пять циклов `wrapped↔normal` при ОБЕИХ открытых панелях** (тот самый сценарий, который ронял адаптер в T321/T322) — по одному слою каждого вида, сирот нет, `Protocol error` / `failed to open` / `Size was <= 0` / паник = **0**, pid жив; п.6 обе темы — светлая `wrapped`: плита = бар = рельс `236,238,250`, светлая `normal`: низ `255,255,255`, `frame_wrap_*` нет. **Уточнение к отчёту:** числа п.4 в отчёте сняты при НЕмапленных рельсах, хотя написано «обе панели открыты» — при мапленном рельсе `wrap_inset_left/right = 0`, полоса сохраняет прежний футпринт и меняет только зону (T322-эррата), что я и увидел (`x=40 w=16 zone=0`). Кода не касается, но в отчёте подпись к промеру неточная. **Эррата архитектора (одна строка, в этом же коммите):** гейт легаси-варна был написан как `!LOGGED.swap(true) && cfg.bottom_strip != default` — `swap` срабатывает ПЕРВЫМ и сжигает одноразовость на первом же `apply` чистого конфига; секция, добавленная позже хот-релоадом, глоталась молча навсегда. Воспроизведено живьём (правка `height = 8` на поднятом шелле — ноль варнов), порядок условий переставлен, перепроверено: чистый старт — 0, первая легаси-правка — 1, вторая — по-прежнему 1. Ровно тот запрет «молча игнорировать чужую настройку нельзя», ради которого гейт и заводился. **Границы:** исполнитель вышел за формальную зону в `bar/mod.rs` (1 строка) и `bar_settings.rs` (24 строки, метки сегментов `Hide/Wrap` → `Normal/Wrapped`) — переименование энума без этого не компилируется, правка механическая, принято. **Хвост в `TBD.md`:** в `normal` под баром видна строка обоев в 1px (`y=41` при высоте бара 42, `y=40` — бордер) — к T312 отношения не имеет (бар не трогали), но глазу заметно на светлых обоях | `done/front/T312-frame-modes-normal-wrapped.md`, отчёт в `reports-log/front/` |
| T313 | **FRONTEND** | Picker схем со свотчами + схема Mocha Mousse | **ЗАКРЫТА 2026-08-20 — принята с первого захода, плюс четыре правки архитектора по живым замечаниям владельца.** Код исполнителя: `8629bac6` (схема, `schemes.rs`) и `4a960516` (пикер, `bar_settings.rs` + `theme_config.rs`) — раздельными коммитами, как требовал бриф. **Решение «одна схема или две» принято и обосновано числами:** только тёмная. Светлая палитра брифа (`#FCF5F3`) — та самая отменённая ошибка QA-миньона из T310, и она НЕ проходит ворота T317 (`text.muted #A88B72` на `#FCF5F3` = 2.95:1). **Все контрасты пересчитаны архитектором независимо (WCAG, линеаризация sRGB) — совпали до сотой:** muted 5.43, secondary 10.14, primary 13.73, error 4.95, warning 6.69, success 6.66, info 7.01; отвергнутые статусы брифа на тёмной базе — error 2.88, success 3.65 (нечитаемы как текст, осветлены исполнителем в тёплый тон). **Тест на контраст проверен мутацией:** подставил `muted = #877b6f` → покраснели и новый `mocha_mousse_muted_passes_wcag_aa_on_primary`, и общий T317-итератор `muted_passes_wcag_aa_on_primary_in_all_schemes`, оба с дословным `3.93:1`. `chronos-ui` 27/27, `chronos --lib` 610/610. **Живая приёмка мышью, а не рассуждением** (ydotool, координаты на этой машине = экран/2): клик по карточке Mocha → `theme: selected scheme="Mocha Mousse" is_light=false surface_alpha=0.85`, бар/рельсы/панель перекрасились в одном кадре (`24,24,37` → `24,21,29`); клик по Light → то же для светлой. **П.3 сверен дословно:** до клика в `theme.toml` лежали `surface_alpha = 0.85` и `blur_enabled = true`, после клика поменялся ровно ключ `scheme`, оба соседних значения байт-в-байт целы. **П.4** — отметка переезжает (`scheme_core_matches` сравнивает цветовое ядро без surface-токенов, поэтому полупрозрачная тема не ломает подсветку). **П.7** — холодный старт с Mocha в файле: `theme: env=None, file=…, bg.primary l=0.13` (= `#241F23`). **Честность отчёта отдельно:** исполнитель сам написал, что широкую раскладку снял на ВРЕМЕННОЙ правке (`EditorSettings => 760`), вернул её и в финальном диффе `tabs.rs` нет — сверено, диффа не было. Он же вынес наблюдение, что `toggle-theme` после появления пикера жёстко возит Default↔Light мимо выбранной схемы. **Правки архитектора по ходу живого разбора с владельцем (четыре коммита):** `d3ca6570` — ширина вкладки System settings 410 → **800** (владелец: «надо вкладку расширить»); при 410 видимая доля = 370 < `GRID_BREAKPOINT` 720, то есть весь респонсив T231 на этой странице был структурно недостижим, а пикер мог рисоваться только в одну колонку; тест `breakpoint_uses_visible_slice_not_fixed_wayland_canvas` переписан под новую пару (узкий таб = LauncherSettings 370, System settings 760 — широкий). `30d1a3e5` — сегменты (`segmented`/`seg_chip`) тянутся на всю строку, дырка между подписью и контролом убрана; Blur перестал наезжать на Elevation: длинная строка `import 45-surface-effects-chronos.lua` не имела flex-границы, брала натуральную ширину и вылезала из ячейки грида в соседнюю колонку — длинное объяснение переехало в подпись строки, на чипе остался короткий статус, `setting_label` теперь уступает контролу, слайдер 110 → 96. `f786836d` — карточка настроек тянется на всю высоту вкладки: под ней зияло ~85px голого фона колонки; корень оказался не в карточке, а в том, что корень вкладки не растягивался в `h_full`-колонке контента (цепочка `flex_1` → `flex_1` + явный `flex_col` → `flex_grow_1`; `flex_1` на карточке брать нельзя — обнуляет базис и убивает скролл длинной страницы). **Хвост в `TBD.md`:** контролы в колонках стартуют с пяти разных отступов (левая 180/180/245/310/370, правая 540/570/600/715) — лесенка; лечится фиксированной колонкой подписи. Плюс наблюдение исполнителя про `toggle-theme` | `done/front/T313-theme-picker-and-mocha-mousse-scheme.md`, отчёт в `reports-log/front/` |
| T310 | **DESIGN** | Канон решения по оболочке: `normal`/`wrapped` + палитра (нарезка на исполняемые тикеты) | **ЗАКРЫТ 2026-08-20 — исчерпан, все потомки приняты.** В исполнение никогда не шёл: тело брифа написано QA до перепромера мокапа, его вывод «не рисовать плиту» опровергнут архитекторским дополнением (разделы A–K) — в мокапе хром сплошной, а «разорванная рамка» была двойным инсетом. Из канона нарезано и закрыто: **T311** (единая плита, толщина по краям), **T312** (режимы `normal`/`wrapped`, алиасы, снос планки), **T313** (палитра Mocha Mousse + пикер схем); родственная линия — T314, T315, T318, T319, T321, T322. Эпик 1 в исходной формулировке отменён, Эпик 2 переформулирован владельцем (`#FCF5F3` из мокапа — тёпло-белый фон, а Pantone 17-1230 — коричневый `#A47864`; миньон приклеил модное имя к чужому цвету). **Живые решения вынесены в `checkpoint/ARCHITECTURE.md`, разделы 16–17** — тикет-файл больше не единственный носитель канона; сам файл переехал в `done/design/` как разбор, `active/design/DESIGN.md` указывает туда | `done/design/T310-wrap-mode-invisible-and-mocha-mousse-palette.md` |
| T277 | **QA/REVIEW** | Враждебный аудит T276 (standalone rail/content правой панели) | **ЗАКРЫТ 2026-08-20 как выполненный другими тикетами, аудит отдельным прогоном не гонялся.** Предмет проверки (11 пунктов чек-листа) с тех пор трижды перепахан и принят с живыми смоками: раздельные поверхности и namespace'ы — `side_panel_right/mod.rs:289,350`, живьём `side_panel_right_rail w=40` + `side_panel_right_content w=920`; ресайза в drag-пути нет по конструкции (T273/T276, зафиксировано в шапке модуля: «removes window resize from the drag path entirely»), а на левой панели ту же инвариантность стережёт сканер-тест `window_options_have_no_resize_calls` (`side_panel_left/mod.rs:1223`); эксклюзивная зона — один владелец, значение живое через `set_exclusive_zone` (`mod.rs:272-273`); открытие/закрытие обеих поверхностей и класс «осиротевших слоёв» разобраны и починены в T321/T322 с промером «20 переключений → ноль протокольных ошибок»; вкладки, `select-tab` и ширины перепроверены пиксельно в T320; rail-only как дизайн подтверждён T302. Держать тикет в `hold` дальше значило бы просить миньона доказать то, что уже доказано четырьмя приёмками | `done/qa/T277-audit-standalone-right-panel-surfaces.md` |
| T191 | **QA** | Смок слайса 5 (план §8 P1–P10, цели CS2/PUBG/SCUM) | **ОТКЛОНЁН 2026-08-20 — снят как неактуальный, не выполнялся.** Припаркован ещё 2026-08-02 с формулировкой в собственной шапке: «low priority vs Agent Shell DE». За три недели продукт ушёл именно туда — волна 2026-08-15…20 это правая панель, левый агентский воркспейс, оболочка и тема; игровой слайс не двигался, `Captures` до сих пор честный empty-state («no capture backend — slice 6»). Смок написан против плана, которого больше нет в работе; переоткрывать — только новым тикетом от текущего состояния Gamer-режима, а не воскрешая этот | `reject/qa/T191-slice-5-smoke.md` |
| T323 | **QA** | Аудит шелла глазами покупателя, весь продукт одним заходом | **HOLD/SPLIT 2026-08-21.** Один проход не покрыл шелл; кадры сгорели в `/tmp`. Нарезка T324–T328. Мега-бриф не исполнять. Черновики первого захода остаются в `reports-fresh/` как гипотезы, не приёмка | `active/hold/T323-release-grade-ux-audit-every-surface.md` |
| T324 | **QA** | Бар, dock, виджеты — живой UX | **ПРИНЯТ 2026-08-21.** Вердикт НЕТ. Сверено: календарь без `.bg(` (`calendar_popup/view.rs` + `transparent_black` Root); listener без `ActiveMonitorChanged`; network двухстрочный на `HEIGHT_MIN=20`. Клики по точкам живые (гипотеза T323 опровергнута). Улики `dump/qa-ux/T324/` 68+49. bar.toml height 20 — внешняя UI-запись, не QA | `done/qa/T324-bar-dock-widgets-ux-audit.md`, отчёт в `reports-log/qa/` |
| T325 | **QA** | Попапы, start, launcher, OSD, click-catcher | **ПРИНЯТ 2026-08-21.** Вердикт НЕТ. B1: Sound/Calendar `grab: false` без catcher (T332). Заливка — T329, не дубль. 30 кадров, panic/protocol/ERROR=0, 11 TOML sha без дрейфа | `done/qa/T325-popups-overlays-ux-audit.md`, отчёт в `reports-log/qa/` |
| T326 | **QA** | Левая панель | **ПРИНЯТ 2026-08-21.** Вердикт НЕТ. Fresh chat/IPC живые. Новый баг reselect проекта → T333. session/load = T281/T285, не дубль. 5 заглушек Slice B/C — TBD | `done/qa/T326-left-panel-ux-audit.md`, отчёт в `reports-log/qa/` |
| T327 | **QA** | Правая панель, 22 id, Gamer/Developer | **ПРИНЯТ 2026-08-21.** Вердикт ДА (врёт). 22 id, rails 11+11. Upgrade all врёт (T334), ACP wrap (T335), IPC spawn до resolve (T336) | `done/qa/T327-right-panel-modes-ux-audit.md`, отчёт в `reports-log/qa/` |
| T328 | **QA** | Рама, схемы, alpha/blur, обои | **ПРИНЯТ 2026-08-21.** Вердикт НЕТ. Рама ок. B1–B5 → T338–T342, F1 → T343. Стол не восстановлен | `done/qa/T328-frame-theme-wallpaper-ux-audit.md`, отчёт в `reports-log/qa/` |
| T329 | **FRONTEND** | Календарь: непрозрачная подложка | **ПРИНЯТ 2026-08-21.** Плита семьи Sound (`surface_color` 0.82 + border + elevation + blur-слой), диф — 1 файл, 610 тестов. Блокер отчёта («не открывается», «canvas глотает клики») опровергнут живьём: попап открывается, у исполнителя `ydotool --absolute` уезжал на второй монитор. Читаемость поверх яркого контента ждёт T341 (блюра нет) | `done/front/T329-calendar-popup-opaque-plate.md` |
| T330 | **BACKEND** | Listener `ActiveMonitorChanged` → refresh воркспейсов | **Очередь.** T324 B2 | `active/back/T330-workspace-active-monitor-refresh.md` |
| T331 | **FRONTEND** | Network влезает в HEIGHT_MIN 20 | **HOLD 2026-08-21.** Пол пересматривает T337; не втискивать сеть в 20, пока пол не сдан | `active/hold/T331-network-fits-bar-min-height.md` |
| T332 | **FRONTEND** | Sound/Calendar: click-away + singleton, `grab` остаётся false | **Очередь после T329.** T325 B1. Образец: `tray_menu` `open_for_popup` | `active/front/T332-anchored-popup-click-away.md` |
| T333 | **FRONTEND** | Reselect активного проекта — no-op, не чистить session | **Очередь.** T326 B2. Параллелен T329/T331 | `active/front/T333-active-project-reselect-noop.md` |
| T334 | **FRONTEND** | Upgrade all не обещает AUR (T294 pacman only) | **Очередь.** T327 B1. `updates.rs` | `active/front/T334-updates-upgrade-all-honest-label.md` |
| T335 | **FRONTEND** | Open agents.toml читается на 320 px | **Очередь.** T327 B2. `acp_settings.rs` | `active/front/T335-acp-settings-open-fits-320.md` |
| T336 | **FRONTEND** | select-tab вне mode set: resolve до ensure_tab_view | **Очередь.** T327 B3. `view.rs` | `active/front/T336-resolve-tab-before-ensure.md` |
| T337 | **FRONTEND** | `HEIGHT_MIN` бара = высота, на которой виджеты читаются | **Очередь.** Grim height=20. T331 HOLD | `active/front/T337-bar-height-min-readable.md` |
| T338 | **BACKEND** | Не сносить стол: не стартовать пустой awww поверх mpvpaper | **Очередь.** T328 B1 | `active/back/T338-wallpaper-do-not-stomp-desk.md` |
| T339 | **FRONTEND** | Next: видимый отказ, если в Wallpapers только видео | **Очередь.** T328 B2 | `active/front/T339-wallpaper-next-empty-feedback.md` |
| T340 | **FRONTEND** | Выбранный чип ≥ 4.5:1 во всех схемах | **ПРИНЯТ 2026-08-21.** Плита `interactive.active` + `text.primary` во всех четырёх chip-билдерах, Solarized `interactive.active` опущен на base01. Ворота `selected_chip_passes_wcag_aa_in_all_schemes` проверены СВОЕЙ мутацией (Mocha → 1.14:1, тест падает). Живьём Solarized читается. Числа контраста в отчёте все четыре неверны (реально 6.68/9.24/12.05/**4.89**), вердикт не меняется. Побочка на глифы левой панели → **T344** | `done/front/T340-scheme-selected-chip-contrast.md` |
| T341 | **FRONTEND** | Blur: установить `45-surface-effects-chronos.lua` | **Очередь.** T328 B4 | `active/front/T341-blur-module-install.md` |
| T342 | **FRONTEND** | `surface_alpha` на раму и Start, не только бар | **Очередь.** T328 B5 | `active/front/T342-surface-alpha-chrome.md` |
| T343 | **FRONTEND** | Граница бара в `normal` переживает height hot-reload | **Очередь.** T328 F1 | `active/front/T343-bar-border-survives-height-hot-reload.md` |
| T344 | **FRONTEND** | Левая панель: шеврон/точка/пустой чат — на `text.muted`, не на `interactive.active` | **Очередь.** Побочка T340; токен состояния красил глифы, на Solarized ≈1.2:1 | `active/front/T344-left-panel-glyphs-off-interactive-active.md` |
