# ChronOS kitchen

Кухня **репозитория ChronOS**. Не копия экосистемной.

- Эта: `ChronOS/.chronos-ops/`
- Экосистема (кросс-репо): `/home/neo/projects/chronos-ecosystem/.chronos-ops/`

Правила — `RULES.md`. Снимок очереди:

```
bash .chronos-ops/bin/kitchen-status.sh
```

Cutover из `docs/orchestration/tasks/` — отдельный заход, не пока 287B
в поле. Пока очередь живёт в `docs/orchestration/`.
