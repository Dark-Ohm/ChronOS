# T246 — убрать фейковый permission-мок с System-таба: отчёт

**Дата:** 2026-08-05
**Статус:** закрыт — вариант 1 (полное удаление), по выбору пользователя.
**Роль:** FRONTEND (Rust, GPUI), минимум кода.

## Решение — полное удаление (вариант 1)

Пользователь выбрал «Полностью удалить» (спрошен напрямую перед удалением
файла целиком, как требовал тикет). Удалено:

- `system.rs`: импорт `use …permission::render_permission_card;` (стр. 17)
  и рендер `.child(render_permission_card(cx))` с секционным комментарием
  (стр. 138–139); комментарии перенумерованы (1. Header, 2. Scrollable).
- `permission.rs` — **удалён целиком** (77 строк, мёртвый мок без
  `on_click` у Allow/Deny — только `cursor_pointer` + hover).
- `mod.rs`: убран `mod permission;`.
- `surfaces.rs`: doc-комментарий `(mpris, disks, wallpaper, permission)` →
  `(mpris, disks, wallpaper)`.

Сервисный слой `hermes_acp/transport.rs` уже обрабатывает permission на
уровне сервиса (auto-approve) — UI-мок ни с чем не был связан.

## Верификация (живая + статическая)

- **Код:** `render_permission_card` — 0 ссылок во всех `crates/` (символ
  не существует — рендер невозможен физически).
- **Тесты:** `cargo test --release -p chronos --lib -- side_panel_right`
  — **167/167 ok** (совпадает с ожидаемым числом тикета).
- **Сборка:** `cargo build --release -p chronos` — чисто.
- **Живой grim, обе темы** (smoke-открытие правой панели → дефолтный
  System-таб, grim после ≥5с settle): акцент-сканирование пикселей
  (`#007acc`, fuzz 2%) по всей панели —
  - **тёмная:** акцент только с y=273 (спектр-строки) и ниже (футер);
    в полосе бывшей карточки (y≈90..120) — **ноль**;
  - **светлая:** акцент только на y=0 (верхняя кромка панели); вся
    панель ниже — **ноль**, включая зону карточки.
  → кнопки Allow/Deny с акцентной рамкой отсутствуют в обеих темах.
  Панель рендерится нормально (Light C поверхности и текст
  подтверждены гистограммой).

Примечание: OCR-проверка текста недоступна на этой машине — tesseract
без `eng.traineddata` (только afr/osd), поэтому текстовая верификация
заменена структурной пиксельной.

## Триггер

T246 триггерит **T253** (пересъёмка `07-tab-system` + переоценка теста
первого кадра) — отдельный тикет, НЕ запускался в этой сессии. Репёр
теперь устарел: permission-карточки на System-табе больше нет.

## Коммит

`ui : remove fake permission mock from System tab (T246)`
+ `orchestration : T246 closed — report + ticket moved to done/`.
