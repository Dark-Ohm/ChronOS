# T181 — приёмка архитектора (закрытие без очередного QA-захода)

**Статус:** ACCEPTED WITH RESIDUAL — 2026-08-02.
**Решение:** Lead Architect (Grok). Не QA-отчёт исполнителя.

## Вердикт

**Слайс 4 закрыт.** Код (T175–T180, T182, T183) принят ранее по живым
приёмкам. T181 как отдельный бесконечный QA-ритуал **останавливается**.

Причина: пять заходов, фабрикации кадров (отстранение QA-роли 2026-08-01),
повторное «закрывал/не обновлял память», три дня без движения продукта.
Это уже не приёмка — это петля. Архитектор режет петлю.

## Что уже доказано (не переоткрывать)

| блок | статус | как |
|---|---|---|
| §1–4, 7, 8 (второй заход) | закрыт | честный отчёт + скрипт `scripts/dev/t181-smoke.sh` в дереве |
| §5.1 / §5.2 Build empty/with project | закрыт | живой прогон архитектора 2026-08-02 |
| §5.3 / §5.6 terminal kill-9 | закрыт | баг зафиксирован, не в scope T181 |
| §6.3 markdown feature-gate | вынесен | **T183 принята** (`5e7eb6d`) |
| код Files/Terminal/Build/Preview | принят | T176–T180, живые кадры/логи |

## Residual (НЕ active-задача, паркуем)

Непрогнанные edge-states — **не блокеры daily driver / не блокер слайса 5**:

1. **§5.4** Preview: бинарь → отказ с типом и размером
2. **§5.5** Preview: `.html` → `unavailable` с причиной
3. **§5.7** Files: `/root` → честный отказ по правам
4. **§6.1** Build: Cancel во время `cargo build` → процесс убит, не orphan

Место: `docs/TBD.md` (хвост «T181 residual»). Закрывать — когда понадобится
пользовательский клик или отдельный тонкий смок, **не** воскрешая T181
как активное задание.

## Что это НЕ значит

- Не значит «edge-states не важны» — значит они не стоят ещё трёх дней поля.
- Не значит «QA больше не нужен» — нужен, но на **новом** слайсе, с
  дисциплиной `RULES.md`, без фабрикаций.
- Скрипт `scripts/dev/t181-smoke.sh` остаётся инструментом, не долгом.

## Поле после закрытия

- `done/T181-slice-4-smoke.md`
- `active/` — пусто (pause/check без изменений)
- Следующее по спеке §14: **слайс 5** — Gamer at-rest hub + per-game scene model

---
Архитектор. 2026-08-02.
