# T181 — QA-смок слайса 4: рабочий стол разработчика

**Статус: ЗАКРЫТА 2026-08-02 — ACCEPTED WITH RESIDUAL** (решение архитектора).
Residual §5.4/5.5/5.7/§6.1 → `docs/TBD.md`. Отчёт:
`docs/orchestration/tasks/report-log/T181-slice-4-smoke-report.md`.
Не переоткрывать. **Роль была:** QA.

**Закрыто и не переоткрывать:** §1–4/7/8 (второй заход, честный отчёт),
§5.3/§5.6 (терминал после `kill -9` не рестартует шелл — баг зафиксирован,
в задачу не превращать), §5.1/§5.2 (закрыты 2026-08-02 живым прогоном
архитектора лично — см. `docs/orchestration/tasks/rejected/
T181-slice-4-smoke-report-fabricated.md` и `-fabricated-2.md` за
дисциплинарной историей, не за фактами), §6.3 (баг найден, вынесен в
отдельную задачу T183, не сюда).

**Дисциплина роли:** предыдущий исполнитель этой роли отстранён 2026-08-01
за две фабрикации улик подряд (`docs/orchestration/agents/QA.md`, разбор в
`docs/ARCHITECT.md`). Если тебя запустили под тем же именем/сессией —
останови и сообщи архитектору, не бери задание.

---

## Осталось закрыть

- **§5.4 / §5.5 Preview: бинарь / .html.** Открой Files, сними кадр,
  **прочитай координаты нужной строки с этого кадра** (не бери из таблицы
  ниже как готовые — они float при навигации, таблица только ориентир для
  рейла и кнопки раскрытия). Клик по строке, кадр результата: бинарь
  (`target/release/chronos`) — отказ с типом и размером; `.html`
  (создай мелкий `test.html`, если рядом нет готового) — `unavailable`
  с причиной.
- **§5.7 Files: `/root`.** Та же техника — кадр → координаты с кадра →
  навигация/ввод пути → кадр честного отказа с правами.
- **§6.1 Отмена задачи в Build.** Запусти реальный `cargo build` через UI
  на достаточно медленном проекте, чтобы успеть кликнуть Cancel; кадр с
  кнопкой, координаты с кадра, клик, `pgrep cargo`/`pgrep rustc` до и
  после — подтверди, что процесс умер, не осиротел. (В T178 `ydotool` не
  попал в кнопку — в этот раз координаты брать со свежего кадра, не из
  памяти прошлой попытки.)

**Что НЕ делать:** не трогай §1–4/7/8/5.1/5.2/5.3/5.6 — закрыты. Не лезь
в T183 (markdown feature-gate) — отдельная задача, отдельная роль.

## Как отчитываться

- каждый пункт — **PASS / FAIL / НЕ ПРОВЕРЕНО с причиной**;
- «не проверено» с причиной — законный ответ, засчитывается; выдуманное
  «работает» — нет;
- если инструмент (`ydotool`) не попал в цель — пиши «не смог проверить
  инструментом», а не «функция сломана»;
- кадры открывать глазами и подписывать тем, что на них видно, не тем,
  что ожидалось увидеть;
- лог целиком проверить на `grep -n "panicked at"`.

**Отчёт:** `docs/orchestration/tasks/report/T181-slice-4-smoke-report.md`.
Только эти четыре пункта — не переписывай закрытые разделы.

---

## Механика панели (без этого кадры выйдут пустыми)

**Панель раскрывается в два приёма.** Клик по иконке в рейле только
*выбирает* вкладку — панель остаётся шириной 54 px. Контент раскрывает
кнопка `⊞`/`⊟` **в самом низу рейла**. Признак в логе: `apply per-tab
width … content_open=false` — значит виден только рейл. На этом уже
потерялись двое до тебя.

Координаты на пульте (DP-1, 2560×1440) — ориентир, не факт, пересчитывать
по свежему кадру:

| элемент | реальные | для `ydotool` (половина) |
|---|---|---|
| рейл, колонка иконок | `x ≈ 2537` | `x = 1268` |
| первая иконка (System) | `y ≈ 55` | `y = 27` |
| шаг между иконками | `40` | `20` |
| Files (2-я) | `y ≈ 95` | `y = 47` |
| Terminal (4-я) | `y ≈ 175` | `y = 87` |
| Preview (5-я) | `y ≈ 215` | `y = 107` |
| кнопка раскрытия контента | `x ≈ 2539, y ≈ 1414` | `1269 707` |

`ydotool` берёт **половинные** координаты (подтверждено многократно на
живых смоках), сокет `YDOTOOL_SOCKET=/run/user/1000/.ydotool_socket`.

IPC без рестартов, сокет `/run/user/1000/chronos.sock`, `socat` в системе
нет — шли так:

```python
import socket, sys
s = socket.socket(socket.AF_UNIX, socket.SOCK_STREAM)
s.connect("/run/user/1000/chronos.sock")
s.sendall(sys.argv[1].encode()); s.shutdown(socket.SHUT_WR); s.close()
```

Команды: `set-workspace-mode:developer`, `set-workspace-mode:gamer`,
`toggle-workspace-mode`, `toggle-side-panel-right`.

Запуск с чистым логом (не грепай `~/.local/state/chronos/chronos.log` от
старого прогона):

```bash
pkill -9 -x chronos; rm -f /run/user/1000/chronos.sock; sleep 1
RUST_LOG=info nohup ./target/release/chronos > /tmp/t181/run.log 2>&1 &
```

## Верификация

```
cargo test -p chronos
cargo test -p chronos-services
cargo clippy -p chronos --all-targets
cargo build --release -p chronos
```

**Гонять релиз, а не dev.** «Компилируется и тесты зелёные» для оконного
кода не доказывает ничего.

## Коммит

Кода ты не пишешь. Если правишь только отчёт — коммит не нужен, сделает
Архитектор при приёмке.

---

Закрывающая задача слайса 4. План —
`docs/superpowers/plans/2026-07-31-developer-workbench-slice-4.md`, §5 и §7.
Спека — `docs/superpowers/specs/2026-07-30-adaptive-developer-gamer-shell-ide-design.md`,
§13, §41, §532.
