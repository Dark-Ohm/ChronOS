# T253 — пересъёмка System-таба после T246, переоценка теста первого кадра

**Приоритет:** P2 — верификационная задача, зависит от T246 (и
желательно T248).
**Роль:** QA/vision-критик, живой замер.
**Источник:** `docs/orchestration/tasks/report-log/T223-design-audit-report-v4-reshoot.md`
топ-10 п.9.

## Контекст

Текущий кадр `07-tab-system-dark.png`/`07-tab-system-light.png` не
проходит тест первого кадра ИЗ-ЗА permission-мока (T246) и отчасти
из-за пустой mpris-карточки (T248). После обоих фиксов System-таб
теоретически становится годным первым экраном (CPU/RAM/GPU виджеты +
диски — аудит предполагает "вероятно да", но это гипотеза, не
проверено).

## Что нужно

**Зависимости сняты (2026-08-13): T246 и T248 слиты и приняты** — проверено
при аудите T252 (mpris схлопывается до строки «No player», permission-мока в
дереве нет). Тикет разблокирован, ждал только исполнителя.

1. `select-tab:system` (IPC), `grim` кроп по геометрии `hyprctl layers`
   (не полноэкранный снимок — дисциплина из T233-reshoot захода 2,
   исключает контаминацию посторонними окнами), обе темы.
2. Сравнить с прежним `07-tab-system-*` — permission-мок и (если T248
   тоже слит) раздутая mpris-карточка отсутствуют.
3. Прогнать "тест первого кадра" (см. методологию в
   `docs/orchestration/tasks/report-log/T223-design-audit-report-v4-reshoot.md`
   §2) заново на новом кадре — годится ли он теперь как "первый пост на
   r/unixporn" самостоятельно, без остальных 27 кадров волны.

## Зона файлов

Нет кода — только живая съёмка + отчёт. Не создаёт конфликтов зон.

## Верификация

Новый кадр `07-tab-system-*` (обе темы) сохранён в
`docs/orchestration/tasks/notes/T253-*`, короткий вердикт записан в
отчёт (годится / всё ещё есть проблема — какая).

## Коммит

Нет кода — только отчёт + кадры (если кадры коммитятся в репо, не
`/tmp` — сверить с практикой T239, где кадры легли в
`docs/orchestration/tasks/notes/`).

## Статус (2026-08-05) — частично выполнено, НЕ закрыт

Пересъёмка сделана (`T253-T254-live-pc-use-report.md`), хэши сверены
живьём архитектором — permission-мок и раздутая mpris-карточка
подтверждённо отсутствуют на новых кадрах. Но:

1. **Кадры не перенесены** в `docs/orchestration/tasks/notes/` — лежат в
   `/tmp/t253-live/`, будут потеряны при очистке. Не закрывать, пока не
   перенесены.
2. **Новая находка при живом просмотре кадров архитектором** (не
   отчётом-исполнителем — сам отчёт это пропустил): заголовок панели
   вверху обоих кадров статично показывает `kitty` с крестиком —
   `header.rs:8`, `const WINDOW_TITLE: &str = "kitty"`, хардкод,
   никогда не отражает реальное активное окно. Тот же класс обмана, что
   был у permission-мока. Заведён **T256** — тест первого кадра для
   System-таба **не проходит**, пока T256 не решён.
3. Холодная (не архитектор) vision-приёмка так и не выполнена — отчёт
   сам это признаёт.

Держать активным до: (а) переноса кадров в notes/, (б) резолюции T256,
(в) финального вердикта теста первого кадра на исправленном кадре.
