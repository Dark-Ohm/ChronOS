# T164 — scene-модель: состояние, персист, восстановление

**Статус:** active. **Роль:** BACKEND. Общие правила —
`docs/orchestration/agents/RULES.md`.

Открывает слайс 2 спеки Shell-IDE. План слайса —
`docs/superpowers/plans/2026-07-31-shell-composition-slice-2.md`, прочитать
целиком: там формат файла, объём (вариант B) и то, что осознанно отложено.
Спека — `docs/superpowers/specs/2026-07-30-adaptive-developer-gamer-shell-ide-design.md`,
твои разделы §3.3, §3.6, §5, §11.

**Зона (твоя, никто больше её не трогает):**
- `crates/app/src/scene.rs` — новый файл, весь твой
- `crates/app/src/main.rs` — только регистрация модуля и вызов `scene::init`
- `crates/app/src/workspace_mode.rs` — только точка вызова восстановления

**НЕ трогать:** `side_panel_right/**`, `dock/**`, `bar/**` (это T165),
`monitor.rs` и `side_panel_left/**` (это T166).

**Отчёт:** `docs/orchestration/tasks/report/T164-scene-state-model-report.md`.

---

## Что делаем

Общее состояние сцены **вне панельных вью** (§11 спеки: «add shared
workspace-mode and scene state outside individual panel views»). Сегодня
композицию шелла не задаёт никто: каждая панель решает за себя. Этот слой —
единственный источник правды о том, «какая сцена активна».

**Образец для подражания — `crates/app/src/workspace_mode.rs` целиком.** Он
принят вчера (T160), написан по тем же правилам и решает ту же задачу
классом ниже: глобал + конфиг + чистые функции. Повторяй его структуру, не
изобретай свою:

- `#[derive(Debug, Clone, Default)] pub struct SceneState` + `impl Global`
- `config_path()` → `~/.config/chronos/scenes.toml`
- `load_config()` — битый файл даёт `warn!` и дефолт, **не панику и не
  перезапись** (`workspace_mode.rs:103`)
- `save_config()` — `create_dir_all`, `toml::to_string_pretty`, ошибки в
  `warn!` (`workspace_mode.rs:127`)
- чистые функции резолвинга, тестируемые без GPUI и файловой системы
  (`resolve_initial`, `should_prompt` — образцы на `workspace_mode.rs:148,196`)
- `init(cx)` ставит глобал, логирует итог через `tracing::info!`

## Формат файла

Точный формат — в плане слайса, раздел «Формат `~/.config/chronos/scenes.toml`».
Скопируй его в докблок модуля, как это сделано в `workspace_mode.rs:1-14`.

**Три вещи, которые обязаны быть правильными с первого коммита:**

1. **`version = 1` в файле.** Не «на будущее» — вариант C (внешние окна)
   утверждён как следующая итерация, файл будет расти. Отсутствие поля
   трактуется как `version = 1`.
2. **Неизвестные секции переживаются, а не затираются.** Парсер обязан не
   падать на `[scene.windows]` (резерв под C) и на любых незнакомых полях.
   **Тест на это обязателен.** Причина: T163 (вчера) стоил двух эррат
   ровно потому, что формат конфига не был рассчитан на рост.
3. **`display` — UUID строкой, никогда не индекс и не `DisplayId`** (§3.6).
   В этом слайсе поле только парсится и сериализуется; **резолвить его в
   реальный вывод НЕ надо** — этого в варианте B нет, и `monitor.rs` в твоей
   зоне отсутствует. Не выдумывай зависимость от него.

## API, на который сядет T165

T165 (композиция рейла и дока) стартует сразу после твоей приёмки и садится
на этот API. Он должен существовать и быть покрыт тестами:

```rust
pub fn current(cx: &App) -> Option<&Scene>;          // активная сцена, если есть
pub fn rail_tabs_override(cx: &App) -> Option<&[String]>;
pub fn dock_override(cx: &App) -> Option<&[String]>;
pub fn active_tab_override(cx: &App) -> Option<&str>;
```

Возвращают `None`, когда сцены нет или поле не задано — это штатный случай,
означающий «бери дефолт режима», а не ошибку. Сигнатуры можно уточнить
(например, отдавать owned `Vec<String>`), если так чище ложится на GPUI —
но семантика «`None` = дефолт режима» неизменна.

## Восстановление последней сцены

§5 спеки: смена режима **может** восстановить последнюю сцену этого режима.
Секция `[last]` в файле хранит `id` последней сцены по каждому режиму.

- при `workspace_mode::set` активная сцена меняется на `[last][новый режим]`,
  если такая сцена есть в файле; если нет — сцена становится `None`
  (композиция = чистый дефолт режима), и это **не ошибка**
- `[last]` обновляется при смене активной сцены и персистится

**Красная линия:** это НЕ автопереключение режима. Ты не добавляешь ни
одного нового вызова `workspace_mode::set`/`toggle`/`request_switch` —
наоборот, `set` зовёт тебя. Контракт слайса 1 («режим не переключается сам»)
проверяется грепом на приёмке, как в T162. Нарушишь — задача отклоняется
целиком, без разговора о качестве остального.

## Тесты

Обязательный минимум, все — на чистых функциях, без GPUI:

1. пустой/отсутствующий файл → дефолт, без паники
2. мусор вместо TOML → `warn` + дефолт, **файл не перезаписан**
3. неизвестная секция `[scene.windows]` и незнакомое поле → парсится, не
   теряет остальное
4. `[last]` резолвится в существующую сцену; ссылка на несуществующий `id`
   → `None`, не паника
5. неизвестный `mode` в сцене → сцена игнорируется с `warn`, остальные живы
6. round-trip: сериализация → парс → те же данные

## Верификация (в отчёт — командами и выводом, не пересказом)

```
cargo test -p chronos                 # весь бинарь, не только твой модуль
cargo clippy -p chronos --all-targets
rg -n "workspace_mode::(set|toggle|request_switch)" --type rust crates/
rg -n "cx.primary_display" --type rust crates/app/src/scene.rs
```

Последние два — доказательство, что ты не нарушил контракт слайса 1 и не
полез в чужую зону. Вывод грепа приложи целиком, даже если он пустой.

Живой прогон в этой задаче **не требуется** — UI ты не трогаешь. Но если
шелл после твоих правок не стартует, это выяснится на приёмке и вернётся
тебе. Проверь, что `cargo build --release -p chronos` проходит.

## Коммит

Ветка от актуального `master`. Сообщение: `scene : модель сцены, персист
scenes.toml и восстановление последней сцены режима (T164)`. Без
AI-трейлеров. **Перед коммитом `git diff --staged` глазами и поимённый
`git add`** — несамодостаточные коммиты shared-файлов у нас эпидемия.
**Работу коммитишь ты, а не архитектор** — в T161 и T163 отчёт приходил при
незакоммиченном дереве, это считается дефектом оформления.
