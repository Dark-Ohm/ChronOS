# FRONTEND — точка входа роли

**Роль:** всё, что видно глазами. GPUI-разметка, состояние виджетов,
взаимодействие, анимация, тема.

**Зона:** `crates/app/**` (панели, бар, композер, транскрипт, попапы),
`crates/ui/**`. Сервисный слой и ACP — не твоё (это BACKEND).

**Общие правила:** `docs/orchestration/agents/RULES.md` — прочитать перед стартом.

**Активные задания — волна 2 слайса 4, идут параллельно.** План слайса —
`docs/superpowers/plans/2026-07-31-developer-workbench-slice-4.md`.

- ~~T178 (Build/Logs)~~ — **принята**, `done/` + `report-log/`. Одна эррата,
  исправил сам: `has_cargo_toml` в `tasks/project.rs` была мёртвой копией
  детекта — и **покрыта собственным тестом**, из-за чего зелёное покрытие
  маскировало недостижимый код. Проверяй не «есть ли тест», а «зовёт ли
  кто-то функцию из продукта».
- ~~T179 (Preview)~~ — **принята**, `done/` + `report-log/`. Живой прогон
  делал я: исполнитель честно написал, что кадров вкладки не открывал, и не
  стал выдавать снятое за доказательство. Так и надо.
- `docs/orchestration/tasks/active/T180-markdown-preview-goes-online.md` —
  **новая, из приёмки T179.** Предпросмотр локального markdown делает
  исходящие HTTP-запросы (бейджи `img.shields.io` из README тянутся
  по-настоящему). Просмотр файла на своём диске не должен ходить в сеть.

**Волны 1 и 2 слайса 4 закрыты:** T176 (Files), T177 (Terminal),
T178 (Build/Logs), T179 (Preview). Осталась T180 и QA-смок слайса.

**Как раскрыть контент панели в живом прогоне** (я потерял на этом десять
минут): клик по иконке в рейле только *выбирает* вкладку, панель остаётся
шириной 54 px. Контент раскрывает кнопка `⊟` **в самом низу рейла**
(`x ≈ 2539`, `y ≈ 1414`; `ydotool` — половинные: `1269 707`). Признак в
логе: `apply per-tab width … content_open=false` — значит видно только
рейл, и любые твои кадры «вкладки» будут пустыми.

**Два образца, оба в `done/` + `report-log/`:** T176 (Files) — сервисный
слой отдельно, вьюха по образцу `SystemTab`, тяжёлое в `background_spawn`;
T177 (Terminal) — вынос движка в `crates/services/src/terminal/` без единого
GPUI-типа, размер грида считается от фактической ширины (`compute_grid`,
80 колонок = 624 px при `cell_w = 7.8`).

**Дисциплина, урок T177 — читать `RULES.md`, раздел «Границы исполнителя».**
Отчёт кладётся в `report/` — и всё. Не двигать задание в `done/`, не класть
отчёт в `report-log/`, не писать коммит «принята». В T177 это было сделано
за 31 секунду после кодового коммита; приёмки не было, и вскрылось оно
случайно. Код оказался хороший — но это везение, а не процесс.

**Второй урок T177: не занижай свою работу.** §4 отчёта объявил клавиатуру
неработающей и повесил диагноз на композитор. Клавиатура работает, включая
`Enter`; ввод не доставил `ydotool` исполнителя. Инструмент не смог — так и
пиши: «не смог проверить инструментом», а не «функция сломана».

**Урок T176, стоил мне получаса разбирательства:** если `cargo` вдруг не
видит модуль, который точно есть в дереве и объявлен в `lib.rs`, — сначала
`cargo clean -p <крейт>`. Общий `CARGO_TARGET_DIR` между основным деревом и
worktree загрязняет артефакты, и ошибка выглядит как несамодостаточный
коммит, хотя коммит в порядке.

T171 закрыта — принята с первого захода (`4edd8cf`). Ширина панели стала
свойством вкладки; живой прогон закрыл архитектор — System 400, Editor 560,
сквозная полоса слева ушла.

T172 закрыта — принята с первого захода (`95c1da1`). `destination-out`
вычищен из дерева целиком; исполнитель перерисовал больше, чем я просил, и
поймал ошибку в моём рецепте проверки (см. блок про иконки ниже).

T169 закрыта — принята со второго захода (`d3326bc` + `99813b4`). Эррата была
одна: `mix-blend-mode` в иконке. Во втором заходе исправлено шире задания —
`source-control` по своей инициативе разведена с соседними «узловыми».

T170 закрыта — принята с первого захода (`be6dcee`).

**Иконка живёт в ДВУХ местах.** Файл в `crates/app/assets/icons/` — это
половина дела; вторая — строка в списке `icons!(...)` в
`crates/app/src/assets.rs`, откуда `include_bytes!` встраивает её в бинарь.
Забыл вторую — вкладка рисуется пустой клеткой, и **ни один тест этого не
поймает** (тесты проверяют пути, а не наличие байтов). Стоило эрраты в
T169 — и виноват был архитектор, не исполнитель: `assets.rs` не был в зоне.

**Про иконки — кровное, стоило одной эрраты:** наш рендерер (`usvg`)
**не поддерживает** `mix-blend-mode: destination-out`. Помеченный элемент не
вычитается из подложки, а заливается поверх неё — иконка становится сплошным
пятном. В дереве от этого молча ломались `rail-terminal` и `rail-binds`
(T172).

Проверять новую иконку можно **без живого шелла**, но с одной поправкой:
**ImageMagick `mix-blend-mode` умеет, а `usvg` — нет**, поэтому обычный
рендер покажет иконку целой и обманет. Резать свойство надо руками:

```
sed -e 's/currentColor/#dfe4f5/' -e 's/ style="mix-blend-mode:[a-z-]*"//g' \
    икона.svg > /tmp/c.svg
magick -background '#1a1a24' /tmp/c.svg -resize 110x110 -flatten /tmp/r.png
```

и открыть `/tmp/r.png` глазами — это и есть то, что нарисует шелл. Дырки
делать через `fill-rule="evenodd"` внутри одного `<path>`, обводкой
(`fill="none" stroke="currentColor"`) либо непересекающимися примитивами.

**Три урока с T168, читать перед стартом:**
1. Тест, воспроизводящий логику продукта внутри себя (свой `HashMap` вместо
   настоящего поля), — не тест. `#[gpui::test]` + `TestAppContext` в форке
   **есть**: `Source/gpui/src/test.rs`, используется в самом gpui.
2. Кадр снят — значит **открыт**. Не «проанализирован по пикселям».
   Не открыл — пиши «не проверено», это засчитывается.
3. Прежде чем записать падение в «баг форка», прочитай строку **выше** в
   логе. Паника в wayland-диспетче почти всегда второй кадр каскада.

**Последнее закрытое:** T165 — композиция рейла и дока по режиму, **принята
2026-07-31 без эррат, с первого захода**. Коммит `f2a2997`, задание в
`done/`, отчёт в `report-log/`. Что сделано образцово: `ALL` сохранён как
полный каталог (тесты покрытия иконок и подписей не деградировали), написан
тест на стабильность относительного порядка общих вкладок между режимами
(§5 в машинной форме), выход за зону в `bar/widgets/dock.rs` заявлен прямо,
а не спрятан. **И главное — исполнитель поправил архитектора:** ориентир в
брифе про Gamer противоречил спеке (строка 149, «keeps the settings group
intact»), он пошёл по спеке и процитировал источник. **Правило: спека выше
ориентира в задании; расхождение — в отчёт с цитатой, а не молча по букве
брифа.**

Слайс 1 закрыт целиком. T164 (scene) принята. T166 идёт параллельно.

Закрыты и в `done/` — не брать: T152 (иврит/RTL), T154 (поле ввода
композера), T155 (поглощена), T156 (cfg-гейты), T157 (замер: +1.96 MiB за
`Input+Table+VirtualList`), T158 (усыновление компонента; премисса обрезки
опровергнута — 128 байт).

Полезные скиллы: `chronos-shell`, `gpui-rsx`, `gpui-layer-shell`,
`chronos-gpui-popup`, `chronos-gpui`.

**Главное для этой роли:** «компилируется» не доказывает ничего. Доказывает
релизный бинарь и кадр `grim`.
