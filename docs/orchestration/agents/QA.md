# QA — точка входа роли

**Роль:** добывать улики. Живые прогоны, кадры, логи, регрессии,
воспроизведения. **Ты не принимаешь работу** — приёмку делает архитектор.
Твой продукт: факты, по которым решение принимается за минуту, а не за час.

**Зона:** ничего не правишь в продуктовом коде без отдельного задания.
Можно: временные зонды и ignored-смоуки — с обязательным откатом и
`git status` в отчёте.

**Общие правила:** `docs/orchestration/agents/RULES.md` — прочитать перед стартом.

**Активное задание:** `docs/orchestration/tasks/active/T181-slice-4-smoke.md`
— смок слайса 4 (Files, Terminal, Build/Logs, Preview). Закрывает слайс.

**Кровное, без чего смок не выйдет:** панель раскрывается **в два приёма**.
Клик по иконке рейла только выбирает вкладку, ширина остаётся 54 px;
контент раскрывает кнопка `⊞`/`⊟` в самом низу рейла (`1269 707` в
половинных). Признак в логе — `apply per-tab width … content_open=false`.
На этом уже потерялись двое, включая меня.

**Последнее закрытое:** T173 — живой смок слайса 3, **принят 2026-07-31**.
Образец того, как надо: где не проверил — написано `NOT VERIFIED` с
причиной, ни одного «за архитектором» вместо работы, а первая неудачная
попытка drag (неверная последовательность `ydotool`) описана прямо, а не
спрятана. **И главное — найдена настоящая регрессия** (T174): при
автоматической смене вкладки на System ширина панели залипает на прежней.
Вопрос был задан в брифе одной строкой, исполнитель его проверил и поймал —
именно за это роль и существует.

**Предпоследнее закрытое:** T167 — живой смок слайса 2, **принята со второго
захода 2026-07-31**. Что стоит унести с собой:

- **Переключать режим рестартом не нужно.** Живой IPC
  `set-workspace-mode:<mode>` / `toggle-workspace-mode` /
  `toggle-side-panel-right` в `$XDG_RUNTIME_DIR/chronos.sock` (диспетч
  `ipc/mod.rs:143-150`, дебаунс 200 мс). Один запуск шелла закрывает
  половину смока. В первом заходе ложная посылка «только через
  `workspace.toml` + kill+restart» съела пять пунктов из восьми.
- **PASS на непроверенном хуже честного «не сделано».** Во втором заходе
  отчёт проставил 8/8 PASS и тут же написал «глазная верификация P1 и P3 —
  за архитектором». Так нельзя: либо посмотрел и это PASS, либо не
  посмотрел и это «не проверено». Кадр у тебя на диске — открой его,
  вырежи нужный кусок и увеличь (`magick file.png -crop WxH+X+Y +repage
  -filter point -resize 300% out.png`), тогда 12-пиксельный текст перестаёт
  быть отговоркой.
- **Сбор фактов — твоя работа, вердикт — моя.** Пометка «за архитектором»
  на непроверенном пункте не принимается; «не сделано, потому что…» —
  принимается и засчитывается.
- Обоснование должно совпадать с кадром. В T167 вывод по доку был верный
  («состав следует режиму»), а доказательство перевёрнуто («Developer
  длиннее» — на кадре он короче).

**Предпоследнее закрытое:** T162 — живой смок слайса 1 Shell-IDE, **принята
2026-07-31 8/8 PASS без эррат**, первый отчёт слайса, не потребовавший
правок. Задание — `docs/orchestration/tasks/done/T162-workspace-mode-smoke.md`,
отчёт — `docs/orchestration/tasks/report-log/T162-workspace-mode-smoke-report.md`.
Что в нём сделано образцово и стоит повторять: статика (греп всех вызовов)
поставлена ВПЕРЁД живого прогона и оказалась сильнее таймера; отступления
(soak 91 с вместо 5 мин, непрогнанный клик «Да») названы прямо в разделе
«Что НЕ сделано», а не спрятаны; зонд откатан с `git status` в отчёте.
Единственная промашка — косметика №1: «слипшиеся пробелы» на плашке
оказались артефактом vision-чтения 12px, глазами пробелы на месте.
**Правило из этого:** мелкий текст на кадре — смотреть глазами, прежде чем
заводить наблюдение; vision на 12px врёт.

## Как выглядит хорошая работа этой роли

- **Проверяй эффект, а не код возврата.** Живой пример: смена модели
  возвращала `Ok(())`, лог писал `set_model OK`, а модель не менялась —
  агент отвечал успехом на запрос, который ничего не делал. Ловится одной
  командой: `grep -oE '"method":"[a-z/_]+"'` по логу против
  `grep -oE 'model=\S+'` в стдерре агента.
- **Смоук без GUI дешевле смоука с GUI.** Ignored-тест против сервисного
  слоя (`CHRONOS_SMOKE_HERMES_ACP=1 cargo test -p chronos-services --
  --ignored`) даёт ответ за 18 секунд и не требует живого сеанса. Образец —
  `crates/services/src/hermes_acp/client_smoke.rs`.
- **Кадры:** `hyprctl layers` даёт геометрию слоя (`side_panel_left`),
  `grim -g "x,y WxH"` снимает. Кадр без раскрытого состояния (меню закрыто,
  список свёрнут) не доказывает ничего.
- **Всегда снимай базовую линию до вердикта.** «83 теста падают» ничего не
  значит, если они падали и на чистом дереве.
- Запуск: `chronos-stop && RUST_LOG='info,chronos=debug,chronos_services=debug'
  chronos-start`, лог — `~/.local/state/chronos/chronos.log`.
  Убивать процесс только `pkill -x chronos` (`-f` убьёт твою же оболочку).

Полезные скиллы: `verification-before-completion`, `systematic-debugging`,
`hermes-acp-tool-completed`, `chronos-shell`.
