# BACKEND — точка входа роли

**Роль:** сервисы, протоколы, данные. D-Bus, Wayland/Hyprland IPC, ACP,
хранилища, фоновые задачи.

**Зона:** `crates/services/**`, `crates/luau/**`, `crates/plugins/**`.
UI не твой (это FRONTEND).

**Общие правила:** `docs/orchestration/agents/RULES.md` — прочитать перед стартом.

**T199** `31ec352` + **T201** `51219ab` ACCEPTED — schema + agent_api + skill
`skills/chronos-bar-config/`.

**Активных BACKEND bar-задач нет.** Дальше T203 dogfood — FRONTEND/skill.

T190/T185/T187 closed. T189 Scenes — pause.

Закрыта: T166 (единственный резолвер пультового вывода + вотчер hotplug) —
принята 2026-07-31 с эрратой в два захода. Цель §3.6 достигнута: единственный
вызов `cx.primary_display()` во всём `crates/` — внутри `pult_display_info`.
**Урок, которого раньше не было в этом файле:** `pub fn` в модуле,
экспортированном из `lib.rs`, под dead-code-предупреждение **не попадает** —
в lib+bin-крейте компилятор молчит про функцию с нулём вызовов. Первый заход
именно так и провалился: фолбэк был написан, задокументирован и не подключён,
а сборка была зелёной. Проверка «функция реально зовётся» — грепом, не
доверием к компилятору. Второй урок, оформительский: «коммитит архитектор»
при прямом «Коммитишь ты» в брифе — не отступление, а невыполненный пункт;
это уже четвёртый случай в проекте.

Закрыта: T164 (scene-модель) — принята 2026-07-31 с эрратой в два захода.
Блокер был деструктивный: `restore_for_mode` безусловно писал отфильтрованный
конфиг, стирая сцены с опечаткой в `mode` из живого файла пользователя. Урок:
**функция с именем `restore_*` не имеет права писать на диск** — читающий путь
не мутирует источник. Второй урок: тест, воспроизводящий логику продукта
внутри себя (`if version == 0 { version = 1 }`), не тест, а копия. Эррата
закрыта чисто, все три пункта. Коммиты `5543a4a` + `8d82a12`.

Закрыта: T160 (workspace-mode — состояние, персистентность, IPC). Принята с
эрратой: **ветка диспетча в `ipc/service.rs::accept_loop` не была написана** —
канал проложен, арм ждёт, отправлять некому. Компилятор писал `unused imports`
на именах, которые задача сама и добавила; отчёт списал их на ствол. Урок:
предупреждение об unused на своём же новом имени — не шум, а отчёт компилятора
о недоделанной работе.

Закрыта и в `done/` — не брать: T150 (SQLite-хранилище тредов +
`session/list`/`session/load`).

Полезные скиллы: `chronos-shell` (+ `references/slow-service-dispatch.md`),
`hermes-acp-tool-completed`, `tokio-coop-budget-on-main-thread`,
`rust-skills-master`.

**Кровные правила рантайма:** tokio — только для IPC/D-Bus/ACP; тики и UI —
GPUI-исполнитель. Ни одного tokio-примитива на главном потоке (см. скилл про
coop-бюджет: это стоило проекту дня диагностики).
