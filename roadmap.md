# ChronOS — Roadmap

Форвард-лук, одностраничный. Не дублирует детали — за рационализацией
решений см. `DECISIONS.log`, за канонической архитектурой — `ARCHITECTURE.md`,
за оперативным полем (кто чем занят прямо сейчас) — `HANDOFF.md`. Этот файл
отвечает на один вопрос: «куда мы идём и в каком порядке».

Обновлено: 2026-07-24 (три новых фронта заведены, детали — `HANDOFF.md`).

## Волна «Shell-IDE + dev hot-reload + bar layout» — В ПОЛЕ (2026-07-24)

Настоящая конечная цель проекта уточнилась: ChronOS — shell-IDE гибрид
(агент слева, IDE-панель на 10 вкладок справа), не только desktop shell.
Полный разбор решений — `DECISIONS.log` 2026-07-24 (три записи).

- [ ] **Shell-IDE правая панель** — фундамент (rail + System-таб +
      9 заглушек) роздан T112 (DeepSeek). 9 реальных вкладок — впереди,
      4 из них (MCP/LSP/API-providers/Hyprland-binds) требуют новых
      backend-сервисов с нуля (в дереве их сегодня нет).
- [ ] **Dev hot-reload bake-off** — спайк T110 (OpenCode, Track A
      `hot-lib-reloader`) / T111 (GLM, Track B `subsecond`), полигон
      `network.rs`. Победитель становится dev-инструментом Архитектора,
      не продуктовой фичей.
- [ ] **Bar widget layout config** — спека готова
      (`docs/superpowers/specs/2026-07-24-bar-widget-layout-config-design.md`),
      план/T-задача ещё не роздана. Выносит хардкоженный порядок
      `register_builtin` в `bar.toml` + hot-reload, используя уже
      существующую `BarSection` lane-модель (не с нуля).

## Волна «Top Bar редизайн» — ЗАКРЫТА ПОЛНОСТЬЮ

- [x] A — Cava — `c519e2e`+`eb043fd`. B — workspace-точки `8457bbc`.
      C — dock+Пуск `07df942`. E — история уведомлений `f4ddd72`.
      System popup (Zed) `f7de445`. Consolidation на пультовый
      монитор (Mimo №10) `0a99a67`. MPRIS multi-player (Grok №14)
      `a3d36ba`.
- [x] Токен-фундамент (Hermes №15) `3e04264` — Catppuccin status,
      font_mono, высота 30, bg.tertiary.
- [x] Лэйаут по мокапу `c7ccc02` + SVG-иконки/AssetSource `f370618`
      (Архитектор сам, разовый мандат 2026-07-20).
- [x] D — project switcher `6061736` (Архитектор сам, №9 у Mimo снят).
- [x] **Клик-конфирм пользователя** — пилюля → попап → портал-пикер,
      проект добавлен, переключение живое. №9 принят полностью.

## Волна «добивка бара» — ЗАКРЫТА (2026-07-20)

Свип попапов на STYLE.md (Grok №15 `1d736da`), battery/mpris SVG +
hover-свип + CAVA (Mimo №11 `6723493`), светлая тема Light C + hot-reload
(GLM №1/№2 `0f0ee88`/`5bb6c77`), network — лампочка+спидометр (DeepSeek
№1 `0838446`), трей-частокол (Hermes №16 `7eada8b`), upgrade-фидбек
(Mimo №12/эррата `79c8baa`+`b25452c`), светлая на попапах/лаунчере
(Grok №16 `3f6e165`), воркспейс-точки динамические (Архитектор `608b584`),
контраст поверх заливок + Latte-статусы (Архитектор `009853f`).

## Волна «разведка форка + сторонних крейтов» — ЗАКРЫТА (2026-07-20)

Скилл `skills/chronos-gpui/` собран (4/4 зоны: Grok/Hermes/OpenCode/
DeepSeek). Ложный кровный факт про скролл опровергнут и почищен в 6
местах. Разведаны компиляцией: `gpui-form` (готов, Cline), `gpui-rsx`
(готов, дисциплина individual-attributes, Cline), `gpui-animation`
(требует форка библиотеки, Grok). Прочитаны без компиляции и записаны
кандидатами: `ccf-gpui-widgets` (текстовый ввод — сильный кандидат),
`gpui-liveplot`, `native-theme`, `gpui-video-player`. Отклонены:
`crepuscularity-ssr`, `gpui_hexeditor`, `open-gpui-docking`,
`gpui-flowtext` (последняя — актуальна для Chronos-IDE, не ChronOS).

Пуш: `1f059da` на `origin/master`, 75 коммитов, синхронизировано.

## Ближайшее (открытая очередь)

- Итерация дистиллятов иконной системы (Claude Design, промпт выдан):
  core-глиф 45-55% холста, кольцо убрать; циан канон `#5fd3e8`.
- Задание №14-эквивалент закрыто (DeepSeek). Открытых заданий в поле
  нет — очередь пуста, следующий шаг за Архитектором/пользователем.

## Дальше — светлая тема

Светлая тема: Catppuccin Latte отменён как визуальный язык (глазам
плохо), принята айдентика "атом/киберпанк/сигилы/сакральная
геометрия/Хронос" — холодная сине-лавандовая база, неон только в
линиях/glow/watermark, не в заливке. Эталон — `design/Project
Switcher.dc.html` вариант "Light C", рационализация в `DECISIONS.log`.
Порт в `crates/ui/src/theme/schemes.rs::light_scheme()` НЕ начат —
отдельная кодовая задача, часть визуала (watermark/glow-рёбра) может
не лечь 1:1 на GPUI-слои.

## На горизонте (пользовательский бэклог, не расписано в брифы)

- Gradient borders — порт из `Source/` (Kael-вырез уже принят,
  фундамент есть), ждёт свободного агента.
- Пересчёт `status.*` цветов темы под канонiчный Catppuccin (сейчас
  смесь Catppuccin + Tailwind-суррогатов — несостыковка всплыла при
  разборе urgency-цветов в notifications, не экстренно).
- `desktop_terminal` (Grok №11, спайк) — жизнеспособность доказана,
  продуктовое API для desktop-виджетов — отдельная веха, не начата.
- `stash@{1}` (`tmp-foreign-wip-for-grok-verify`) — живые
  интеграционные тесты network/upower Hermes, единственная копия,
  нигде не закоммичены — решить, отдавать отдельным заданием или
  разбирать вручную.

## Боковые панели (правая — капстоун 2026-07-21, в полёте)

- **Правая:** спека+план `docs/superpowers/…-right-side-panel*`.
  Принято: Task 1 `net_stats` `dbce8ac`, Task 2 `Theme::font_ui`
  `18c88f0`, Task 7 скелет `da744a2`. Остальное — по плану 3–6/8–12.
  MPRIS full-card / art / expanded — в плане v1 ограничениями.
- **Левая** — agent-панель с чатом: T107 ПРИНЯТ целиком (layer-shell
  overlay, Hermes ACP чат, sessions sidebar, drag-resize, композер), T108
  (мульти-агентный свитчер) ПРИНЯТ (real modes/models из ACP), T109 (Agent
  Thread canvas по мокапу) ПРИНЯТ с правками архитектора живьём —
  `10fa206`. Resize-регрессия закрыта (`fbcadd6`).
  **Поведение панели сменилось** (2026-07-23, живая сессия с
  пользователем): hover-peek ОТКЛЮЧЁН (закомментирован, не удалён) —
  панель теперь keybind-toggled dock (IPC `toggle-side-panel-left`,
  зеркалит `toggle-launcher`; hyprland-bind пользователь добавляет сам).
  Resize вниз до ~36px сворачивает thread-column в rail (сайдбар-рельс,
  симметрично кнопке лаунчера). Exclusive zone (окна не тайлятся под
  панель) ПОПРОБОВАНА и ОТКЛОНЕНА тем же вечером — двигать тайловые окна
  при каждом открытии/резайзе чат-панели оказалось плохим UX; идея как
  opt-in-режим (привязка к ховеру) отложена, не реализована.
  Хвосты: ghost-trail при резайзе (форк, отложено), дропдаун-jank,
  **нет каретки/курсора в композере** (самопальный текст-инпут без
  курсора/выделения — см. ниже, `ccf-gpui-widgets`), модели/режимы теперь
  фетчатся на connect (`create_session()`), не ждут первого промпта.
  Детали — HANDOFF, `DECISIONS.log` 2026-07-23, `T109-...md`.
- Кандидаты на потом (не v1): `gpui-video-player`, `gpui-liveplot`
  (разведаны 2026-07-20) — не подменять текущий план.

## Текстовый ввод — `ccf-gpui-widgets` (разведано 2026-07-20)

Самая продуктово значимая находка сегодняшнего дня — закрывает
named gap, а не абстрактную возможность. `DECISIONS.log` 2026-07-16
отказался от текстового ввода Kael как overkill («2987+243 LOC +
отсутствующий undo_manager… launcher-ввод добить руками») и оставил
заглушку. Boковая agent-панель с чатом (см. выше) без многострочного
текстового поля с курсором/историей/фокус-навигацией не строится.

`ccf-gpui-widgets` (18000 строк, v0.1.0) даёт ровно это:
`text_input.rs`/`cursor_blink.rs`/`editing_core.rs`/`focus_navigation.rs`
+ россыпь готовых виджетов (`file_picker`, `dropdown`, `slider`,
`toggle_switch`, `confirmation_dialog` и др. — не всё нужно, но список
широкий). `gpui = "0.2.2"` голый (версия совпадает с нашим форком),
`smol` внутри — паттерн executor'а не проверен (тот же класс вопроса,
что у `gpui-animation`).

**Трение — их собственная плоская `Theme` (`u32`-поля:
`bg_primary`/`text_primary`/…), не `gpui-component`.** Дешевле, чем
казалось: структура семантически близка нашей, виджеты принимают
`custom_theme: Option<Theme>` явно — маппинг `chronos_ui::Theme` → их
`Theme` это одна функция на десяток полей, не архитектурная работа.

**Разведано компиляцией 2026-07-21 (Hermes) — вердикт: ВЕНДОР ОБЯЗАТЕЛЕН,
ОТЛОЖЕН.** НЕ компилируется против форка: 58 ошибок, 5 классов форк-дельт
(`FocusHandle::focus(+cx)` ×43, `update_entity→R` ×3, `Corner` удалён,
`Bounds::from_corner_and_size` удалён, `ScrollHandle::max_offset` сменил
тип). Animation-паттерн, не rsx — `[patch]`-зависимостью не подключить.
Ввод хирургически извлекаем (`text_input+editing_core+cursor_blink+
focus_navigation`, 4 файла), но дельты чинить ровно в них. Тема-маппинг
нетривиален: их 52 плоских `u32`-поля vs наши `Hsla`-группы → адаптер
`Hsla→u32` ~40 полей + коллизия `gpui::Global`. **Отложено:** named gap
реален, но не на критическом пути правой панели; вернёмся отдельной
задачей к agent-панели. Отчёт: `report-log/hermes-report-widgets-recon.md`.

**Апдейт 2026-07-23 — gap теперь на критическом пути.** T109 (левая
agent-панель, Agent Thread canvas) сдана; живой смок пользователем сразу
поймал ровно то, что здесь предсказано: "нет моргающей палочки когда я
ввожу" — самопальный `text_input` в `composer.rs` не рисует курсор,
выделение, wrap-навигацию (гарантированный fallback из брифа T109 C-2,
`gpui-component` заблокирован конфликтом версий gpui — форк
`zed-industries/zed` git-gpui vs наш `gpui-ce`, `E0432` на
`AsyncApp/Result/SharedString`). Вендоринг `ccf-gpui-widgets` — тот же
5-класс форк-дельт (`FocusHandle::focus(+cx)`, `update_entity→R`, `Corner`
удалён и т.д.), что и в разведке, но теперь оправдан: реальный пользователь
уже упёрся в реальный gap на реальной панели, не абстрактная возможность.
Следующий шаг — отдельная задача на хирургический вендоринг
(`text_input+editing_core+cursor_blink+focus_navigation`, 4 файла + тема-
адаптер), не блокирует остальной T109-канвас.

## Кандидаты на переходы/анимацию — `gpui-animation` (разведано 2026-07-20)

Не привязано к панелям, применимо уже сегодня. `gpui = "0.2.2"` без
`gpui-component`, спит на нашем же GPUI executor (`cx.spawn(...)
.detach()` — сама библиотека соблюдает правило, которое мы весь день
ловили как баг у своих виджетов), `smol::Timer` — просто future внутри
таски, третьего рантайма не заводит. Даёт декларативный слой ПОВЕРХ
уже вшитых в форк `EasingCurve` (`gpui/src/easing.rs`, находка DeepSeek
№2): `.transition_on_hover(duration, curve, |state, style| ...)`,
умная интерполяция HSLA по кратчайшему пути, откат при прерывании одной
анимации другой. Прямой кандидат применения — hover шести виджетов
правого кластера бара (Mimo №11, сейчас мгновенный щелчок цвета через
голый `.hover(|s| s.bg(...))`) стал бы плавным затуханием без ручной
анимационной логики. Пометка: README называет себя «early development,
API subject to change» — готовиться к breaking changes при апдейте.

**Разведано компиляцией 2026-07-20 (Grok №18) — статус НИЖЕ, чем у
gpui-form/gpui-rsx.** НЕ компилируется out-of-the-box: 3 API-дельты
форка против crates.io `0.2.2` (`AsyncApp::update` non-fallible,
`BoxShadow.inset` — лишнее поле, `Style.text` `#[refineable]`).
После 3 хирургических правок их исходников (~15 строк) — компилируется
и живой hover-переход подтверждён grim (тёмный → промежуточный синий →
целевой голубой, не snap). **Правило для будущих разведок:** совпадение
семвера (`0.2.2` у обеих сторон) НЕ значит совместимость ABI, когда одна
сторона — git/path-форк, а другая — crates.io. Взять эту библиотеку =
взять на сопровождение форк её кода (patch+vendored tree), не просто
добавить строку в Cargo.toml.

**РЕШЕНИЕ: БЕРЁМ, ВЕНДОРНУТО 2026-07-21.** Крейт `Source/gpui-animation/`
(Grok, принят) — копия `@ad77bea` + 3 форк-дельты + `PATCHES.md` под
будущий апдейт, атрибуция в `Source/NOTICE`, проведён в `Source`
workspace.members, `cargo check` против форка зелёный (26с, 0 ошибок).
Следующий шаг (не начат): зависимость `crates/app` + пилот на hover
правого кластера бара (Mimo №11 — сейчас голый `.hover`).

## JSX-разметка — `gpui-rsx` (разведано и ПОДТВЕРЖДЕНО компиляцией, 2026-07-20)

`gpui-rsx` разведано компиляцией 2026-07-20 (Cline №2) — компилируется
против нашего форка без правок их кода, auto-`.id()` подтверждён с двух
сторон (их codegen + наша трейт-граница `StatefulInteractiveElement`).
Дисциплина использования зафиксирована: ТОЛЬКО individual attributes
(`bg={theme.bg.primary}`), `class="bg-*"` запрещён (хардкодит палитру
мимо `Theme::global`). Пилот на `system_popup/view.rs` (29 `div()`,
38 `.child(`) — не роздан.

**РЕШЕНИЕ: БЕРЁМ, во владение (вендор, не dep). ВЕНДОРНУТО 2026-07-21.**
rsx — чистый proc-macro (deps только syn/quote/proc-macro2,
gpui-зависимости нет → типизируется в потребителе), поэтому и компилился
без правок. Крейт `Source/gpui-rsx/` (Cline, принят) — побайтовая копия
`@307a0461`, 0 правок их кода, атрибуция MIT в `Source/NOTICE`, проведён
в workspace.members, `cargo check` зелёный (3.95с). Коммит Source
`99cab5e`. Обоснование вендора vs `gpui-rsx="0.6.0"` одной строкой:
владение (слабый версионный контроль автора — свой пиннутый форк
надёжнее плавающего dep). Следующий шаг (не начат): пилот на
`system_popup/view.rs` (29 `div()`, 38 `.child(`).

## Известные системные ограничения (не менять походя)

- ~~`overflow_y_scroll()` не резолвится в нашем форке gpui~~ **НЕВЕРНО,
  опровергнуто 2026-07-20** (нужен `.id()` — см. DECISIONS). Ранее считалось —
  только
  `overflow_hidden()` (клип, не скролл). См. ARCHITECTURE.md §4.1.
- `follow_mouse=1` — популярные попапы никогда не закрываются по потере
  фокуса, только явным действием. См. ARCHITECTURE.md §4.1.
- ydotool на этой машине нестабилен (dual-monitor calibration drift) —
  живые клик-смоки синтетическим вводом ненадёжны, финальную приёмку
  кликом делает пользователь лично.

## Волна «Правая боковая панель» — v1 закрыта, v2 принята (2026-07-21)

- [x] v1 T7–T11: hover-peek, MPRIS-карточка, метры CPU/RAM/GPU+сеть, power-row,
      бэкенды `net_stats`/`system_resources`/`power`/audio stream-mute/`font_ui`.
- [x] **v2 по мокапу** `design/System Sidebar.dc.html` на `gpui-rsx` (`7109860`):
      352px, скролл-середина, Catppuccin (отменяет сине-циан), header/permission/
      media/24-метры/диски/power-footer.
- [x] Оживление: MPRIS art/progress (`3d9b8b3`), udisks2 живые диски (`8c8ccb7`).
- [x] Решение gpui-component: **НЕ брать** (+2.66 MiB), рецепт на ветке
      `pilot/gpui-component-spike`. rsx — для статик-разметки, div для динамики.
- [ ] Хвосты: header→живой `ActiveWindow.title`, idle-анимация медиа, T12
      бар-триггер, permission-карта (shell-polkit-агент, отдельный трек).

## Chronos-AUR — порт Alloy Tauri→GPUI (в полёте, 2026-07-21)

Отдельное приложение экосистемы (Путь 2, см. DECISIONS 2026-07-21). План
`Chronos-AUR/docs/port-plan.md`. Фаза 1 (4 эксперта роздано): `aur-core` движок
(Cline), ShellExec fish/zsh/bash (Grok), `aur-app` GPUI-каркас (Hermes), порт
страниц Packages+SystemUpdate (Zed). Фазы 2-3: остальные 5 страниц + shell-IPC +
permission-карта/polkit-агент.
