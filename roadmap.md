# ChronOS — Roadmap

Форвард-лук, одностраничный. Не дублирует детали — за рационализацией
решений см. `DECISIONS.log`, за канонической архитектурой — `ARCHITECTURE.md`,
за оперативным полем (кто чем занят прямо сейчас) — `HANDOFF.md`. Этот файл
отвечает на один вопрос: «куда мы идём и в каком порядке».

Обновлено: 2026-07-19 (поздний вечер).

## Сейчас — активные брифы волны «Top Bar редизайн»

Все хвосты предыдущего захода закрыты (dock right-click подтверждён
живьём, battery-клик снят вопросом — на десктопе без батареи виджет
пуст, чинится System-popup'ом ниже; Mimo №7 и Hermes №12 закоммичены).

- [x] A — Cava-визуализатор — ПРИНЯТО, `c519e2e` + эррата `eb043fd`.
- [x] Border+hover+AUR-бейдж на 4 попапах (Hermes №13) — ПРИНЯТО,
      `8d74583` (локально, не запушено).
- [ ] B (Cline №11) — workspace-точки — не начато.
- [ ] C (Mimo №8) — dock → в бар + кнопка «Пуск» — не начато.
- [ ] D (Mimo №9) — project switcher — не начато, ждёт C. Визуальный
      эталон готов и принят живьём — `design/Project Switcher.dc.html`
      (тёмный + светлый "Light C").
- [ ] E (Hermes №14) — история уведомлений — не начато, разблокирована
      (border-полировка выше принята).

## Направление — chrome на один «пультовый» монитор (принято 2026-07-19)

Отход от традиционного DE: бар/панели/попапы/лаунчер живут на ОДНОМ
пультовом мониторе, второй → холст под окна (Hyprland) + desktop-
виджеты (`plasminal`, named consumer уже в DECISIONS.log 2026-07-10) +
роль додумываем позже. Рационализация — `DECISIONS.log` 2026-07-19
(ночь). Скоуп СЕЙЧАС: точим шелл под пультовый; роль второго отложена.

**Следующая фундаментная задача — consolidation** (после того как Zed
дожмёт system_popup, чтобы не плодить параллельный WIP в баре):
- Designation пультового монитора по **uuid** в конфиге
  (`~/.config/chronos/`, паттерн как `dock.toml`/`projects.toml`).
  `cx.primary_display()==None` на Wayland; форк даёт стабильный
  across-reboot `uuid()` из имени вывода. **Gate:** сперва живьём
  залогировать `uuid()` каждого дисплея, подтвердить Ok+стабильность,
  ПОТОМ строить конфиг (не повторять сюрприз `primary_display`).
- `bar/mod.rs:174` — открывать бар только на пультовом, не на всех.
- Побочно закрывает долг по 8 попапам (при одном chrome дисплейного
  вопроса нет).
Кандидат — Mimo (владеет config-персистом), в изолированном worktree,
ПЕРЕД Mimo №9 (project switcher чище ложится на консолидированный бар).

## Дальше — System popup (замена battery-слота) + светлая тема

System popup: дизайн-мокап ГОТОВ и принят живьём
(`design/System Popup.dc.html` — brightness fill-bar, power-profile
трёхсегмент, gaming-mode toggle с эффект-строкой, сигил-иконка вместо
battery). Спека — `design.md` §6. Код: Zed №2 НЕ принят, доработка
Задание №3 в полёте (фикс дисплея по `window.display` + Bug 2 +
gaming repaint) — см. HANDOFF.

Светлая тема: Catppuccin Latte отменён как визуальный язык (глазам
плохо), принята айдентика "атом/киберпанк/сигилы/сакральная
геометрия/Хронос" — холодная сине-лавандовая база, неон только в
линиях/glow/watermark, не в заливке. Эталон — `design/Project
Switcher.dc.html` вариант "Light C", рационализация в `DECISIONS.log`.
Порт в `crates/ui/src/theme/schemes.rs::light_scheme()` НЕ начат —
отдельная кодовая задача, часть визуала (watermark/glow-рёбра) может
не лечь 1:1 на GPUI-слои.

## На горизонте (пользовательский бэклог, не расписано в брифы)

- MPRIS — переключение между несколькими плеерами (сейчас всегда первый
  Playing).
- Gradient borders — порт из `Source/` (Kael-вырез уже принят,
  фундамент есть), ждёт свободного агента.
- Пересчёт `status.*` цветов темы под канонiчный Catppuccin (сейчас
  смесь Catppuccin + Tailwind-суррогатов — несостыковка всплыла при
  разборе urgency-цветов в notifications, не экстренно).
- `desktop_terminal` (Grok №11, спайк) — жизнеспособность доказана,
  продуктовое API для desktop-виджетов — отдельная веха, не начата.
- `stash@{1}` (`tmp-foreign-wip-for-grok-verify`) — живые
  интеграционные тесты network/upower Hermes, единственная копия,
  нигде не закоммичены — решить, отдавать отдельным заданием или
  разбирать вручную.

## Известные системные ограничения (не менять походя)

- `overflow_y_scroll()` не резолвится в нашем форке gpui — только
  `overflow_hidden()` (клип, не скролл). См. ARCHITECTURE.md §4.1.
- `follow_mouse=1` — популярные попапы никогда не закрываются по потере
  фокуса, только явным действием. См. ARCHITECTURE.md §4.1.
- ydotool на этой машине нестабилен (dual-monitor calibration drift) —
  живые клик-смоки синтетическим вводом ненадёжны, финальную приёмку
  кликом делает пользователь лично.
